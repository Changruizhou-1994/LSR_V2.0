# LSR 寄存器管理软件开发变更记录

- 基线版本：LSR V1.1.3
- 当前开发版本：LSR 2.0
- 记录日期：2026-08-31
- 原则：保留 `LSR_V1.1.3` 原目录，新功能在独立开发目录中实施

## 1. 版本概览

| 版本 | 主要内容 | 状态 |
|---|---|---|
| V1.2.0 | 需求 1：寄存器参数解耦、规则检查、Verilog 名称冲突检查 | 已完成 |
| V1.2.1 | Bit 枚举越界由错误降级为警告 | 已完成 |
| V1.2.2 | Bit 名称关键字规则调整、检查结果分级排序、需求 7 默认值拼接修复 | 已完成 |
| V1.3.0 | 需求 2：搜索与高亮定位 | 已完成 |
| V1.4.0 | 需求 8：显式保存、未保存关闭提示、撤销与重做 | 已完成 |
| V1.4.1 | 搜索作为“工具”下拉菜单项，与“规则检查”并列并排在其后 | 已完成 |
| V1.4.2 | 整库导入统一追加到Module列表末尾 | 已完成 |
| V1.5.0 | 需求 6：编辑位置保持；需求 9：跨屏高DPI自动适配 | 已完成 |
| V1.5.1 | 需求 9：按显示器记忆的舒适度缩放 | 已完成 |
| V1.6.0 | 需求 5：右下角上下文详情与长文本编辑面板 | 已完成 |
| V1.7.0 | 需求 10：四张表混合自适应列宽及描述可见性优化 | 已完成 |
| V1.8.0 | 需求 5/9：BIT缩略图、四宫格约束及右下区域统一缩放 | 已完成 |
| V1.9.0 | 需求 11：Module/Register/BIT Excel式行操作与显式自动编址 | 已完成 |
| V1.9.1 | 需求 11补丁：拖动目标边界高亮线 | 已完成 |
| V1.9.2 | 需求 11补丁：Add/Copy下方新增及右键双向插入 | 已完成 |
| V1.9.3 | 需求 9补丁：下拉框调试后缀清理及跨屏字体同步 | 已完成 |
| V1.10.0 | 需求 12：上下双区域联动调整 | 已完成 |
| V1.10.1 | 需求 12补丁：独立细分割线及宽松拖动命中区 | 已完成 |
| V1.10.2 | 需求 5/9/12补丁：右下字体跨屏同步及底部按钮对齐 | 已完成 |
| V1.10.3 | 需求 12补丁：移除错误扩散到整个编辑区的分割器提示 | 已完成 |
| V1.10.4 | 需求 5/12补丁：右下取值/缩略图重排、页签状态合并及完整外框 | 已完成 |
| V1.10.5 | 需求 13：状态栏信息重构；右下页签基线闪烁修复 | 已完成 |
| V1.10.6 | 需求 13补丁：彻底移除页签窗格灰线并进一步精简状态栏 | 已完成 |
| V1.11.0 | 需求 13最终方案、需求 14：单圆点状态、标题绝对路径、非模态可筛选规则报告 | 已完成 |
| V1.11.1 | 需求 13性能补丁：保存完成后在独立线程执行规则检查 | 已完成 |
| V1.11.2 | 需求 13界面补丁：移除当前BIT状态组上方残留灰线 | 已完成 |
| V1.12.0 | 需求 15：精确/星号通配符搜索；崩溃日志与受控退出 | 已完成 |
| V1.13.0 | 需求 16：大型数据库受影响行级撤销与普通编辑局部刷新 | 已完成 |
| V1.14.0 | 需求 17：打开后台全检与保存后受影响Module增量检查 | 已完成 |
| V1.15.0 | 需求 18：旧版数据库结构兼容、业务数据真值保护与原子升级 | 已完成 |
| V1.15.1 | 需求 19：统一新增Register/BIT的用户可见、ATE和SCAN默认值 | 已完成 |
| V1.16.0 | 需求20/21：后台结果修订号防过期与本地工作副本WAL并发优化 | 已完成 |
| V1.16.1 | 需求22：Register默认值与BIT定义一致性、工作副本安全修复与基础回归 | 已完成 |
| V1.16.2 | 需求22补丁：默认值异常边界、完整生成门禁与基础功能异常回归 | 已完成 |
| V1.16.3 | 需求23：规则错误Module/Register/BIT三层联动定位与居中高亮 | 已完成 |
| V1.16.4 | 需求24：规则报告200项结果刷新性能及编辑保存稳定性 | 已完成 |
| V1.16.5 | 需求25/26：撤销重做层级定位；规则报告说明列宽与拖拽 | 已完成 |
| V1.16.6 | 需求27：规则定位及撤销重做的深蓝主焦点与浅灰层级辅助高亮 | 已完成 |
| V1.17.0 | 需求28：工具菜单按Module生成8/16位动态FUSE MAP Excel | 已完成 |
| V1.17.1 | 需求28补丁：桌面Excel兼容性、Gen Document子菜单及数据库名导出命名 | 已完成 |
| V1.17.2 | 需求28补丁：BIT区完整框线、通用Part、十六进制地址及同级工具菜单 | 已完成 |
| V1.17.3 | 需求28补丁：FUSE MAP空片全0初值及Part前导零文本格式 | 已完成 |
| V1.17.4 | BIT取值原文编辑、宽松warning及Gen Fuse Map字体统一 | 已完成 |
| V2.0 | 基于V1.17.4有效功能整理并正式发布2.0 | 已完成 |
| V2.0开发补丁1 | 需求32：打开/检查严格只读、默认值手动确认重算及兼容性统一报告 | 已完成 |
| REQ045 Preview | 需求44全局字体缩放一致性 + 需求45缩略图等宽字体 | 已完成Preview，等待用户确认 |

### 1.1 固定开发环境与发布基线

- 项目权威Python环境为 `C:\codex_area\LSR_V1.2.0\.build_python39\python.exe`，版本3.9.13。
- 当前终端是否能直接解析裸命令 `python` 与项目环境是否存在是两回事。V1.6.0测试过程中一次裸命令调用因 `PATH` 未注册而失败，随后所有语法检查、测试和打包均使用上述Python 3.9.13完整路径完成，未更换项目环境。
- Codex工作区附带的Python运行时缺少PySide2，因此不作为本项目运行或发布环境。
- GUI/数据库/构建依赖沿用Python 3.9环境内的PySide2、SQLAlchemy、Flask-SQLAlchemy和PyInstaller 5.13.2。
- 正式构建入口为 `build_release.ps1` 和 `LSR.spec`；当系统阻止PowerShell脚本时，只对单次构建使用临时 `-ExecutionPolicy Bypass`。
- V1.9.0发布验证基线为41项单元测试、7项离屏界面测试以及成品EXE `--smoke-test`。
- 环境版本、测试结果、发布路径和文件校验值属于后续每次交付的必需记录，不再从交付说明中压缩省略。

## 2. V1.2.0：寄存器参数和规则检查

### 2.1 参数解耦

- 新增“设置 → 寄存器参数 → 位宽与地址递增...”入口。
- 新增“寄存器参数设置”窗口。
- 默认寄存器位宽和地址偏移递增作为两个独立参数保存。
- 地址步长只允许固定值 1、2、4。
- 新建寄存器使用默认寄存器位宽。
- 寄存器新增和地址重排只使用用户配置的固定地址步长，不再根据寄存器位宽或 Bit 数量推导。
- 已有寄存器仍使用数据库中各自的 `REG.width`。

### 2.2 单一配置源

- 根目录 `design_param.cfg.txt` 作为权威配置文件。
- GUI 和已配置的生成脚本读取同一份配置。
- 保留旧字段 `data_width` 供现有生成器兼容，同时新增语义明确的：

```ini
default_register_width = 32
register_address_step = 4
```

### 2.3 规则检查

- 启用“工具 → 规则检查”。
- 新增结果窗口及“重新检查”“定位”“关闭”按钮。
- 支持双击结果定位 Module、Register 或 Bit 单元格。
- 存在阻断性错误时停止设计 Verilog 和 DV Verilog 生成。
- 主要检查项目：
  - Module、Register、Bit 名称格式。
  - Module 基地址和 Register 偏移地址重复。
  - Register 位宽和默认值范围。
  - Bit 起始位、位宽、默认值和枚举值范围。
  - Bit 越过 Register 位宽。
  - 同一 Register 内 Bit 重叠。
  - Register 默认值与 Bit 组合值不一致。
  - Register 名、Bit 名及最终 Verilog 信号名冲突。
  - 大小写规范化后产生的名称冲突。
  - 不同 `Register + Bit` 组合产生同一个信号名。

### 2.4 新增代码文件

| 文件 | 作用 |
|---|---|
| `design_settings.py` | 参数加载、校验、保存和地址步长计算 |
| `rule_checker.py` | 与 Qt、SQLAlchemy 解耦的寄存器规则检查器 |
| `dialogs.py` | 参数设置窗口和规则检查结果窗口 |
| `tests/test_design_settings.py` | 参数和地址步长测试 |
| `tests/test_rule_checker.py` | 名称、位宽、默认值和冲突规则测试 |

### 2.5 修改的主要文件

| 文件 | 修改内容 |
|---|---|
| `LSR.py` | 接入设置窗口、规则检查、生成阻断、固定地址步长和资源路径 |
| `main.ui` | 新增参数子菜单并启用规则检查入口 |
| `design_param.cfg.txt` | 增加默认位宽和固定地址步长配置 |
| `LSR.spec` | 更新 PyInstaller 数据文件、隐藏导入和版本名称 |
| `build_release.ps1` | 增加可重复执行的发布脚本 |
| `scripts/gen_*.py` | 生成脚本统一读取根目录配置 |

## 3. V1.2.1：Bit 枚举检查放宽

- `BIT_ENUM_OUT_OF_RANGE` 从“错误”调整为“警告”。
- 枚举值越界仍显示在规则检查结果中。
- 枚举警告不再阻止 Verilog 生成。
- Bit 本体默认值越界仍是阻断性错误。
- 新增单元测试，验证枚举越界的严重程度为“警告”。

## 4. V1.2.2：规则分级和名称语义调整

### 4.1 Bit 名称关键字

- Bit 原始名称允许使用 `REG`、`LOGIC` 等 Verilog/SystemVerilog 关键字。
- 不再仅因为 Bit 原始名称是关键字而报告错误。
- 改为检查规范化后的 `Register_Bit` 拼接名称及派生信号名。
- Module 和 Register 原始名称仍执行关键字检查。
- SystemVerilog 关键字集合补充 `logic`、`bit`、`interface`、`package`、`always_ff` 等关键字。

### 4.2 结果分级和排序

- 规则结果按 Module 分组。
- 每个 Module 内先显示错误，再显示警告。
- 新增优先级列：
  - `P0`：名称冲突、非法名称、Bit 越界、Bit 重叠、默认值越界等严重错误。
  - `P1`：地址、属性、组合值不一致等其他阻断性错误。
  - `P2`：非阻断性警告，例如 Bit 枚举值越界。
- 结果窗口保留错误、警告数量统计。

## 5. V1.2.2：需求 7 默认值拼接修复

### 5.1 原因

旧实现存在以下问题：

```python
dv += bit.default_value << bit.position
```

- 使用加法而不是按位或。
- 没有根据 Bit 位宽生成掩码。
- Bit 默认值不是整数时会被静默改成 0。
- 修改 Bit 位宽时不会重新计算 Register 默认值。
- 新增、复制、删除 Bit 后没有统一更新 Register 默认值。

### 5.2 新算法

新实现统一使用：

```python
field_mask = (1 << bit.width) - 1
register_default |= (bit.default_value & field_mask) << bit.position
```

计算前执行以下校验：

- Register 位宽必须是正整数。
- Bit 起始位必须是非负整数。
- Bit 位宽必须是正整数。
- Bit 不得超出 Register 位宽。
- Bit 之间不得重叠。
- Bit 默认值必须是整数。
- Bit 默认值必须满足 `0 <= value < 2^width`。

任何一项不合法时拒绝保存并回滚，不再静默修改数据。

### 5.3 自动重算时机

- 修改 Bit 起始位。
- 修改 Bit 位宽。
- 修改 Bit 默认值。
- 新增 Bit。
- 复制 Bit。
- 删除 Bit。
- 撤销相关 Bit 操作时沿用相同计算入口。

### 5.4 UI 行为

- Register 表“默认值”列改为只读。
- 鼠标提示说明该值由所有 Bit 默认值自动计算。
- 保存失败后回滚数据库，并重新显示原 Bit 数据。
- 修改成功后只更新当前 Register 默认值单元格，避免无关表格跳转。
- 新增和复制 Bit 时自动寻找连续空闲位，避免创建后立即发生重叠。

### 5.5 新增代码和测试

| 文件 | 作用 |
|---|---|
| `register_logic.py` | 严格默认值组合及连续空闲位搜索 |
| `tests/test_register_logic.py` | 按位或、掩码、越界、重叠、非法类型和空闲位测试 |

## 6. V1.3.0：搜索与高亮定位

### 6.1 菜单和窗口

- V1.3.0 在菜单栏增加搜索入口；V1.4.1 将其移至“工具”下拉菜单的“规则检查”之后。
- 新增 `Ctrl+F` 快捷键。
- 搜索窗口提供搜索词、搜索范围、结果状态、“上一个”“下一个”和“关闭”。
- 支持 Enter、Shift+Enter、Esc 和首尾循环导航。

### 6.2 搜索范围

- Module：名称、基地址、简要描述、详细描述。
- Register：名称、地址、位宽、权限、默认值、可见性、ATE和描述。
- Bit：名称、位置、位宽、权限、默认值、Key、可见性、SCAN、SET、CLEAR和描述。
- Bit取值：取值和描述。
- 数值字段同时建立十进制和十六进制搜索标记。

### 6.3 稳定定位

- 新增字段级 `SearchResult` 数据结构。
- 每条结果保存 Module、Register、Bit、BitValue 数据库 ID 和字段名。
- 跳转时重新计算地址排序和Bit位置排序后的显示行。
- 同步设置四级当前对象，选择目标列并滚动至可见位置。
- 不使用搜索开始时的表格行号，避免排序变化后跳错对象。

### 6.4 新增和修改文件

| 文件 | 修改内容 |
|---|---|
| `search_engine.py` | 纯数据搜索索引、范围过滤、文本及数值匹配 |
| `dialogs.py` | 新增搜索窗口和键盘导航 |
| `main.ui` | 搜索作为“工具”中的同级菜单项，排在“规则检查”之后，并保留 `Ctrl+F` |
| `LSR.py` | 搜索接入、按数据库 ID 定位和单元格高亮 |
| `tests/test_search_engine.py` | 搜索字段、范围和数值匹配测试 |
| `tests/test_search_dialog.py` | 结果循环及 Enter/Shift+Enter 测试 |
| `tests/ui_search_navigation_smoke.py` | 样例数据库跨表定位集成测试 |

## 7. 测试记录

### 7.1 V1.3.0 自动测试

- 单元测试：27 项全部通过。
- Python 3.9 源码编译检查：通过。
- Qt 无界面 UI 加载测试：通过。
- 样例数据库搜索与跨表 ID 定位测试：通过。
- PyInstaller 成品自检：通过，退出码 0。

### 7.2 样例数据库检查

样例库：`scripts/dayu_top.db`

- Bit 枚举越界警告：27 项。
- 阻断性错误：18 项。
- Bit 原始名称关键字错误：0 项。
- 可正常计算默认值的 Register：179 个。
- Register 默认值与 Bit 组合值不一致：3 个。
- 存在 Bit 重叠或其他非法定义、无法安全计算：1 个。

以上是样例数据库本身的检查结果，没有自动修改样例数据。

### 7.3 当前发布文件

- 文件：`release/LSR_V1.7.0.exe`
- 大小：56,449,408 字节。
- SHA-256：`3163835FB8766E17D036D429A9570F893B762C3DC56EA42C97C03C1FC7DD7244`

## 8. 当前需求状态

| 编号 | 需求 | 状态 |
|---|---|---|
| 1 | 寄存器规则检查及参数设置 | 已完成 |
| 2 | 搜索与高亮定位 | 已完成 |
| 5 | 右下角上下文详情编辑面板 | 已完成 |
| 6 | 编辑后保持当前位置 | 已完成 |
| 7 | 修复寄存器默认值拼接错误 | 已完成 |
| 8 | 显式保存与撤销/重做 | 已完成 |
| 9 | 跨屏幕高DPI自动适配与舒适度缩放 | 已完成 |
| 10 | 表格列宽自适应与描述可见性优化 | 已完成 |
| 11 | Excel式插入、拖动、剪贴板与显式自动编址 | 已完成 |
| 12 | 上下双区域联动调整 | 已完成 |

## 9. 已知约束

- 当前发布目录必须保留 `scripts`、`design_param.cfg.txt` 和 `ReMSettings.ini`。
- V1.2.2 不会自动修复导入数据库中的历史默认值不一致，只会报告；编辑相关 Bit 后会按新算法重新计算。
- 旧版本正在运行时，Windows 会锁定对应 EXE；因此补丁版本使用新的版本文件名发布。

## 10. 需求 2 搜索功能分析与实施记录

- 搜索功能最初在 V1.3.0 作为顶级入口实现；V1.4.1 根据使用反馈移入“工具”下拉菜单并排列在“规则检查”之后。
- 当前工程没有搜索窗口、搜索快捷键或跨四张表的统一搜索索引。
- `navigateToRuleIssue()` 已具备部分跨表定位能力，可以抽取为通用定位服务。
- 当前 Module、Register、Bit 点击处理混用了排序前索引和排序后行号，搜索功能不能直接依赖这些行号。
- 搜索结果应保存数据库 ID 和字段名，并在每次跳转时重新计算当前显示行。
- 搜索窗口建议新增到 `dialogs.py`，搜索索引和匹配逻辑建议放在新的纯数据模块中，便于单元测试。
- 搜索已按上述分析实施，涉及 `main.ui`、`LSR.py`、`dialogs.py` 和新增的纯数据搜索模块及测试。
- 需求 2 的详细字段、交互、定位流程、验收条件和实施记录已经写入主需求文档第 3 节。

## 11. 需求 8 显式保存与撤销/重做方案分析

### 11.1 现状结论

- 当前 Module、Register、Bit 和 Bit取值的编辑、增删、复制及部分刷新流程会直接调用 `db.session.commit()`，因此用户每次操作都会立即改写原数据库。
- 当前 `operationStack.py` 只有撤销栈，没有重做栈和“已保存位置”；保存的对象多为 ORM 对象、表格行号或旧值。
- 表格按地址或Bit位置重新排序后，行号不是稳定身份；删除、刷新、切库后 ORM 对象也可能失效。
- 当前没有统一的窗口关闭、打开其他数据库和新建数据库前的未保存确认流程。

### 11.2 选定方案

采用“临时工作副本 + 显式保存检查点 + 命令式撤销/重做”：

1. 打开或新建数据库时建立临时工作副本，所有界面编辑只提交到工作副本。
2. `Ctrl+S` 经过完整性检查和同目录临时文件后原子替换目标文件。
3. 撤销/重做命令使用数据库 ID 和前后快照，不再把显示行号作为对象身份。
4. 维护当前命令位置和已保存位置；二者不同时显示未保存标记 `*`。
5. 保存不清空撤销历史；保存后可以继续撤销，回到保存位置时自动清除未保存标记。

没有选择 SQLite 全会话长事务。原因是现有代码提交点多，长事务会长期锁定数据库，并放大局部异常的回滚范围；工作副本对现有代码侵入更可控，也能保证保存前原文件不变。

### 11.3 UI 决策

- 左上角现有“文件”菜单的下拉子菜单在“打开数据库”后新增“保存”和“另存为...”，快捷键分别为 `Ctrl+S` 和 `Ctrl+Shift+S`；不新增与“文件”平级的保存入口，也不在表格区域增加保存按钮。
- 菜单栏新增“编辑”菜单；现有“撤销”从“文件”移入“编辑”，并新增“重做”。
- 撤销使用 `Ctrl+Z`；重做使用 `Ctrl+Y`，同时接受 `Ctrl+Shift+Z`。
- 主界面表格旁不新增保存按钮，保存只有一个全局含义。
- 需求 5 最终取消局部“保存/应用修改”按钮；右下字段编辑器直接更新工作副本，全局 `Ctrl+S` 仍是写入目标数据库的唯一保存动作。

### 11.4 安全与业务规则

- 关闭、退出、新建、打开其他数据库前统一提示“保存、不保存、取消”。
- 保存失败不得覆盖或破坏原文件，必须保留工作副本和未保存状态。
- 保存前检测目标文件是否被外部修改，避免静默覆盖。
- 规则检查错误允许保存不完整设计；字段格式和数据库完整性错误阻止保存，严重规则错误继续阻止 Verilog 生成。
- 整库追加、合并选中模块以及 Bit 默认值联动分别作为单条复合撤销命令。
- 数据库合并只改变工作副本，用户显式保存后才覆盖目标数据库。
- 保存数据库使用 SQLite Backup API 生成同目录临时库，校验后原子替换；不直接复制仍打开的 SQLite 文件。
- 全局保存和撤销栈只管理寄存器数据库内容，主题、窗口布局和寄存器参数等应用级设置仍由各自设置窗口单独保存。

完整交互流程、命令栈语义和验收条件见 `LSR_功能改造需求.md` 第 9 节；该方案随后已在 V1.4.0 中实施。

## 12. V1.4.0：显式保存与撤销/重做实施记录

### 12.1 工作副本和保存

- 新增 `database_workspace.py`，负责打开/新建工作副本、SQLite完整性检查、目标文件指纹、原子保存和临时目录清理。
- 打开数据库时通过 SQLite Backup API 复制到会话临时目录，SQLAlchemy 只连接 `working.db`。
- 新建数据库的默认 Module、Register 和 Bit 只写入工作副本，第一次 `Ctrl+S` 前不创建目标数据库。
- 保存使用 SQLite Backup API 写入目标目录 `.lsr-save-*.db`，校验成功后调用原子替换。
- 保存前比较目标文件的大小、修改时间和 SHA-256，外部修改时不会静默覆盖。
- 保存失败保留工作副本和未保存状态，可以重试或另存为。

### 12.2 菜单和关闭行为

- 左上角“文件”菜单增加“保存”“另存为...”和“退出”。
- 新增“编辑”菜单，包含“撤销”和“重做”。
- 窗口标题显示目标文件名；存在未保存内容时添加 `*`。
- 关闭窗口、退出、新建或打开其他数据库前统一检查未保存状态。
- 提示提供“保存、不保存、取消”：保存失败或取消时不关闭；不保存时只丢弃工作副本，原文件不变。

### 12.3 撤销和重做

- `operationStack.py` 改为带游标和保存检查点的命令历史。
- 每条命令保存四张表中实际变化行的修改前/修改后快照及对象数据库 ID。
- 撤销按子对象到父对象删除、按父对象到子对象恢复，支持 Module、Register、Bit 和 Bit取值的增删改。
- 保存只更新保存检查点，不清空历史；撤销到保存位置自动清除未保存标记。
- 撤销后执行新操作会删除重做分支。

### 12.4 测试

- 新增 `tests/test_database_workspace.py`：验证保存前原库不变、保存、丢弃、另建数据库和外部修改检测。
- 新增 `tests/test_database_history.py` 和 `tests/test_operation_stack.py`：验证行快照撤销/重做、游标和保存检查点。
- 新增 `tests/ui_save_workflow_smoke.py`：验证编辑、标题标记、撤销、重做、保存、取消关闭及不保存关闭的完整流程。
- V1.4.0 源码阶段共 37 项单元测试通过，主界面加载、搜索定位和保存流程无界面集成测试通过。
- `release/LSR_V1.4.0.exe --smoke-test` 成品自检通过，退出码为 0。

## 13. V1.4.1：搜索菜单位置调整

- 移除菜单栏顶级“搜索”入口。
- 在左上角“工具”下拉菜单中增加“搜索”。
- “规则检查”和“搜索”是“工具”下的两个同级菜单项，“搜索”排列在“规则检查”之后；没有创建“规则检查”的下级子菜单。
- `Ctrl+F`、搜索弹窗、范围选择、结果循环、跨表定位和高亮行为保持不变。
- 37 项单元测试、主界面加载、搜索导航、保存工作流和 V1.4.1 打包成品自检均通过。

## 14. V1.4.2：整库导入末尾追加

- “文件 → 合并到当前库”不再复用来源Module基地址。
- 所有来源Module均按来源地址顺序导入，并从当前最大Module基地址加1开始分配连续临时地址。
- 即使来源地址与当前库重复，或者来源库内部地址规划与当前Bus Matrix不同，也能完成导入。
- 导入Module全部集中在地址排序列表末尾；导入后自动选择并滚动到第一个新Module。
- 弹窗显示导入数量和临时地址范围，并提醒用户按照Bus Matrix手动修正。
- 用户编辑Module基地址后，表格按新地址自动排序，不修改其他Module或Register偏移地址。
- 整库导入仍只进入临时工作副本，支持一次撤销/重做，用户显式保存后才写入目标数据库。
- 修复撤销整库导入后界面仍引用已删除ORM Bit对象的问题，撤销/重做后使用新会话按数据库ID重载。
- 新增 `merge_logic.py`、4项地址分配单元测试和 `ui_merge_append_smoke.py` 集成测试。
- V1.4.2 共41项单元测试通过；整库追加、撤销/重做、搜索、保存和成品启动自检均通过，成品自检退出码为0。

## 15. V1.5.0：编辑位置保持与跨屏高DPI适配

### 15.1 编辑位置保持

- 新增统一的表格视图状态捕获和恢复逻辑，保存当前行、列、横向滚动位置和纵向滚动位置。
- Module、Register、Bit 和 Bit取值均按数据库 ID 定位；刷新后重新计算排序显示行，不再把旧显示行号作为对象身份。
- 修复 Register 刷新后被 `current_selected_row=0` 二次覆盖、Bit取值刷新后固定选择第0行的问题。
- 修改 Module/寄存器地址或 Bit 起始位引发重新排序时，光标跟随同一数据库对象移动到新行，并保持原编辑列。
- 简化表格点击处理，统一执行“排序后显示行 → 数据库关系列表索引”的映射。
- 表格数据刷新不再反复覆盖用户拖动后的列宽。

### 15.2 撤销时的内存访问冲突修复

- 回归测试发现：撤销/重做会释放当前 SQLAlchemy 会话，旧的 ORM 对象随即脱离会话；位置恢复逻辑直接读取旧对象属性会触发 `DetachedInstanceError`。
- 在无屏幕 Qt 测试中，异常后的原生消息框会进一步触发 Windows 访问冲突，表现为 `python.exe` 引用空地址。
- 新增安全的数据库对象 ID 提取：优先从 SQLAlchemy 身份状态读取主键，不触发脱离对象的属性加载。
- 无屏幕测试环境中的撤销/重做异常改为输出 Python 堆栈，不创建原生消息框；正常桌面环境仍显示错误对话框。
- “编辑 → 撤销/重做”和“整库合并 → 撤销/重做”崩溃路径均已回归通过。

### 15.3 自动高DPI适配

- QApplication 创建前启用 Qt 高DPI缩放、高DPI图标和非整数缩放透传策略。
- 新增 `LSR.manifest`，打包后的 EXE 声明 Windows `PerMonitorV2, PerMonitor` DPI Awareness。
- 主窗口和四张数据表统一使用系统字体，行高和表头高度根据系统字体度量自动计算。
- 监听窗口跨屏和逻辑DPI变化，自动重新应用字体及表格尺寸；没有增加任何手动缩放按钮或设置项。
- 列宽只在界面初始化时设置，普通刷新和DPI变化不覆盖用户调整结果。
- PyInstaller 5 会过滤自定义清单中的DPI节点，因此在打包阶段扩展其生成清单后再嵌入；禁止在单文件EXE生成后修改资源，以免破坏尾部PKG归档位置。

### 15.4 新增测试

- 新增 `tests/ui_position_dpi_smoke.py`，覆盖 Module、Register、Bit、Bit取值编辑位置保持。
- 覆盖地址/起始位改变后的排序跟随、当前列和滚动位置恢复、列宽保持、系统字体和高DPI属性。
- 当前41项单元测试全部通过；主界面加载、保存/撤销、搜索定位、整库合并/撤销和位置/DPI界面测试全部通过。
- `release/LSR_V1.5.0.exe --smoke-test` 成品自检通过，退出码为0；已从成品资源中确认 `PerMonitorV2, PerMonitor` 和 `longPathAware` 清单节点存在。

## 16. V1.5.1：按显示器记忆的舒适度缩放

- 在 Windows Per-Monitor DPI 自动适配之上增加用户舒适度比例，不修改系统 DPI，也不新增按钮、菜单或设置窗口。
- `Ctrl+滚轮`、`Ctrl++`、`Ctrl+-` 以 10% 为步长调整，范围为 80%～200%；`Ctrl+0` 恢复 100%。
- 触控板和高精度滚轮产生的小幅滚动量先累计到标准刻度，反向滚动时清除相反方向余量。
- 应用字体、四张表字体、行高、表头高度以及用户当前列宽同步缩放，编辑对象、当前列和滚动位置保持不变。
- 使用屏幕名称、厂商、型号和序列号生成稳定标识；缺少硬件信息时使用屏幕几何信息兜底。每块显示器通过 `QSettings` 独立记忆缩放比例。
- 修正 PySide 对同一物理 `QScreen` 可能返回不同 Python 包装对象的问题，改用稳定屏幕标识判断，避免缩放过程中误加载旧比例。
- 扩展 `tests/ui_position_dpi_smoke.py`，覆盖滚轮放大、键盘缩小、100%复位、字体/行高/列宽联动以及设置持久化。
- 41项单元测试、主界面、保存/撤销、搜索定位、整库合并/撤销和位置/DPI界面测试全部通过。
- `release/LSR_V1.5.1.exe --smoke-test` 成品自检通过，退出码为0；构建清单已确认包含 `PerMonitorV2, PerMonitor` 和 `longPathAware`。

## 17. V1.6.0：右下角上下文详情编辑面板

### 17.1 界面结构

- 将原右下 BIT 取值区域改为 `BIT详情` 和 `字段编辑` 两个页签。
- `BIT详情`包含当前寄存器的紧凑位域图、当前 BIT 高亮、BIT 起止位/位宽/合法范围以及原 BIT 取值定义表。
- BIT 取值继续使用原有 `Add`、`Copy`、`Delete` 按钮风格；没有有效选择时自动禁用不适用的按钮。
- 位域图关闭横向、纵向滚动条并按可用区域自适应；窗口、DPI或舒适度缩放变化后自动重新适配。

### 17.2 选择联动

- 选择 Module 后，Register 自动定位到按地址排序的第一项，BIT 自动定位到按位位置排序的第一项。
- 选择 Register 后，BIT 自动定位到按位位置排序的第一项。
- 选择 BIT 名称默认进入 `BIT详情`，其取值表始终绑定当前 BIT；选择 BIT 详细描述进入 `字段编辑`。
- 选择 Module/ Register 的简要或详细描述进入 `字段编辑`，其他字段维持表格内直接编辑。

### 17.3 编辑、历史与保存

- 长文本修改使用450毫秒短延时合并连续输入，直接更新工作副本，不设置局部应用/还原按钮。
- 切换字段、保存、另存为、关闭、撤销或重做前会立即提交待处理文本。
- 编辑内容实时同步回对应表格单元格，作为一条数据库操作进入撤销/重做历史，并触发现有未保存标记。
- 最终写入目标数据库仍统一使用“文件 → 保存”或 `Ctrl+S`。

### 17.4 稳定性与验证

- 修正 BIT 取值新增后用“取值”误作表格行号的问题；新增和复制后按数据库 ID 定位新记录。
- 空 BIT、空取值列表以及没有选中取值时不再执行无效复制/删除操作。
- 新增 `tests/ui_context_panel_smoke.py`；41项单元测试以及搜索、保存、位置/DPI、整库追加、上下文面板共5项界面冒烟测试全部通过。
- `release/LSR_V1.6.0.exe --smoke-test` 成品自检通过，退出码为0；发布文件大小为56,447,137字节，SHA-256为 `92743474D07054330E61EECC61A84C82F55E7E2C5185B09B76F8F87666D446A3`。

## 18. V1.7.0：表格列宽自适应与描述可见性优化

### 18.1 原因确认

- 旧代码通过4组固定宽度数组初始化所有列，表头模式虽然允许人工拖动，但不会根据内容自动收窄。
- BIT详细描述之前有8个固定列，舒适度放大时这些列同比增宽，导致描述预览被推到视口右侧。

### 18.2 混合自适应实现

- 为Module、Register、BIT和BIT取值表分别建立列角色、最小宽度、最大宽度和描述伸展权重。
- 内容列测量表头、普通单元格和下拉框当前值；结果受上下限约束，避免短字段浪费和长字段无限撑宽。
- 描述列先保证最小宽度，再按权重分配视口剩余空间；Register详细描述权重高于简要描述，BIT详细描述在100%缩放时至少240像素。
- 监听四张表视口尺寸变化，窗口放大后描述列自动获得更多空间；空间不足时保留可读最小宽度并允许横向滚动。

### 18.3 人工列宽与需求9兼容

- 表头人工拖动宽度按表和列记录，普通数据刷新及DPI事件不覆盖。
- 双击表头分隔线清除单列人工覆盖并恢复自动宽度。
- 舒适度缩放时人工列同比缩放，自动列按新字体重新测量，寄存器位域图继续同步适配。

### 18.4 验证

- 新增 `tests/ui_adaptive_columns_smoke.py`，使用2048像素主窗口验证短名称收窄、描述列优先、四表宽度适配、人工宽度、双击复位和110%缩放。
- 原 `ui_position_dpi_smoke.py`继续通过，确认需求6和需求9行为未回归。
- Python 3.9.13下41项单元测试及搜索、保存、位置/DPI、整库追加、上下文面板、自适应列宽6项界面测试全部通过。
- `release/LSR_V1.7.0.exe --smoke-test`成品自检通过，退出码为0；文件大小56,449,408字节，SHA-256为 `3163835FB8766E17D036D429A9570F893B762C3DC56EA42C97C03C1FC7DD7244`。

## 19. V1.8.0：BIT缩略图与右下区域缩放优化

### 19.1 四宫格和上下文标题

- 主界面左右列、每列上下区域显式设置相同伸展权重，继续维持四等分的基础视觉结构。
- `Module > Register > BIT > BIT详情`改为单行中间省略标签，宽度不足时通过悬停查看完整路径；标签最小宽度和尺寸提示为0，不再撑宽右侧整列。

### 19.2 寄存器位域缩略图

- 标尺由左下当前寄存器的 `REG.width`生成，最高标记为 `width - 1`；8、16、32、64位及非标准位宽均使用相应刻度。
- 所有连续未使用位域无条件压缩为灰色分段并保留真实位域提示；有效 BIT 使用低饱和蓝灰色，当前 BIT 使用克制描边高亮。
- 有效 BIT 根据位宽和名称长度获得显示权重；当前 BIT 名称放不下时通过引导线在图下显示完整名称和位域。
- 越界 BIT 不进入正常绘图区，已有规则检查继续负责报告真实数据错误。

### 19.3 舒适度缩放

- 右下标题、页签、BIT摘要、取值表、按钮、字段编辑器以及缩略图全部响应 `Ctrl+滚轮`、`Ctrl++`、`Ctrl+-`和`Ctrl+0`。
- 缩放时同步调整右下布局边距、标题高度、缩略图视图高度、字体、标尺、线宽和场景高度，并按当前寄存器/BIT重新绘制。
- 缩放不改变四宫格比例、当前选择、编辑位置、滚动位置和数据库内容。

### 19.4 验证

- `ui_context_panel_smoke.py`新增四等分几何、标题不撑宽、未使用区灰色压缩、32位与8位动态标尺验证。
- `ui_position_dpi_smoke.py`新增右下页签字体、缩略图高度与场景重绘随舒适度缩放的验证。
- Python 3.9.13下41项单元测试和6项离屏界面测试全部通过。
- `release/LSR_V1.8.0.exe --smoke-test`成品自检通过，退出码为0；成功自检会清除旧阶段/错误日志，避免历史日志干扰判断。
- 发布文件大小56,452,861字节，SHA-256为 `B804F4E4EB7DD81658A2B83DA4F31A3220A8116145F0A7C3B3AD3D415B6041DE`。

## 20. V1.9.0：Excel式行操作与显式自动编址

### 20.1 原UI保持

- 未修改 `main.ui`，没有新增可见顺序列、底部Paste按钮或独立排序窗口；四宫格布局、原表头、配色、列宽策略及Add/Copy/Delete按钮外观保持不变。
- Module、Register和BIT表复用原左侧行号栏作为整行拖动手柄，普通数据单元格继续用于选择和编辑。
- 三张表只增加标准右键菜单：在上方插入、复制、剪切、粘贴并覆盖、删除；同时支持 `Ctrl+C`、`Ctrl+X`、`Ctrl+V`。

### 20.2 视觉顺序与地址编辑

- 新增会话级视觉顺序映射，以数据库ID定位对象，不增加数据库字段，也不改变现有数据库结构。
- 手工修改Module基地址、Register偏移地址或BIT起始位后不再按数值立即重排，选择、当前列、编辑位置和滚动位置继续保留。
- 从左侧行号拖动后只改变视觉顺序，地址保持原值；拖动动作进入统一撤销/重做历史。
- 视觉顺序及待编址状态随数据库操作的撤销/重做上下文一同恢复；打开其他数据库或关闭工作副本时清理会话状态。

### 20.3 插入与剪贴板

- Module、Register和BIT的Add统一改为在当前行上方插入；新行使用作用域内不重复的 `RESERVE_n`并自动进入名称编辑。
- Module受数据库基地址唯一约束，采用当前最大基地址加1作为临时地址但仍在选中行上方显示；Register和BIT暂时沿用目标位置值，等待显式刷新。
- 新Module自动带一个默认Register和BIT，新Register自动带一个默认BIT；新增BIT前检查总位宽是否仍可容纳1位。
- Copy按钮不再立即创建副本，只写入软件内部剪贴板。Module深复制全部Register/BIT/取值，Register深复制全部BIT/取值，BIT深复制全部取值。
- 粘贴覆盖目标内容但保留目标基地址/偏移地址/起始位和父级关系；Cut仅在粘贴成功后删除源对象并清空剪贴板。

### 20.4 显式自动编址与保存

- 顶层“刷新 → 刷新寄存器”根据当前表执行对应规则：Module按当前视觉顺序重新占用已有合法基地址槽位；Register从0开始按固定步长1/2/4递增；BIT从0开始按前一BIT位宽累加。
- Module地址交换先使用事务内临时唯一值，避免SQLite唯一约束导致交换失败；BIT总位宽越界时整次刷新取消，不提交部分结果。
- 拖动、插入、粘贴或删除后进入“视觉顺序待编址”状态；保存和另存为会提示先刷新，避免数据库地址与界面视觉顺序不一致。
- 所有文件生成入口要求工作副本已经通过 `Ctrl+S`保存，未保存或待编址状态不会自动保存、自动编址或直接生成。

### 20.5 验证

- 新增 `tests/ui_requirement11_smoke.py`，覆盖地址编辑不跳行、行号拖动、视觉顺序撤销/重做、保存阻止、Register固定步长刷新、上方插入、Copy语义、粘贴保留目标地址、BIT累加以及Module基地址槽位分配。
- Python 3.9.13下41项单元测试以及搜索、保存、位置/DPI、整库追加、上下文面板、自适应列宽、需求11共7项离屏界面测试全部通过。
- `release/LSR_V1.9.0.exe --smoke-test`成品自检通过，退出码为0。
- 发布文件大小56,462,452字节，SHA-256为 `F5B477085B3F0C25F35F7BCA75A91696A0375257F5D39E153BE4F789F1002388`。

## 21. V1.9.1：拖动目标边界高亮

### 21.1 交互改进

- Module、Register和BIT仍从原左侧行号栏拖动，不增加新列、按钮或常驻控件。
- 拖动期间使用透明覆盖层，在行号区域和完整表格宽度上绘制同一条高亮插入线。
- 指针位于目标行上半区时显示上边界，位于下半区时显示下边界；拖到可见数据末尾后显示在最后一行下边界。
- 高亮色根据当前Qt主题的选中颜色计算，并限制饱和度和亮度，保证深色、浅色主题下清晰但不过度刺眼。
- 指示层不接收鼠标或键盘事件，不改变表格尺寸、列宽、行高和原有拖动落点逻辑；鼠标释放、取消或窗口失焦后立即隐藏。

### 21.2 验证

- 扩展 `tests/ui_requirement11_smoke.py`，覆盖上下半行边界计算、行号区与表格区同步显示、真实鼠标按下/拖动激活以及释放后隐藏。
- Python 3.9.13下41项单元测试和7项离屏界面测试全部通过。
- `release/LSR_V1.9.1.exe --smoke-test`成品自检通过，退出码为0。
- 发布文件大小56,464,198字节，SHA-256为 `44D80203CC154011B05668B8A6FF4196F313E3A463089C48E7455B4C2F2A4753`。

## 22. V1.9.2：Add/Copy下方新增与右键双向插入

### 22.1 行为调整

- Module、Register和BIT区域原有Add按钮由“当前行上方插入”调整为“当前行下方插入”。
- 选中列表最后一行后点击Add，新行直接追加到列表末尾，支持连续向后录入；没有有效选择时同样追加到末尾。
- 三张表右键菜单同时提供“在上方插入”和“在下方插入”；右键“在下方插入”和底部Add按钮共用同一个插入实现。
- Module、Register和BIT区域底部Copy恢复为直接深复制当前行，并将副本放在当前行下方；选中末行时副本直接追加到末尾。
- 底部Copy不修改软件内部剪贴板；右键“复制、剪切、粘贴并覆盖”继续使用V1.9.0的内部剪贴板逻辑。
- Module副本包含全部Register/BIT/取值，Register副本包含全部BIT/取值，BIT副本包含全部取值；Module使用新的临时唯一基地址，Register和BIT暂时沿用源位置并等待刷新。
- 新对象默认字段、Module基地址唯一约束、Register默认BIT、BIT容量检查、视觉顺序待刷新、撤销/重做和保存限制均保持V1.9.0既有规则。
- 不修改 `main.ui`、底部按钮排列、按钮文本、表格列、四宫格比例或拖动高亮线样式。

### 22.2 验证

- 扩展 `tests/ui_requirement11_smoke.py`，覆盖Register和Module选中末行后Add追加到末尾、右键上方插入使用正确目标边界、底部Copy直接复制到下方，以及右键Copy不新增行且正确写入剪贴板。
- 使用项目固定的 Python 3.9.13 环境完成构建；41 项单元测试和 7 项 UI 回归测试全部通过。
- 成品 `LSR_V1.9.2.exe --smoke-test` 退出码为 0。
- 成品大小：56,465,192 字节；SHA-256：`8B9A7B417AF6919C0EC593FEB8942012D4A2C8E0B2B5568537D1B381C86A6A36`。

## 23. V1.9.3：读写权限/Key保护显示与字体适配修复

### 23.1 问题与修复

- Register和BIT读写权限、BIT Key保护原先把内部行列定位拼入下拉框可见文字，导致显示类似 `RW:0:3`、`NO_KEY:0:5` 的调试后缀；V1.9.3改为只显示真实权限或Key值。
- 下拉框的行列信息改为内部动态属性，编辑信号以内部 `(row, column)` 定位保存目标，不再依赖解析可见文字。
- 跨屏幕、DPI变化和舒适度缩放时，逐一同步表格内嵌控件及其弹出列表字体，确保放大后再缩小也与普通表格单元格保持一致。
- 不修改数据库结构、表格列、菜单、按钮和整体UI布局。

### 23.2 验证

- `ui_position_dpi_smoke.py`增加读写权限/Key保护纯业务值和内嵌下拉框字体同步检查。
- 使用项目固定的Python 3.9.13环境完成构建；41项单元测试和7项UI回归测试全部通过。
- 成品 `LSR_V1.9.3.exe --smoke-test` 退出码为0。
- 成品大小：56,465,576字节；SHA-256：`E9729559BBFE4ABAF6BBE20DDBA53B80C8D05B77284BA0622D50B599C4A6DE26`。

## 24. V1.10.0：上下双区域联动调整

### 24.1 布局与交互

- 将主界面重组为一个纵向主分割器：上部横向区域包含Module和BIT，下部横向区域包含Register和BIT详情/字段编辑。
- 唯一的水平分割线横贯整个主窗口；上下拖动时，同一行左右两个面板同步改变高度，左右边界继续保持对齐。
- 上下区域不可完全折叠；默认采用50:50，双击分割手柄恢复50:50。
- 使用QSettings记忆用户最后一次调整比例，窗口关闭前立即保存；数据库刷新、切换、跨屏和舒适度缩放不重置比例。
- 复用原表格、按钮、右下上下文面板和全部信号，不增加按钮、菜单、表格列或数据库字段。

### 24.2 验证

- 更新 `ui_context_panel_smoke.py`，由旧的左右列布局断言改为上下双区域和左右对齐断言。
- 新增 `ui_vertical_bands_smoke.py`，覆盖四个功能区的上下归属、全宽纵向调整、Register区域扩展、状态持久化和双击复位。
- 使用项目固定的Python 3.9.13环境完成构建；41项单元测试和8项UI回归测试全部通过。
- 成品 `LSR_V1.10.0.exe --smoke-test` 退出码为0。
- 成品大小：56,467,637字节；SHA-256：`3635193AB816C7BC878E2A52848BDC47F288947D37AFDB5FF1AA8B9A956B0AA7`。

## 25. V1.10.1：独立细分割线视觉补丁

### 25.1 界面调整

- 不改变V1.10.0的上下双区域结构，也不移动Module、Register、BIT、右下详情面板或Add/Copy/Delete按钮行。
- 将主分割器手柄改为独立自绘：保留8像素鼠标命中高度，只显示1像素横线和48×3像素中央短握柄，减少与上方按钮的视觉竞争。
- 分割线使用当前Qt主题调色板，明暗主题切换时自动保持低对比度；拖动、双击复位和比例记忆逻辑不变。

### 25.2 验证

- `ui_vertical_bands_smoke.py`增加手柄类型、8像素命中高度及原有上下布局结构检查。
- 使用项目固定的Python 3.9.13环境完成构建；41项单元测试和8项UI回归测试全部通过。
- 成品 `LSR_V1.10.1.exe --smoke-test` 退出码为0。
- 成品大小：56,468,392字节；SHA-256：`FCE115120F8E031402B07DB90A593A0857A2E1A3C8F5899EF9B29A10C91DF8D3`。

## 26. V1.10.2：右下字体与按钮基线一致性补丁

### 26.1 问题与修复

- 右下路径、当前BIT、合法范围及字段提示标签原先逐个设置显式字体，Windows跨屏时可能继续使用旧屏解析后的较大字号；现改为继承主窗口字体。
- 保留 `Ctrl+滚轮`统一缩放，并在屏幕切换或DPI变化后使用120毫秒防抖重新应用显示设置，清除仍停留在旧屏的状态标签字号。
- BIT取值按钮原先位于 `QTabWidget`页内部，受到页签边框和内边距影响，比左下Register按钮行高约7像素；现将按钮栏移到右下面板最外层底部，与左下按钮共用相同面板边距和底部基线。
- BIT取值按钮只在“BIT详情”页可见；字段编辑页不显示，Add、Copy、Delete逻辑保持不变。
- 不改变四区结构、主分割线、表格、列宽、数据库和保存逻辑。

### 26.2 验证

- `ui_context_panel_smoke.py`增加右下按钮外层归属、左右底部按钮基线及页签可见性检查。
- `ui_position_dpi_smoke.py`增加状态标签缩放、字号一致性及模拟跨屏刷新后清除旧字号检查。
- 使用项目固定的Python 3.9.13环境完成构建；41项单元测试和8项UI回归测试全部通过。
- 成品 `LSR_V1.10.2.exe --smoke-test` 退出码为0。
- 成品大小：56,468,733字节；SHA-256：`F603129DF0A6A17A7BEC6D65DE4939B72395A7A68C3078B02F0BF97BA0BCFB85`。

## 27. 正式需求范围调整（不发布新EXE）

- 用户确认放弃原需求3“批量编辑与寄存器地址编排”和原需求4“数据库版本对比与安全合并”，两项已从正式需求总览、详细设计、实施顺序和待开发列表中删除。
- 为保持V1.2.0至V1.10.2版本记录、测试名称和既有文档引用可追溯，其余需求编号不重排。
- 已经交付的固定步长、行拖动、显式自动编址、整库追加、合并选中模块和事务回滚功能继续保留；不再规划批量编辑窗口、地址编排预览窗口或数据库差异对比系统。
- 本次只更新需求文档和开发变更记录，不修改源码，不生成新的EXE版本。

## 28. V1.10.3：分割线工具提示范围补丁

### 28.1 问题与修复

- “上下拖动可调整上、下两个横向区域”原先设置在整个主分割器上，Qt会在其子控件没有自身提示时沿父级查找，导致鼠标悬停在几乎整个编辑区域都显示该内容。
- V1.10.3移除主分割器全局工具提示，不再显示这条说明；中央灰色握柄、拖动光标、上下拖动和双击复位均保持不变。
- 不修改四区结构、按钮、表格、字体、缩放、数据库或保存逻辑。

### 28.2 验证

- `ui_vertical_bands_smoke.py`增加主分割器和自绘手柄无工具提示检查，并继续覆盖拖动、双击复位和布局结构。
- 使用项目固定的Python 3.9.13环境完成构建；41项单元测试和8项UI回归测试全部通过。
- 成品 `LSR_V1.10.3.exe --smoke-test` 退出码为0。
- 成品大小：56,468,683字节；SHA-256：`809E4613B754DF4FDB5F9211C6DA7A01909DF6C51F0649940D95AA9A08845466`。

## 29. V1.10.4：右下详情布局与完整外框补丁

### 29.1 界面调整

- 右下BIT详情页交换BIT取值表和寄存器缩略图的位置：取值表位于上方并获得主要可伸展空间，缩略图位于下方且保持完整紧凑显示。
- 将“当前BIT、位范围、合法范围”设置为页签栏右侧状态组件，与“BIT详情/字段编辑”共用一行，减少固定纵向占用。
- 新增仅用于视觉分组的右下完整主题边框，将路径标题和页签内容收在同一个与其他三块等大的方框中；不新增业务按钮或数据库字段。
- Add、Copy、Delete按钮仍位于方框下方并与左下Register按钮保持水平对齐；其他三个区域及主分割器不变。

### 29.2 验证

- `ui_context_panel_smoke.py`增加完整外框、路径/页签归属、页签右侧BIT状态以及取值表位于缩略图上方的结构检查。
- 使用项目固定的Python 3.9.13环境完成构建；41项单元测试和8项离屏UI回归测试全部通过。
- 成品 `LSR_V1.10.4.exe --smoke-test` 退出码为0。
- 成品大小：56,468,901字节；SHA-256：`0CAD7F7522AA8727E37E468C6FD2AE3D28F7A8B10FFBDDFE8CAF3C1A84225761`。

## 30. V1.10.5：状态栏信息重构与页签闪烁修复

### 30.1 状态栏调整

- 删除四张表选中单元格时输出的 `row / column / content`调试文字，保留现有保存、搜索、复制、撤销、刷新和生成等限时操作提示。
- 新增数据库文件名、保存/待编址状态、默认寄存器位宽/地址步长及舒适度缩放四个固定状态区；数据库完整路径通过悬停查看。
- 保存状态可区分“已保存”“未保存”和“待刷新编址”，未保存状态采用适配明暗主题的低饱和警示色。
- 状态栏宽度不足时自动隐藏数据库名称和寄存器参数，始终保留保存状态及缩放比例。

### 30.2 同版界面稳定性修复

- 关闭右下上下文页签多余的基线绘制，消除实时上下拖动主分割线时“当前BIT/合法范围”附近灰色横线闪烁。
- 右下完整外框、页签、中央分割线及其他三个区域保持不变。

### 30.3 验证

- 新增 `tests/ui_status_bar_smoke.py`，覆盖数据库、保存/待编址、参数、缩放、紧凑模式及调试信息清理。
- `ui_context_panel_smoke.py`增加右下页签不绘制基线的结构检查。
- 使用项目固定的Python 3.9.13环境完成构建；41项单元测试和9项离屏UI回归测试全部通过。
- 成品 `LSR_V1.10.5.exe --smoke-test` 退出码为0。
- 成品大小：56,470,247字节；SHA-256：`214C7D656809FE142C262BF2B37754C76ADC9B69155F2C9E931D413CE297679F`。

## 31. V1.10.6：页签灰线与状态栏精简补丁

### 31.1 问题与修复

- 截图确认残留灰线不是主分割线，也不是V1.10.5已关闭的页签栏基线，而是右下 `QTabWidget`内容窗格的顶部边框；现移除整个窗格边框，由已有右下完整外框统一承担边界显示。
- 数据库打开、保存和另存提示由完整绝对路径改为文件名，并统一在3秒后消失；完整路径仅作为数据库文件名的悬停提示。
- 固定状态压缩为“文件名、文档状态、`32-bit/步长4`、`100%`”，待编址显示压缩为“● 待刷新编址”，保留原有警示含义。

### 31.2 验证

- 扩展 `ui_context_panel_smoke.py`，检查页签栏和页签窗格两种灰色基线均已关闭。
- 扩展 `ui_status_bar_smoke.py`，检查数据库消息不含目录、状态参数和缩放采用精简文字。
- 使用项目固定的Python 3.9.13环境完成构建；41项单元测试和9项离屏UI回归测试全部通过。
- 成品 `LSR_V1.10.6.exe --smoke-test` 退出码为0。
- 成品大小：56,470,310字节；SHA-256：`29971CAEBEDD759C6D85A4741A103EDDBE68F3ADF7A03A306FCA0FF9E125D67D`。

## 32. V1.11.0：单圆点状态栏与非模态规则检查报告

### 32.1 需求13最终状态栏方案

- 顶层标题显示版本、公司名称和当前数据库绝对路径，存在未保存修改时追加 `*`；底部不再重复数据库路径或文件名。
- 删除V1.10.5/V1.10.6的数据库、文档、寄存器参数、缩放四段固定状态，改为一个主题自适应的彩色圆点和简短状态文字。
- 状态覆盖未打开数据库、正在编辑、未保存、待刷新编址、检查通过、检查通过但有警告、规则检查错误；规则错误使用更大的红点并可点击打开报告。
- `Ctrl+S`先提交当前单元格和右下字段编辑器，再保存并静默规则检查。规则错误不阻止数据库保存，但仍阻止Verilog生成。
- 打开或切换数据库后静默检查一次；`Ctrl+滚轮`只临时显示缩放比例，随后恢复文档/规则状态。

### 32.2 需求14规则检查报告

- 规则检查报告改为非模态窗口，定位或双击错误后报告保持打开，允许用户边查看边修改主窗口数据。
- 报告采用单实例，重复执行“工具 → 规则检查”只刷新和激活原窗口，不创建多个报告。
- 顶部新增模块筛选下拉框和“重新检查”；模块按主界面视觉顺序显示并附带错误/警告计数，模块筛选结果同时保留全局问题。
- 主界面发生编辑后，已打开报告显示 `结果待更新`；手动重新检查或保存后自动检查刷新结果，并尽量保持当前模块筛选。
- 保存后若报告可见则同步刷新；点击底部规则错误大红点可重新显示同一个报告窗口。

### 32.3 验证

- 使用项目固定Python 3.9.13执行41项单元测试，全部通过。
- 执行10组离屏UI回归，全部通过；新增/重写测试覆盖状态颜色与文字、绝对路径标题、编辑到保存/检查状态转换、非模态单实例、模块筛选、定位不关闭、待更新和重新检查。
- 既有搜索、保存、合并、位置/DPI、Excel式行操作、右下详情、自适应列宽和上下双区域测试全部继续通过。
- 成品 `LSR_V1.11.0.exe --smoke-test` 退出码为0。
- 成品大小：56,473,593字节；SHA-256：`F2278718B6B494DC4025B6CC8151FD46119CFA795E5505EE5151E9B04F5C2D3E`。

## 33. V1.11.1：保存后后台规则检查

### 33.1 性能与线程模型

- `Ctrl+S`和“另存为”仍先同步完成当前编辑提交、SQLite完整性校验、安全备份和磁盘写入；保存成功后立即显示 `已保存，正在检查`并返回Qt事件循环。
- 规则检查由 `DatabaseRuleCheckThread`执行。线程使用独立SQLAlchemy引擎和会话读取刚保存的目标数据库，不跨线程访问主界面ORM会话或Qt控件；检查结果回到主线程后再更新状态和规则报告。
- 不引入文档版本号。检查期间再次保存时只设置一次待重检标志；当前线程结束后启动一次最新检查，旧结果不作为最终状态展示。
- 检查完成前发生编辑时保持未保存/结果待更新语义；关闭、切换数据库和退出软件时等待所属线程安全结束，避免 `QThread destroyed while running`及原生内存访问错误。
- 后台检查异常显示 `已保存，规则检查失败`大红点，点击可查看异常；规则检查错误或异常不改变数据库已经成功保存的事实。

### 33.2 验证

- 使用项目固定Python 3.9.13执行41项单元测试，全部通过。
- 执行11组离屏UI回归，全部通过；新增 `ui_background_rule_check_smoke.py`，覆盖真实独立数据库会话、前后台结果一致、保存不等待慢检查、连续保存合并重检及关闭时线程退出。
- `ui_status_bar_smoke.py`同步更新，验证保存立即进入 `已保存，正在检查`，后台结束后正确切换规则错误或警告通过状态。
- 成品自检不仅加载界面和对话框，还在 `LSR_V1.11.1.exe`内部启动真实后台线程检查随包样例库；`--smoke-test`退出码为0。
- 成品大小：56,475,189字节；SHA-256：`A2A3BEEDE4ACD0665E2B45C1222C41EFE1EABF3E07F58EB49752D7E2D1F720A2`。

## 34. V1.11.2：当前BIT状态组灰色基线修复

### 34.1 根因与修复

- 截图中红框内的灰线不是主分割线、右下外框或页签内容窗格边框，而是 `bitRangeLabel`作为 `QTabWidget.cornerWidget`时异常扩展到整个页签栏，与原生 `QTabBar`绘制矩形重叠后透出的基线。
- 对照渲染确认：状态标签和原生页签栏的几何宽度均为整个标题区域，移除corner槽位后灰线随之消失。
- V1.11.2把“BIT详情/字段编辑”页签和“当前BIT/合法范围”放入独立无边框标题容器；左侧使用不绘制基线的独立 `QTabBar`，右侧状态标签使用剩余空间，原 `QTabWidget`内部页签栏隐藏。
- 独立页签与原页面索引双向同步；状态标签不参与主面板最小宽度计算，避免右下方框被长文字撑宽，四宫格左右等宽、完整外框、编辑区和按钮行均保持不变。

### 34.2 验证

- 对修复前、移除corner槽位和独立标题行三种状态进行了离屏对照渲染，确认红框位置不再绘制灰色横线。
- `ui_context_panel_smoke.py`新增corner槽位为空、原生页签隐藏、独立页签不绘制基线、页签双向联动、状态位于页签右侧及四宫格等宽断言。
- 使用项目固定Python 3.9.13执行41项单元测试和11组离屏UI回归，全部通过。
- 成品 `LSR_V1.11.2.exe --smoke-test`包含真实后台规则线程自检，退出码为0。
- 成品大小：56,475,400字节；SHA-256：`DDCD5555274588AB3D41F157036225855DEC9D149F236DAC57863A16F031FDA3`。

## 35. V1.12.0：通配符搜索与崩溃诊断

### 35.1 搜索行为

- “工具 → 搜索”不新增按钮或开关：无 `*`时执行忽略英文字母大小写的完整字段精确匹配；包含 `*`时按完整字段执行简单通配符匹配。
- `*`表示零个或多个字符，支持开头、中间、末尾和多片段组合；连续星号折叠，只有星号的查询被拒绝，其他正则特殊字符全部按普通文字处理。
- 搜索框显示 `*ADC*DAT*`示例；结果状态明确显示“精确匹配”或“通配符匹配”，原有范围筛选、Enter/Shift+Enter循环定位和单元格高亮保持不变。

### 35.2 崩溃分析和修复

- V1.11.2现场弹窗属于原生空地址读取访问冲突，不是Python内存不足。Windows事件、可靠性记录、WER和应用目录均未留下可定位到具体代码行的证据；30轮数据库全生命周期压力测试未复现普通编辑/保存路径崩溃。
- 正常关闭采用应用侧受控退出：先停止后台规则检查、释放SQLAlchemy会话与临时工作区、关闭日志，再跳过PySide2/PyInstaller在解释器终止阶段不可控的Qt对象析构顺序。
- 新增单文件运行日志 `%LOCALAPPDATA%\LSR\logs\LSR.log`；每次打开覆盖上一份，只记录低频关键事件，并捕获Qt消息、未处理Python异常和Windows原生致命错误的全部线程栈。重复Qt消息去重，不记录鼠标和常规界面刷新，避免用户操作卡顿。

### 35.3 验证

- 项目固定Python 3.9.13语法检查通过；49项单元测试全部通过。
- 11组离屏UI回归全部通过，覆盖搜索定位、保存后台检查、规则报告、上下区域、右下详情、自适应列宽/DPI、状态栏、行操作和数据库合并。
- 额外执行30轮“打开数据库—编辑—保存并启动后台检查—切换数据库—关闭重建界面”压力测试，全部通过且无访问冲突、线程销毁警告或残留Python异常。
- 成品 `LSR_V1.12.0.exe --smoke-test`和受控关闭 `--shutdown-smoke-test`退出码均为0；后者确认单文件日志含 `CLEAN_SHUTDOWN`。
- 成品大小：56,479,621字节；SHA-256：`D368A42B500DECBA06B827EE9361FC65E0F53940879D3AA0FFC84F49C18A2F5D`。

## 36. V1.13.0：大型数据库编辑性能优化

### 36.1 性能根因

- V1.12.0每个数据库操作都在执行前后调用`capture_database_state`，分别读取Module、Register、BIT、BIT取值四张完整表，再计算行差异。普通单元格编辑因此与整库行数线性相关，并同时建立两份大型Python字典。
- 旧Module、Register、BIT和BIT取值保存处理还会重新填充所在整张表；Module刷新会继续刷新当前Register、BIT和BIT取值，重新创建大量`QTableWidgetItem`和下拉框。
- 在128,160业务行体验库上的优化前实测：单次整库快照2.47秒；Module、Register、BIT文字编辑分别约5.14、5.36、4.70秒。

### 36.2 受影响行级撤销

- 新增`DatabaseChangeRecorder`，在一个用户操作期间监听SQLAlchemy `before_flush`，识别真正新增、修改和删除的ORM对象。
- 对修改/删除对象只在flush前按主键读取原行；事务完成后只查询相同受影响ID的新行。生成的`DatabaseCommand`格式、128级历史深度、保存检查点及`Ctrl+Z/Ctrl+Y`接口保持不变。
- 新增父子树和删除父子树均记录实际Module/Register/BIT/BIT取值；普通字段编辑通常只记录1行，BIT默认值联动允许记录BIT和Register两行。
- 跨数据库会话的“合并到当前库”和“合并选中模块”继续使用整库前后快照，并通过既有单操作撤销/重做测试。

### 36.3 普通编辑局部刷新

- Module、Register、BIT和BIT取值的普通字段保存改为只解析和写入当前列，不再从同一行读取其他所有字段。
- 成功提交后不重建四张表；只同步长文本编辑器、面包屑路径、BIT缩略图、Register默认值单元格和自适应列宽。
- 地址和BIT位置修改不会因保存动作改变视觉顺序；非法输入和异常恢复、插入/复制/剪切/粘贴/删除、拖动/刷新编址以及撤销/重做仍按原逻辑执行必要刷新。

### 36.4 大型MCU体验库与结果

- 新增`release/samples/LSR_Large_MCU_5120_Registers.db`及说明：160个Module、5,120个32位Register、40,960个BIT、81,920个BIT取值，共128,160业务行，约6MB。
- 数据库使用Module `0x1000`间隔、Register 4字节间隔、8个4位BIT完整覆盖32位；生成过程执行SQLite `quick_check`，生成脚本和重复性能脚本均保留。
- 同一环境优化后Module、Register、BIT普通编辑分别约0.031、0.020、0.017秒；独立大库UI回归中的四类编辑为0.013～0.029秒，均远低于1秒目标且未调用整库快照或四张表重建。
- 大库测试完成四类编辑、受影响表/行数断言、撤销、重做、本地工作副本保存、目标数据库重新打开及四类数据持久化核对。

### 36.5 扩大回归

- 54项单元测试全部通过；新增事务级修改、新增父子树、删除父子树、撤销/重做及大型数据库生成结构测试。
- 12组离屏UI回归全部通过：除需求16大库专测外，完整覆盖Module/Register/BIT行拖动、插入、复制、剪切、粘贴、删除、刷新编址、整库合并/选中模块合并及其撤销重做。
- 保存/另存为/未保存关闭、后台规则检查、规则报告、搜索定位、状态栏、右下详情、上下区域、跨屏DPI和自适应列宽回归继续通过。
- 需求16按确认范围不修改首次打开、网盘复制、目标保存、搜索和全库规则检查；大库现有全库规则检查可能耗时较长，已经在样本说明中明确。
- 成品`LSR_V1.13.0.exe --smoke-test`和`--shutdown-smoke-test`退出码均为0，后者日志包含`CLEAN_SHUTDOWN`；未留下测试进程。
- 成品大小：56,480,963字节；SHA-256：`DF47E73201115876E908BE88050048DF58B32E331B1C496398C46C5CDC0DE0E6`。
- 大型MCU测试库大小：6,037,504字节；SHA-256：`C389D1794F9523545D988833FD25B8703E4D366F9AD7A11E24D5D4994C12DBA4`。

## 37. V1.14.0：大型数据库规则检查性能优化

### 37.1 根因与数据读取

- V1.13.0首次打开仍在UI线程同步检查；规则检查遍历ORM的`module.regs`、`register.bits`和`bit.bitvalues`懒加载关系，在5,120个Register大库上形成数万次SQLite查询，现场基准运行数分钟且内存增长到约429MB后终止。
- 新增`rule_check_database.py`。后台线程在本地工作副本上开启稳定的SQLite只读事务，以Module、Register、BIT、BIT取值四次批量查询建立使用`__slots__`的轻量检查对象，再调用原有纯数据规则引擎；数据库结构和规则语义不变。
- 首次打开完成表格加载后立即启动后台全量检查，不再在`_completeDatabaseReload`中同步阻塞；主动完整检查也复用批量读取，消除N+1查询。

### 37.2 保存增量检查

- `Ctrl+S`和“另存为”在保存检查点移动前，从撤销栈差异中解析Module、Register、BIT和BIT取值的父子ID，归并为受影响Module集合。
- 增量任务浅加载全部Module以刷新全局Module名称/地址规则，只加载受影响Module的完整子树；一个Module范围覆盖寄存器名称/地址冲突、BIT名称/位域冲突、默认值组合和最终Verilog信号名冲突。
- 增量问题通过Module ID合并到完整基线：删除全部全局Module级旧问题和受影响Module旧问题，加入本次新问题，未改动Module的既有错误/警告保持不变。
- 没有完整基线、无法可靠解析撤销分支、或一次影响超过64个Module时自动执行后台全量检查。整库合并安全退化，普通编辑不扫全库子树。

### 37.3 并发与稳定性

- 首次全检期间允许继续编辑；完整结果先建立基线，期间保存产生的Module范围随后自动补查。连续保存合并待检查范围，任务串行执行，旧结果不能覆盖最新状态。
- 后台任务只读取`workspace.working_path`，不再对可能位于网盘的目标数据库进行规则遍历；目标文件仍只由显式保存流程写入。
- 工作区切换和关闭继续断开完成信号、请求中断并等待所属线程；补充无sender的迟到Qt事件防护，避免退出期间访问已释放线程对象。

### 37.4 大库性能与验证

- 128,160业务行大库批量全检实测约0.86～1.06秒，单个Module增量检查约0.024秒；真实离屏界面约0.41～0.46秒返回打开操作，后台全检约1.27～1.39秒结束，保存约0.09秒返回，增量状态约0.12秒完成。
- 新增规则数据库加载/结果合并单元测试和`ui_requirement17_large_rule_check_smoke.py`，验证后台打开、完整基线、单Module范围、真实BIT重名错误以及增量缓存与重新全检结果逐项一致。
- 57项单元测试全部通过；13组离屏UI回归全部通过，其中需求16和需求17两组直接使用128,160行业务大库。既有保存、连续后台检查、规则报告、搜索、上下布局、右下详情、DPI/列宽、Module/Register/BIT行操作、合并及撤销重做均无回归。
- 成品`LSR_V1.14.0.exe --smoke-test`退出码为0，验证随包UI、菜单、对话框、样例库及新的批量规则线程；`--shutdown-smoke-test`退出码为0，日志包含`CLEAN_SHUTDOWN`，未出现线程销毁或原生访问错误。
- 成品大小：56,484,867字节；SHA-256：`3C2FCBDEDDE645BFB65CDCE28701F55C1693854832ACC091D07A6DD4627B303E`。
- 大型MCU测试库大小：6,037,504字节；SHA-256：`C389D1794F9523545D988833FD25B8703E4D366F9AD7A11E24D5D4994C12DBA4`。

## 38. 需求18兼容性基线分析与V1.15.0实施

### 38.1 实测结果

- `Register_V1.0.0`有效样本`aaa.db`的四张核心表与当前模型字段兼容，V1.14.0实测可加载1个Module、1个Register、1个BIT、0个BIT取值，后台规则检查通过。
- `LSR_V1.1.3\scripts\dayu_top.db`与当前项目`scripts\dayu_top.db`哈希一致，V1.14.0实测可加载18个Module、180个Register、786个BIT、639个BIT取值；当前规则报告65项问题，但数据库正常打开，属于规则差异而非格式失败。
- 两类旧库`PRAGMA user_version`均为0，必须通过核心表、字段、索引和关系结构识别；旧数据库的列顺序和`visibility`类型声明存在差异，但按列名读取能够兼容。
- 两类有效样本均通过SQLite`quick_check`且不存在孤儿父子行。`LSR_V1.1.3\scripts\instance`下两个数据库为0字节空占位文件，不属于有效寄存器库。

### 38.2 已识别风险

- 旧库保留`module.address UNIQUE`物理约束，而当前版本允许重复地址先导入并由规则检查提示；如果不迁移该约束，旧库后续合并/编辑会表现得与当前数据库不同。
- ORM加载前必须加入只读兼容预检，在本地工作副本内先完成已知旧格式迁移、移除旧唯一约束并进行完整性/行数/摘要复核；只有验证通过后才允许升级源库格式。
- 源库升级前必须建立逐字节一致的旧格式备份，格式升级只允许改变结构和内部格式号。空文件、损坏库、缺少必要字段和孤儿关系必须明确提示且不得崩溃。
- 兼容实现采用当前格式快速通道并与业务编辑隔离；发布门禁要求旧库兼容矩阵和当前格式完整回归同时通过。当前格式至少维持57项单元测试、13组UI回归以及需求16/17大库性能与结果一致性验证。
- 完整需求、字段处理原则和自动验收矩阵见`LSR_功能改造需求.md`中的需求18。

### 38.3 V1.15.0实现

- 新增`database_compatibility.py`，通过SQLite只读连接识别四张核心表、必要字段、可选字段、孤儿关系、`module.address`唯一约束和内部格式号；当前格式不执行任何写操作。
- 旧格式先复制到本地工作副本，再在单个事务中补齐已知可选列并重建Module表以移除旧地址唯一约束。迁移前后对每张业务表的全部原有列执行类型化SHA-256摘要，`NULL`、整数、浮点、字节和UTF-8文本分别编码，避免字符串拼接摘要掩盖类型或字段边界差异。
- 工作副本通过`quick_check`、`foreign_key_check`和业务摘要复核后，在源库同目录建立`*.before_lsr_compat.db`精确备份；确认备份哈希与原文件一致且源文件未被外部修改后，以同目录临时文件和`os.replace`原子升级源库。
- 如果源库只读、网盘写入失败或原子替换失败，源库保持原样，已经迁移并验证的本地工作副本仍正常打开；状态栏提示保存时将写入当前格式。
- “文件 → 打开”和“合并到当前库”统一使用兼容工作区。源库中的规则错误仍由规则检查报告，不作为格式迁移失败；0字节占位文件、缺少核心表/必要字段及孤儿父子行则在ORM初始化前拒绝并给出中文原因。

### 38.4 自动验证

- 单元测试由57项增加到66项；新增旧结构迁移、全部原字段摘要一致、精确备份、源库原子升级、只读源降级打开、当前格式零写入、0字节/缺表/缺必要字段/损坏SQLite/孤儿行保护，以及未知扩展表和扩展列保留测试。
- 新增`tests/ui_requirement18_legacy_compat_smoke.py`，分别复制并打开Register V1.0.0真实`aaa.db`和LSR V1.1.3真实`dayu_top.db`，核对四表计数、源库/工作副本业务摘要、备份哈希、典型编辑保存和当前格式重开不再迁移。
- 14组离屏UI回归全部通过：原V1.14.0的13组完整保留并增加需求18旧库兼容测试；保存、撤销重做、搜索、合并、右下详情、DPI/缩放、自适应列宽、Excel式行操作、上下布局、状态栏、规则报告及后台检查均无回归。
- 当前格式128,160行业务大库对照测试中，关闭兼容检测的工作区打开约0.081～0.092秒，开启兼容检测约0.078～0.082秒，差异处于运行波动范围；打开前后SHA-256一致且未创建兼容备份，证明当前格式只走结构元数据快速通道。
- 大库普通编辑实测：Module约0.026秒、Register约0.018秒、BIT约0.015秒、BIT取值约0.013秒；界面加载返回约0.392秒、后台全检约1.208秒、保存返回约0.083秒、单Module增量检查约0.114秒，与V1.14.0基线一致，无明显退化。
- 项目当前样例`scripts/dayu_top.db`已仅做结构升级，业务计数仍为18个Module、180个Register、786个BIT和639个BIT取值；新生成的大库不再携带旧版地址唯一约束。外部Register V1.0.0和LSR V1.1.3金样本保持原格式不变。
- 成品`LSR_V1.15.0.exe --smoke-test`和`--shutdown-smoke-test`退出码均为0；受控退出日志包含`CLEAN_SHUTDOWN`，未出现后台线程销毁或原生内存访问错误。
- 成品大小：56,493,386字节；SHA-256：`2BCA8816E87299D5A31DAB62B8E327EA41F2ABB48495ADD38667C492D2C2B946`。
- 当前格式大型MCU测试库大小：6,033,408字节；SHA-256：`A54F8B3D6C413CD1332F1D36C0F2C959B84ED24976AE17D9662DA967EA254E22`。

## 39. V1.15.1：新增Register/BIT默认值统一

### 39.1 修改范围

- 新建数据库、新增Module和新增Register产生的Register统一为“用户可见=1、ATE=0”，其附带BIT统一为“用户可见=1、SCAN=0”。
- 独立新增BIT由原来的“用户不可见”修正为“用户可见=1”，SCAN继续默认为0；Add和右键上下插入共享该规则。
- COPY、剪切和粘贴继续保留源对象字段值，已有数据库历史行不改写。
- ATE、SCAN列、数据库字段、规则检查、搜索以及`gen_shadow_interface.py`、`gen_scan_handler.py`等生成逻辑均保持不变。

### 39.2 验证记录

- 新增`tests/ui_requirement19_defaults_smoke.py`，覆盖新建数据库、Module/Register/BIT新增默认值和复制保留源值。
- 66项单元测试和15组离屏UI回归全部通过；需求16大库四类编辑、需求17后台全检/增量检查、需求18真实旧库兼容以及保存、搜索、规则报告、布局和行操作均无回归。
- 默认值专项测试不缓存数据库刷新前的ORM对象，所有后续操作均按稳定数据库ID重新定位；警告在离屏环境中转换为测试断言并显式清理工作区，避免测试异常触发原生Qt模态窗口。
- 成品`LSR_V1.15.1.exe --smoke-test`和`--shutdown-smoke-test`退出码均为0；受控关闭日志包含`CLEAN_SHUTDOWN`。
- 成品大小：56,493,474字节；SHA-256：`4EC4CFE45620400304BA8AE654D6B8ADD331331A3B920F61BD2367260E6050CB`。
- 大型MCU测试库仍为5,120个Register、40,960个BIT和81,920个BIT取值，大小6,033,408字节；本需求未改变测试库内容或数据库结构。

## 40. V1.16.0：后台结果修订号与工作副本WAL

### 40.1 现场问题与扩大测试证据

- 将大型MCU压力库扩大到1,600个Module、51,200个Register、409,600个BIT和819,200条BIT取值，数据库约61.55 MB；界面约1.4秒恢复可操作，后台完整检查约11.82秒，普通无并发编辑约0.046～0.059秒。
- 在完整检查刚启动时并发修改BIT名称，SQLite`DELETE`日志模式下编辑提交实测4.51秒；另一轮编辑提交4.89秒，已经接近默认5秒等待上限。未出现数据损坏，两份测试库`quick_check`均为`ok`，但较慢机器存在`database is locked`和当前编辑回滚风险。
- 正常“编辑→Ctrl+S”路径能够合并待检查Module并串行补查：保存期间待重检标志成功建立，最终增量结果正确报告重复BIT错误。
- 受控竞态已复现：后台快照捕获重复BIT错误后，用户撤销回无重复且操作栈恢复已保存，现有实现仍把旧错误应用为红色；说明`dirty`状态和待检查范围不足以唯一标识结果所属文档状态。

### 40.2 已确认的两个独立需求

- 需求20增加仅存在于内存中的单调递增`document_revision`和`saved_revision`。后台任务携带`check_revision`，只有结果修订号、当前修订号和已保存修订号一致时才允许更新绿色/红色状态及规则报告；撤销、重做也递增修订号，旧结果不因内容看似恢复而重新有效。
- 需求21只对本地临时`working.db`启用SQLite WAL，使后台稳定读快照与主线程编辑提交并行；不对用户源库或网盘目标直接启用WAL，不为每次检查复制完整数据库。
- 两项必须配套：WAL消除读写锁等待，文档修订号防止并发加快后旧快照结果误应用。WAL本身不判断结果新旧，修订号本身也不缩短SQLite锁等待。
- 保存继续使用SQLite Backup API和原子替换；目标临时文件必须包含WAL中全部已提交内容，并验证为普通单文件数据库，接收者不需要`.db-wal`或`.db-shm`。

### 40.3 实现内容

- `UIProcess`增加工作区内存修订号；数据库编辑命令、撤销和重做成功后单调递增，保存时记录当前已保存修订号，打开、切换和关闭工作区时重新初始化。
- 后台线程启动时绑定检查修订号；结果修订号与当前/已保存修订号不一致或操作栈未保存时，结果不会更新缓存、状态栏或非模态报告。干净状态自动补跑完整检查，待运行增量检查在基线失效时自动提升为完整检查。
- `DatabaseWorkspace`只在本地临时`working.db`上启用WAL，并把实际日志模式及失败原因交给运行日志；用户源库、旧版金样本和最终保存文件不切换为WAL。
- SQLite Backup保存后显式验证临时目标为`DELETE`模式；测试覆盖未检查点WAL页进入保存文件、目标旁文件不存在、退出后工作目录及WAL旁文件清理。

### 40.4 源码与大库验证记录

- 69项单元测试全部通过；新增WAL作用域、长读事务与写入并发、WAL已提交页保存、单文件目标，以及新建工作区WAL测试。
- 16组离屏UI回归全部通过，覆盖保存/撤销重做、搜索、合并、详情编辑、DPI与布局、状态栏、规则报告、后台检查、需求16/17大库、需求18真实旧库和需求19默认值。
- 修订号专项覆盖连续保存、检查期间继续编辑、编辑后撤销回已保存、重做/再撤销、旧错误拒绝并补跑，以及关闭时规则线程回收；原确定性旧结果覆盖竞态不再复现。
- 5,120个Register库：并发四类编辑约0.010～0.022秒，保存返回约0.09秒，完整检查约0.83秒，单Module增量约0.033秒，峰值工作集约153.7 MiB。
- 51,200个Register、409,600个BIT和819,200条BIT取值库：确定性长读事务期间四类编辑约0.041～0.053秒，实际生产完整检查线程中的编辑约0.077秒；保存返回约0.71～0.96秒，完整检查约9.11～11.39秒，单Module增量约0.21～0.30秒；峰值工作集最高约436.2 MiB，检查后约134.0 MiB，清理后约131.2 MiB。
- 两档大库保存目标均通过`quick_check`，均为单文件`DELETE`模式且无`-wal/-shm`；工作副本保持WAL并在释放线程和引擎后随临时会话目录删除。

### 40.5 发布与成品验证

- 发布版本统一为V1.16.0：运行时`__VERSION__=1.16.0`、窗口标题为`RegisterManager 1.16.0 - 苏州领慧立芯科技有限公司`、构建目录为`build_v1.16.0`、成品为`release\LSR_V1.16.0.exe`，清单程序集版本为`1.16.0.0`。
- 第一次自动标题门禁使用隐藏窗口的`MainWindowTitle`读取方式，Windows未返回隐藏窗口标题，测试按失败处理并清理本次启动进程；随后改为在成品受控关闭日志中记录并核对实际Qt窗口标题。
- 加入标题日志后的第一次重打包因上述一文件程序的解包子进程PID 42672仍占用成品而触发`WinError 5`，构建按非零退出停止；确认其为本次测试进程并清理后重新构建成功，未结束用户原有V1.15.1实例。
- 成品`--smoke-test`和`--shutdown-smoke-test`退出码均为0；独立日志核对`START version=1.16.0`、V1.16.0窗口标题及`CLEAN_SHUTDOWN`全部通过。
- 最终版本矩阵脚本首次读取源码`__VERSION__`时PowerShell引号转义错误，Python按非零退出并报告`SyntaxError`；修正为独立`-c`参数后重跑输出`1.16.0`、退出码0，成品运行时版本仍由前述受控关闭日志独立确认。
- 成品大小56,495,087字节；SHA-256：`EF9266770D788B98DAB1F7F9205342D6E9BF80ACE51B6C5C4DB3D7B63F2B4E08`。
- 当前PyInstaller包未定义Windows`VERSIONINFO`资源，因此资源管理器中的`FileVersion`和`ProductVersion`为空；本版本没有沿用旧值，实际版本由运行时、标题、EXE文件名及程序集清单共同校验。

## 41. V1.16.1：Register默认值与BIT定义一致性修复

### 41.1 根因与影响范围

- 真实`release\Bifang.db`中`TRIM_MMR → TEC_CH0_0`和`TEC_CH1_0`的Register缓存默认值均为`0x40`；对应BIT实际为`[6:0]=0`、`[7]=1`，按位置组合的正确值均为`0x80`。
- 全库分析发现这两项属于“BIT布局合法、物化Register默认值过期”，同库另有70个BIT越界/重叠等非法布局；后者不能通过改写Register默认值掩盖，仍由规则检查报告。
- Register默认值会被C头文件、测试文件、文档/SVD和部分设计脚本直接消费，因此只在Verilog入口检查不够；所有生成入口都必须等待当前保存版本的规则结果，并阻止默认值不一致的数据继续生成。

### 41.2 修复实现

- `register_logic.py`增加严格的安全修复发现模型：只对位宽、位置、默认值和重叠关系全部合法的Register计算派生值；组合始终按掩码、起始位移位和按位或完成。
- 后台规则线程先执行原规则引擎，再只对其已经报告的`REGISTER_DEFAULT_MISMATCH`寄存器计算修复值，避免对51,200个Register无问题大库重复遍历全部BIT。
- 后台结果只有在需求20的检查修订、当前修订、已保存修订和干净操作栈全部一致时，才把修复作为一个可撤销数据库操作应用到本地工作副本；修复使文档变为未保存，原目标在Ctrl+S前不变，检查前旧报告不会覆盖修复后状态。
- 主动规则检查采用相同安全修复路径；复制Register/Module及合并数据库/选中Module时，对合法BIT布局重新物化默认值，非法布局保留并记录原因。
- 所有生成入口新增保存版本规则状态门禁；未检查、正在检查、结果过期、检查失败、未保存或存在默认值不一致时不允许生成。

### 41.3 为什么原测试未覆盖

- 原需求7测试覆盖了纯组合函数、BIT默认值溢出和BIT编辑后的即时重算，但没有断言规则引擎必须产生`REGISTER_DEFAULT_MISMATCH`，也没有打开一个“BIT布局合法但Register旧缓存已经写错”的真实数据库。
- 既有大型MCU测试库从生成阶段就是一致数据，后台/UI测试使用一致的合成库或受控假线程，无法触发已有库的过期物化值。
- 兼容测试的职责是保证旧库业务值不被格式迁移改写，因此只比较原字段真实性，不会主动把语义不一致的Register默认值规范化；这项保护是正确的，但必须由打开后的业务规则层补充处理。
- 原生成门禁主要保护Design/DV路径，C/Test/Doc入口只检查是否已保存，存在读取旧Register缓存的风险；V1.16.1已统一补齐。

### 41.4 失败证据与自动验证

- 修复前新增真实库专项测试按预期失败，工作副本仍读到`TEC_CH0_0/TEC_CH1_0 = 0x40`，断言为`valid stale Register defaults were not repaired`；修复后同一用例通过并得到`0x80/0x80`。
- 基础UI测试首轮因用例在Undo/Redo重载会话后继续引用旧ORM对象而触发`DetachedInstanceError`；这是测试工具缺陷，改为保存稳定数据库ID后重跑，产品断言完整通过。
- 71项单元测试全部通过；15组普通离屏UI回归、3组5,120寄存器大库回归、2组51,200寄存器压力回归全部通过，另有真实Bifang专项包含在普通UI回归中。
- 新增`ui_default_address_foundation_smoke.py`覆盖固定步长1/2/4与位宽解耦、Register/BIT拖动前地址不变、刷新后显式编址、BIT累计位宽、默认值编辑/复制/删除、撤销/重做及保存重开。
- 5,120寄存器库完整检查约0.77～1.10秒、普通编辑约0.010～0.020秒；51,200寄存器库完整检查约10.86～11.88秒、检查期间实际编辑约0.069秒、保存约0.67～0.95秒，未出现锁错误或明显性能退化。
- 原始`release\Bifang.db`测试后SHA-256仍为`4493D9000D6B8F35C68622F62DF5FC8F7F849B83AB4354D4636A2F5D8D26C350`，`quick_check=ok`且两项缓存仍为原始`0x40`，证明自动测试未改用户金样本。

### 41.5 发布与成品验证

- 发布版本统一为V1.16.1：运行时`__VERSION__=1.16.1`、窗口标题为`RegisterManager 1.16.1 - 苏州领慧立芯科技有限公司`、构建目录为`build_v1.16.1`、成品为`release\LSR_V1.16.1.exe`，程序集清单版本为`1.16.1.0`。
- 第一次直接从PowerShell调用窗口型成品时没有等待进程，`$LASTEXITCODE`为空，因此该次不计为通过；改用`Start-Process -WindowStyle Hidden -Wait -PassThru`后，`--smoke-test`和`--shutdown-smoke-test`真实退出码均为0。
- 成品日志取证第一次沿用旧文件名`LSR_current.log`而读取失败；实际本版日志为`LSR.log`，随后按真实路径核对`START version=1.16.1`、V1.16.1窗口标题和`CLEAN_SHUTDOWN`全部通过。
- 成品大小56,498,226字节；SHA-256：`D81A4149936768B42B7D6E646F0F8871958D87050BF87E0E120FB17993DE28C8`。

### 41.6 默认值及基础功能异常测试补强

- 新增`tests/test_register_default_anomalies.py`，覆盖普通错值、`NULL`、负值、超位宽缓存和7类非法BIT/Register定义；另以5,000个Register、20个异常缓存验证候选筛选规模。
- 首次执行真实发现产品遗漏：合法BIT对应的Register缓存为`NULL`时只产生`REGISTER_DEFAULT_INVALID`，未进入安全修复集合；失败断言实际仅返回`STALE/NEGREG/BIGREG`而缺少`NULLREG`。修复后将Register默认值非法、超范围和不一致三类规则统一作为“需经严格BIT重算确认”的候选，同一测试通过。
- 生成门禁扩展到Register位宽/默认值及BIT位置/位宽/越界/重叠/默认值等全部默认值完整性错误；安全缓存修复完成不能掩盖其他非法BIT，也不能让C/Test/Doc等入口消费不可信复位值。
- 新增`ui_register_default_anomaly_smoke.py`：验证4类安全缓存只修工作副本、7类非法定义不误修、撤销/重做、保存、生成门禁、旧后台修复不覆盖新编辑，以及过期修复快照明确失败。
- 新增`ui_foundation_anomaly_smoke.py`：验证满位宽Add/Copy、最后一行删除、跨类型/自身粘贴、非法输入回滚、BIT总宽超限、Module非法地址、Register重复地址刷新、未编址保存阻止及外部目标冲突取消覆盖。
- 普通UI完整回归第一次在Bifang专项处等待：扩展门禁后，真实库另有70项非法BIT定义会正确弹出阻止提示，但旧测试仍期待允许生成并打开离屏模态框。该会话由任务显式中断且无残留进程；测试改为断言“两个安全缓存已修复、其余非法BIT仍阻止生成”后，同组及后续用例全部通过。这是测试期望过时，不是产品线程死锁。
- 最终74项单元测试、17组普通离屏UI、3组5,120寄存器大库UI和2组51,200寄存器压力UI全部通过；失败0、跳过0。5,120寄存器完整检查约0.82～1.15秒，51,200寄存器完整检查约8.80～9.53秒、实际检查期间编辑约0.102秒。
- 本轮用户要求的是异常测试和测试补强，未要求发布新版本；因此只更新开发源码、自动测试和项目根目录权威文档，不覆盖既有`release\LSR_V1.16.1.exe`，其既有哈希及发布记录保持不变。

## 42. V1.16.2：默认值异常与基础功能异常回归补丁

- 本版将V1.16.1发布后完成的异常测试修复正式打包，重点补齐合法BIT定义下Register默认值为`NULL`时未进入安全修复候选的问题。
- Register默认值非法、超范围或与BIT拼接不一致时，只有严格BIT重算成功才允许修复工作副本；BIT位置、位宽、重叠、默认值等定义错误仍保留为真实规则错误。
- C/Test/Doc等全部生成入口统一执行默认值完整性门禁，避免任何生成路径消费不可信的复位值。
- 本版继续使用项目固定Python 3.9环境构建；测试结果、成品校验值和版本矩阵在发布验证完成后记录。

### 42.1 发布验证结果

- Python 3.9.13语法检查及74项单元测试全部通过；默认值异常、基础编辑/地址、Bifang实库修复、旧库兼容和其余常用界面流程全部通过。
- 5,120寄存器大库完整检查约1.03秒、检查期间编辑约0.015秒；51,200寄存器压力库完整检查约11.04～11.16秒、检查期间编辑约0.077秒，后台检查未阻塞正常编辑。
- 两个旧版真实数据库`Register_V1.0.0`的`aaa.db`与`LSR_V1.1.3`的`dayu_top.db`均通过兼容打开测试，源业务数据未被测试改写。
- 补充UI回归中保留了三次无效测试调用记录：一次脚本缺少必需样例参数、一次Bifang非法定义触发离屏提示等待、两次需求16脚本使用了不含BIT枚举值的样例；改用符合各脚本前置条件的样例后均通过，不计为产品断言失败。
- 成品`--smoke-test`和`--shutdown-smoke-test`退出码均为0；日志确认`START version=1.16.2`、窗口标题`RegisterManager 1.16.2 - 苏州领慧立芯科技有限公司`及`CLEAN_SHUTDOWN`。
- 版本矩阵一致：源码运行时1.16.2、窗口标题1.16.2、EXE文件名`LSR_V1.16.2.exe`、构建目录`build_v1.16.2`、清单版本`1.16.2.0`；成品未写入Windows FileVersion/ProductVersion资源，因此这两项为空而不是旧版号。
- 成品大小56,498,398字节；SHA-256：`90ACDB363B5B730BCC4EB2408EA6B6BF4F90DBCF62F16F3EDE80CC0ED8E05E31`。
- 发布测试后`release\Bifang.db`的SHA-256仍为`4493D9000D6B8F35C68622F62DF5FC8F7F849B83AB4354D4636A2F5D8D26C350`，确认真实样例库未被修改。

## 43. V1.16.3：规则错误三层联动定位

- 原`navigateToRuleIssue()`虽然依次设置了Module、Register和BIT当前项，但表格刷新会排队恢复定位前的滚动条；该回调在规则定位之后执行，导致祖先行不能稳定居中，且未显式保留三张表各自的选择高亮。
- 新增不清除其他表选择的层级定位方法：BIT级错误固定选择Module名称、Register名称及BIT实际错误字段，并在表格刷新回调之后再次将三张表分别滚动到中间。
- 定位BIT后同步刷新其BIT取值和右下详情；Module/Register/BIT对象均继续按数据库ID和当前视觉顺序计算显示行，不依赖过期行号。
- 专项测试首先在修改前稳定失败并报告`Module name was not centered`；实现修复后同一规则报告定位路径通过，后续发布验证结果另行记录。

### 43.1 发布验证结果

- Python 3.9.13语法检查及74项单元测试全部通过；20组离屏UI回归全部通过，覆盖规则报告三层定位、搜索、位置/DPI、右下详情、状态栏、保存、行操作、默认值异常、Bifang真实库、旧库兼容和5,120寄存器大库。
- 规则报告专项用例选择非首屏BIT错误，确认Module/寄存器名称与BIT错误字段三处均保持选中，并分别位于对应表格可视区域中部；报告仍保持非模态打开。
- 5,120寄存器库完整检查约0.91～1.18秒，实际检查期间编辑约0.020秒，四类普通编辑约0.011～0.022秒；本版只改变UI选择/滚动逻辑，没有调整大库或规则算法。
- 因验证期间用户正在运行V1.16.2（基线PID 4824/32812），未重复执行峰值约436 MiB的51,200寄存器压力测试，避免与用户进程争抢内存；两项用户进程全程保留，测试结束后没有新增Python/LSR残留进程。
- 成品`--smoke-test`和`--shutdown-smoke-test`退出码均为0；独立日志确认`START version=1.16.3`、窗口标题`RegisterManager 1.16.3 - 苏州领慧立芯科技有限公司`及`CLEAN_SHUTDOWN`。
- 版本矩阵一致：源码运行时1.16.3、窗口标题1.16.3、EXE文件名`LSR_V1.16.3.exe`、构建目录`build_v1.16.3`、清单版本`1.16.3.0`；Windows FileVersion/ProductVersion仍为空，没有携带旧版值。
- 成品大小56,498,740字节；SHA-256：`4FA79087BDFF6939FB87FBA5811C6C3B7B218A85946827AF6929B9C4C7F25678`。
- 发布测试后`release\Bifang.db`的SHA-256仍为`4493D9000D6B8F35C68622F62DF5FC8F7F849B83AB4354D4636A2F5D8D26C350`，确认真实样例未被修改。

## 44. V1.16.4：规则报告大结果刷新性能与稳定性

- 现场补充确认约100～200项错误即可在“报告打开、修改、保存、后台结果刷新”时出现卡顿，排除只有超大数据库才触发的假设。
- 新增200项报告压力测试后，修改前同一路径10次刷新平均7.062秒、最慢9.991秒，并按非零退出报告`200-issue rule report refresh blocked the UI`；卡顿根因得到稳定复现。
- 根因是原`QTableWidget`每次刷新创建约7×问题数个`QTableWidgetItem`，同时`ResizeToContents`反复扫描内容并触发布局；全部发生在后台结果回到Qt主线程之后，所以数据库保存已经结束仍会冻结报告窗口。
- 报告替换为`QAbstractTableModel/QTableView`，只重置问题数据；列宽改为稳定可拖动配置，模块错误/警告计数改为一次汇总，更新期间暂停重绘并保留筛选及当前错误选择。
- 规则报告标记过期、后台刷新和前台刷新遇到Qt对象生命周期异常时新增真实日志，不再只清空窗口引用；现有非模态、模块筛选、重新检查、定位和V1.16.3三层高亮行为保持不变。
- 修改后同一200项10次刷新平均约0.0048～0.0058秒、最慢约0.0053～0.0083秒；真实“打开报告后保存、后台返回200项”链路约0.087～0.091秒并完整保留200项，专项测试通过。

### 44.1 发布验证结果

- Python 3.9.13语法检查和74项单元测试全部通过；21组离屏UI回归全部通过，新增组覆盖200项连续刷新及打开报告后的真实编辑/保存/后台刷新路径。
- 既有非模态报告、模块筛选、重新检查、V1.16.3三层居中定位、连续保存排队、旧修订结果拒绝、状态栏、搜索、保存、行操作、默认值异常和右下详情均无回归。
- Bifang真实库、Register V1.0.0/LSR V1.1.3两个旧库和5,120寄存器大库回归通过；大库完整检查约0.96～1.30秒、检查期间编辑约0.018秒。
- 现场旧闪退会话的日志已被当前新启动会话覆盖，无法声称取得旧崩溃堆栈；本版保留可确定复现的主线程阻塞证据，并为后续Qt报告生命周期异常补齐日志。
- 验证期间用户正在运行V1.16.3（PID 38336/38340），因此未重复运行峰值约436 MiB的51,200寄存器压力库；本版未改变规则算法、数据库或大库路径，两项用户进程全程未被关闭，最终没有任务Python/V1.16.4残留进程。
- 成品`--smoke-test`与`--shutdown-smoke-test`退出码均为0；独立日志确认`START version=1.16.4`、窗口标题`RegisterManager 1.16.4 - 苏州领慧立芯科技有限公司`和`CLEAN_SHUTDOWN`。
- 版本矩阵一致：源码运行时1.16.4、窗口标题1.16.4、EXE文件名`LSR_V1.16.4.exe`、构建目录`build_v1.16.4`、清单版本`1.16.4.0`；Windows FileVersion/ProductVersion为空且没有旧版值。
- 成品大小56,499,793字节；SHA-256：`B36408A4D2DE845D635DB19A82A56B43112C5EA8CBE13C130B1A6CBDC0C7E62D`。
- 发布测试后`release\Bifang.db`的SHA-256仍为`4493D9000D6B8F35C68622F62DF5FC8F7F849B83AB4354D4636A2F5D8D26C350`。

### 44.2 2000项错误体验样例

- 按用户要求新增`release\samples\LSR_2000_Rule_Errors.db`：100个Module、2,000个Register、2,000个BIT和4,000条BIT取值；每个Register只包含一个唯一且以数字开头的非法名称，因此当前规则引擎精确报告2,000项`NAME_ILLEGAL`错误、0项警告，不混入重复地址、默认值或BIT布局错误。
- 新增`tools\generate_rule_error_sample.py`进行确定性生成、SQLite `quick_check`和真实规则计数门禁；新增`ui_rule_error_sample_smoke.py`只在临时副本中执行打开、前台规则报告及编辑保存后的后台刷新。
- V1.16.4源码实测打开2,000项规则报告约0.155秒，报告保持打开时修改并保存后的后台检查及刷新约0.064秒；两次均完整保留2,000项且窗口未关闭。
- 样例大小569,344字节；SHA-256：`874B7C285EFB956442177F8FBCB2979B7C56310783A3D8D4F6079A4085BD4AED`。

## 45. V1.16.5：撤销重做层级定位与规则报告列宽交互

### 45.1 需求25实现与失败证据

- 修改前，撤销/重做虽然按数据库ID恢复了数据对象和原编辑列，但只选择最后一个当前单元格；新增专项测试稳定失败并报告`BIT name was not visibly highlighted`，旧滚动条的排队恢复也可能覆盖最终居中位置。
- V1.16.5在数据库回放和表格重载完成后增加专用定位阶段：Module、Register和BIT名称分别在三张表中保持高亮，目标行分别居中；原编辑字段与名称不同时同时选中，并继续作为当前单元格。
- 最终居中回调排在既有视口恢复回调之后，避免旧滚动条再次覆盖定位；撤销和重做共用同一实现，不改变数据库操作栈、修订号、保存状态或业务数据。
- 专项测试在非首屏BIT详细描述上执行编辑、切换到其他Module、撤销、再次切换和重做，验证数据、三级稳定ID、三级名称选择、原编辑字段焦点及三张表居中全部通过。

### 45.2 需求26实现与失败证据

- 修改前，“说明”列被配置为`QHeaderView.Stretch`，因此与“生成名称”之间的分隔线不可手动调整；新增断言稳定失败并报告“说明列不支持在表头左右拖动”。
- V1.16.5把“说明”恢复为交互式列，首次宽度设为320逻辑像素；“生成名称”作为末列使用剩余空间，因此拖宽说明列时会相应让出空间。
- 规则表禁用自动换行并使用右侧省略显示，长说明不会撑宽或抬高整行；轻量数据模型为单元格提供完整提示文本，模块筛选和后台报告刷新保留用户调整后的列宽。
- 200项报告连续10次刷新最慢约0.0070秒、平均约0.0053秒；新生成的2,000项准确错误库前台打开约0.156秒，报告保持打开时保存及后台刷新约0.064秒。

### 45.3 完整验证与样例说明

- Python 3.9.13语法检查和74项单元测试全部通过；23组离屏UI流程最终全部通过，覆盖需求25/26专项、保存、搜索、上下文详情、DPI、行操作、状态栏、规则报告、默认值异常、Bifang实库、两个旧版本数据库以及5,120寄存器大库。
- 5,120寄存器库四类普通编辑约0.011～0.023秒，保存返回约0.095秒，完整后台检查约0.89～1.29秒，单Module增量检查约0.032～0.127秒，峰值工作集约155.5 MiB；未出现数据库锁或线程残留。
- 第一轮2,000项样例测试真实返回1,999项并按失败停止。核对发现`release\samples\LSR_2000_Rule_Errors.db`在2026-08-19 14:59:45被保存过，首个名称已经从非法`0REG_0001`改为合法`REG_0001`；该文件完整性为`ok`，本轮未覆盖用户修改。
- 随后在`test_output`用确定性生成器创建全新测试库，生成门禁确认2,000错误/0警告且UI流程通过；新测试库SHA-256为`874B7C285EFB956442177F8FBCB2979B7C56310783A3D8D4F6079A4085BD4AED`。
- 任务开始时检测到用户正在运行V1.16.3/V1.16.4，因此没有执行峰值约436 MiB的51,200寄存器压力库，避免额外内存竞争；本版未修改规则算法、数据库或大库读写路径。

### 45.4 发布与成品验证

- 发布版本统一为V1.16.5：运行时`__VERSION__=1.16.5`、窗口标题为`RegisterManager 1.16.5 - 苏州领慧立芯科技有限公司`、构建目录为`build_v1.16.5`、成品为`release\LSR_V1.16.5.exe`、清单版本为`1.16.5.0`。
- 成品`--smoke-test`和`--shutdown-smoke-test`退出码均为0；独立日志确认`START version=1.16.5`、V1.16.5窗口标题和`CLEAN_SHUTDOWN`。
- 首次版本矩阵命令误取XML声明的`version="1.0"`而不是程序集版本，该次诊断不计为通过；改用锚定`assemblyIdentity`值的精确门禁后确认清单为`1.16.5.0`，产品文件本身未出现版本回退。
- 成品大小56,500,468字节；SHA-256：`8F2CAB4553BB2DA0C94F326EF3B73344B17D8B4E35A5DA9BAFE22E8A26644D05`。
- Windows FileVersion/ProductVersion资源仍为空，没有沿用旧版号；运行时、窗口标题、EXE文件名和程序集清单四处版本一致。
- 发布测试后`release\Bifang.db`的SHA-256仍为`4493D9000D6B8F35C68622F62DF5FC8F7F849B83AB4354D4636A2F5D8D26C350`，确认真实样例未被修改。

## 46. V1.16.6：规则定位与撤销重做主次高亮区分

### 46.1 需求27实现

- 规则报告定位不再把Module、Register和BIT三个层级都显示成无法区分的同级灰色选择；实际规则字段成为唯一深蓝主定位，所属Module/Register/BIT名称使用当前Qt主题的非活动选择色作为浅灰辅助路径。
- Module级、Register级和BIT级规则分别确定自身实际字段；名称本身报错时名称直接作为深蓝主定位，不再叠加浅灰。非模态规则报告保持打开，主定位表格获得当前选择和键盘焦点，可直接继续编辑。
- `Ctrl+Z`和`Ctrl+Y`复用相同机制：该次实际编辑字段是唯一主定位，三级名称是辅助路径；原编辑列、稳定数据库ID及三张表居中能力保持V1.16.5行为。
- 主次标记只写入`QTableWidgetItem`的临时显示角色，不进入数据库；普通点击、搜索、下一次规则定位、下一次撤销/重做和主题切换均清除旧标记，避免残留误导。

### 46.2 失败证据和修复过程

- 修改前两个专项用例稳定失败并报告`location highlight roles are not implemented`，证明旧版无法表示唯一主定位与辅助路径。
- 第一版颜色实现发现真实缺陷：写入Qt背景/前景显示角色会触发`itemChanged`，纯颜色变化被旧保存处理器当成编辑，撤销历史由真实1项膨胀为11项并破坏重做。测试只使用临时数据库；最终在写入和清除显示角色期间阻断对应表格信号，颜色变化不再触发数据库处理或操作栈。
- 专项测试随后两次暴露测试工具前置错误：空占位单元为`None`却直接调用`.data()`，以及错误假设`mark_saved()`会清空已有历史。测试分别改为跳过空单元、以编辑前游标为基线后，原产品断言不降低并完整通过。
- 最终专项确认一次BIT编辑只新增一个历史命令；撤销、普通点击清色和重做均增加零项，重做分支不会因纯UI标记被截断。

### 46.3 回归与性能验证

- Python 3.9.13语法检查及74项单元测试全部通过；23组离屏UI回归全部通过，覆盖Module/Register/BIT三类规则错误、撤销/重做、普通点击清色、搜索、规则报告列宽、保存、默认值异常、旧库兼容及5,120寄存器大库。
- 规则报告200项连续刷新最慢约0.0057秒、平均约0.0052秒；精确2,000项测试库前台打开约0.156秒，报告保持打开时保存及后台刷新约0.066秒，需求24/26性能无回退。
- 5,120寄存器库四类并发编辑约0.011～0.025秒，保存返回约0.091秒，完整检查约0.80～1.15秒，单Module增量检查约0.032～0.124秒，峰值工作集约155.2 MiB。
- 任务开始时已有用户LSR进程PID 6524/18536，未执行峰值约436 MiB的51,200寄存器压力库以避免内存竞争；本版未改数据库、规则算法或大库读写路径，两项用户进程全程未关闭。

### 46.4 发布与成品验证

- 发布版本统一为V1.16.6：运行时`__VERSION__=1.16.6`、窗口标题为`RegisterManager 1.16.6 - 苏州领慧立芯科技有限公司`、构建目录为`build_v1.16.6`、成品为`release\LSR_V1.16.6.exe`、清单版本为`1.16.6.0`。
- 成品`--smoke-test`和`--shutdown-smoke-test`退出码均为0；隔离日志确认`START version=1.16.6`、V1.16.6窗口标题及`CLEAN_SHUTDOWN`。
- 成品大小56,501,221字节；SHA-256：`969A46A0BA050E21C9336DD062535D7EDE720220FA6D89304122529256617190`。
- Windows FileVersion/ProductVersion资源仍为空，没有携带旧版值；运行时、窗口标题、EXE文件名及程序集清单版本一致。
- 发布测试后`release\Bifang.db`的SHA-256仍为`4493D9000D6B8F35C68622F62DF5FC8F7F849B83AB4354D4636A2F5D8D26C350`，确认真实样例未被修改。

## 47. V1.17.0：按Module生成动态位宽FUSE MAP

### 47.1 需求和模板分析

- 用户提供的`release\FUSE_MAP.xlsx`实际没有Register名称列：A/B分别为绝对地址和FUSE标签，C～J为8位BIT名称，K/L为HEX/十进制，M～T为逐位值，V～Y为四个Part；新功能以明确需求为准新增C列Register名称，因此8位布局整体变为A～Z。
- 参考表通过合并BIT名称单元格表达多位字段，并使用低饱和浅色、细边框和紧凑逐位值列；V1.17.0延续这一视觉语言，但真正隐藏十进制列、冻结首行并自动重算公式。
- 参考表在分析、渲染和哈希校验期间只读；任务前后副本SHA-256均为`2988C7407D8696DFCD4A40D651DC77B85F72EFB30A490D726D9BBFB53BFFF404`，没有改写用户模板。

### 47.2 功能实现

- “工具”菜单在“搜索”后新增并列项`gen_fuse_map...`；点击后使用标准Module选择框，显示序号、名称、基地址和寄存器数量并默认指向主界面当前Module，再使用标准另存为窗口输出`<Module>_FUSE_MAP.xlsx`。
- 入口复用全部生成器的显式保存和规则结果门禁；生成数据在写文件前从ORM对象快照为普通不可变结构，文件写入不再持有数据库延迟查询或跨线程对象。
- 新增`fuse_map_generator.py`，只使用Python标准库生成便携OOXML，不启动Excel、不依赖Office、`openpyxl`或`xlsxwriter`，PyInstaller单文件成品可直接携带该能力。
- A列严格使用Module基地址加Register偏移；B列按8/16/32位分别显示byte/half-word/word序号；C列为Register名称。BIT名称由高到低排布，多位BIT按真实起始位/位宽合并，空闲和混合位宽的高位区使用灰色。
- HEX列由隐藏十进制列计算，十进制列由逐位值乘权累加；逐位初值严格从BIT默认值和位置组合，不消费可能过期的Register缓存。BIT默认值缺失、越界、重叠或超出Register范围会明确拒绝生成，不用0或RESERVED掩盖真实问题。
- 8位表固定为D～K名称、L HEX、隐藏M、N～U逐位值、V分隔、W～Z Part；16位表扩展为D～S名称、T HEX、隐藏U、V～AK逐位值、AL分隔、AM～AP Part。混合位宽Module使用最大位宽排版，每行仍按自身位宽计算。
- 四个Part列沿用参考表`131M08/131M06/131M04/131M02`，初始写入当前默认HEX的独立文本副本；用户编辑逐位值后可把HEX复制到目标Part，不会因共享公式联动覆盖其他Part。

### 47.3 Excel和自动测试

- 新增5项生成器单元测试，覆盖8位A～Z精确列位、16位扩展到AP、多位BIT合并、HEX/隐藏十进制公式、默认逐位值、混合8/16位地址真实性、BIT重叠拒绝及BIT默认值`NULL`不得静默归零。
- 新增`ui_fuse_map_smoke.py`，真实触发工具菜单、Module选择和另存为路径，断言生成XLSX、合并范围、隐藏列和公式；成品`--smoke-test`也实际生成并验证一个XLSX ZIP包，然后清理测试文件。
- 独立Spreadsheet解析工具确认8位文件范围A1:Z3、16位文件范围A1:AP3，公式均被识别，最终扫描没有`#REF!/#DIV/0!/#VALUE!/#NAME?/#N/A`；8位和16位全宽PNG渲染确认合并、颜色、边框、隐藏列及Part区布局正常。
- 可直接体验的测试文件为`test_output\fuse_map_generated\FUSE8_FUSE_MAP.xlsx`和`FUSE16_FUSE_MAP.xlsx`，大小分别为4,191和4,418字节；其SHA-256分别为`0DA3551F4CF9E037FA6CA7A1A64046292563BF45EE7BAEA4C20FE3DFBE520D68`和`7C648DD9D3424D7A62BEDE62435BB735623F11A16AA303127E3DFCE8DFF8A9D6`。

### 47.4 回归、异常记录和性能

- Python 3.9.13语法检查及79项单元测试全部通过；21组常用离屏UI流程中20组直接通过，需求16首次错误使用不含BIT枚举值的普通样例而在测试前置处失败，改用标准5,120寄存器样例后四类编辑约0.013～0.026秒并通过，该次不计为产品失败。
- 5,120寄存器大库完整后台检查约0.88～1.31秒，单Module增量检查约0.041～0.138秒，检查期间编辑约0.016秒，峰值工作集约155.6 MiB；本需求没有引入数据库全库读写或UI线程Excel依赖。
- 既有发布目录的2,000错误样例此前被用户改过首项，按严格门禁返回1,999项并失败；未覆盖该文件，改在`test_output`确定性生成新样例后准确得到2,000错误/0警告，报告打开约0.161秒、保存后后台刷新约0.077秒。
- 源码烟测与关闭烟测首次被并行启动，关闭烟测没有设置隔离日志目录，尝试写入正被用户V1.16.6会话占用的当前`LSR.log`并以`PermissionError`明确失败；改用独立`LSR_LOG_DIRECTORY`并顺序执行后两项均通过。这是测试隔离配置错误，不隐藏异常，也不修改产品“一次打开只保留一份当前日志”的策略。
- 测试结束后没有任务Python或Node残留；用户原有V1.16.6 PID 44940/36348保持运行且未被关闭。未执行峰值约436 MiB的51,200寄存器压力库，避免与用户当前进程争抢内存，本需求也未改变大库和规则算法。

### 47.5 发布验证

- 版本矩阵一致：运行时`__VERSION__=1.17.0`、窗口标题`RegisterManager 1.17.0 - 苏州领慧立芯科技有限公司`、构建目录`build_v1.17.0`、成品`release\LSR_V1.17.0.exe`、程序集清单`1.17.0.0`。
- 成品`--smoke-test`退出码0并实际完成FUSE MAP生成路径；`--shutdown-smoke-test`退出码0，隔离日志确认`START version=1.17.0`、V1.17.0窗口标题和`CLEAN_SHUTDOWN`。
- 成品大小56,509,861字节；SHA-256：`87718459E818C31470FCF57C73CD9612D703EF1284F189A5603C3E79732B5CA4`。
- 发布后`release\Bifang.db`的SHA-256仍为`4493D9000D6B8F35C68622F62DF5FC8F7F849B83AB4354D4636A2F5D8D26C350`；真实数据库和参考Excel均未被测试改写。

## 48. V1.17.1：FUSE MAP桌面Excel兼容、菜单归位和文件命名

### 48.1 故障证据与根因

- 用户生成的`TRIM_MMR_FUSE_MAP.xlsx`在桌面Excel中提示修复并在确认后变为空表；本次保留的Excel修复日志明确记录`/xl/worksheets/sheet1.xml`加载错误且整个工作表部件被替换，没有把真实故障隐藏为提示或自动忽略。
- 工作表XML本身可以被宽松的第三方解析器读取，但OOXML节点顺序把`autoFilter`放在`mergeCells`之后，不符合桌面Excel采用的严格顺序；这也解释了V1.17.0自动解析和渲染通过、真实Excel仍失败的测试缺口。
- FUSE MAP并不需要筛选功能，V1.17.1移除该多余节点，避免在合并BIT名称的工作表中引入严格模式兼容风险；公式、合并、隐藏列、颜色、地址和默认值计算均未改变。

### 48.2 菜单与命名修正

- `gen_fuse_map...`从“工具”顶层移入“工具 → Gen Document”子菜单，与“生成寄存器文档...”并列；规则检查和搜索的位置不变。
- 默认导出名改由当前数据库文件名生成并统一为大写，例如`Bifang.db`对应`BIFANG_FUSE_MAP.xlsx`；Module选择仍只决定导出的寄存器内容，不再改变文件名。
- 文件名转换过滤Windows非法字符并提供`REGISTER_FUSE_MAP.xlsx`安全兜底；默认目录和用户上次导出目录记忆逻辑保持不变。

### 48.3 验证范围与异常处理

- Python 3.9.13语法检查和81项单元测试全部通过；新增数据库名命名及空路径兜底测试。24组离屏UI回归全部通过，覆盖FUSE菜单/命名流程、Bifang真实库、两个旧版库、5,120寄存器大库、2,000条规则错误、并发保存、撤销重做和既有基础异常场景。
- 5,120寄存器大库四类编辑约0.012～0.023秒，保存返回约0.093～0.094秒，完整检查约0.86～1.15秒，单Module增量检查约0.035～0.127秒，峰值工作集约152.2 MiB；本补丁未改变数据库和规则算法。
- 用隔离桌面Excel进程真实打开8位、16位和Bifang真实TRIM_MMR三类输出，分别确认2行/26列、2行/42列和55行/26列，Register内容、隐藏十进制列和HEX公式均存在；没有产生修复日志，隔离Excel PID 41952在验证后正常退出，用户原有Excel PID 13676未被触碰。
- 首轮成品烟测真实失败并报告“Tools action is missing”：门禁遗留了“FUSE入口必须位于工具顶层”的旧条件，同时下一条又要求入口不得位于顶层。该测试矛盾未被绕过，删除过期条件后重新打包，最终成品生成烟测和受控关闭烟测均为0。

### 48.4 发布验证

- 版本矩阵一致：运行时`__VERSION__=1.17.1`、窗口标题`RegisterManager 1.17.1 - 苏州领慧立芯科技有限公司`、构建目录`build_v1.17.1`、成品`release\LSR_V1.17.1.exe`、程序集清单`1.17.1.0`。
- 最终成品`--smoke-test`和`--shutdown-smoke-test`退出码均为0；隔离日志确认`START version=1.17.1`、V1.17.1窗口标题及`CLEAN_SHUTDOWN`。
- 成品大小56,510,216字节；SHA-256：`CB2EB12FFAC6DF75DBBF78215064CA44D84DED7BEDEC490429F28EB53179FE68`。
- 发布后`release\Bifang.db`的SHA-256仍为`4493D9000D6B8F35C68622F62DF5FC8F7F849B83AB4354D4636A2F5D8D26C350`，确认真实数据库未被修改；任务结束时没有Python、V1.17.1或任务Excel进程残留。

## 49. V1.17.2：FUSE MAP框线、地址、Part及工具菜单修正

- 需求归档：用户确认FUSE MAP不是普通小改动，而是LSR的重要核心功能；需求总览已把需求28类型提升为“重要核心功能”，并把命名、动态位宽、框线、公式、地址、Part、菜单、视觉检查和桌面Excel兼容列为不可省略的完整范围。

### 49.1 Excel布局修正

- 依据用户在桌面Excel中的截图复现多位BIT合并框线缺失：旧生成器只写合并范围左上角单元格，其余底层单元格没有样式记录，Excel无法稳定继承完整外框。V1.17.2先给BIT名称区每个底层单元格写入相同的完整细框线，再执行合并；8位覆盖D～K，16位按同一规则动态扩展到D～S。
- A列表头由`ABS_ADDR`改为`REG_ADDR`，绝对地址仍按Module基地址加Register偏移地址计算，但改为大写十六进制`0x...`文本；桌面Excel实际显示8位样例`0x110`、16位样例`0x200`和Bifang TRIM_MMR首地址`0x200`。
- 默认Part表头由参考示意名称改为`PART1/PART2/PART3/PART4`，用户可按具体产品手动改名；各行Part初值继续是默认HEX的独立文本副本。
- `Gen Document`和`gen_fuse_map...`恢复为“工具”下的两个同级菜单项，`gen_fuse_map...`紧邻并位于`Gen Document`下方；规则检查和搜索顺序保持不变。

### 49.2 公式、视觉与桌面Excel验证

- 8位专项以BIT默认值`0xA5`验证N～U逐位值严格按`128/64/32/16/8/4/2/1`加权到隐藏M列，L列`DEC2HEX(M2,2)`结果为`A5`；16位专项以`0x1234`验证V～AK按bit15～bit0权重组合到隐藏U列，T列`DEC2HEX(U2,4)`结果为`1234`。
- 独立Spreadsheet解析确认8位A1:Z2、16位A1:AP2和Bifang A1:Z55的表头、地址、公式、BIT值及Part列正确，三份文件均无公式错误；全宽渲染视觉检查确认8位D～K、16位D～S及Bifang 54个Register行的合并框线连续，没有截图中的断线。
- 隔离桌面Excel最终真实打开三份工作簿并通过：8位26列、16位42列、Bifang 55行/26列，地址均以`0x`显示，关键合并区域四边框存在，逐位加权结果准确，且未生成任何Excel修复日志；最终隔离Excel PID 48568正常退出。
- 桌面Excel验证脚本前两轮分别暴露二维数组下标写法和16位Part列仍按W列读取的测试工具错误；两次任务Excel PID 45232/44364均在`finally`中按精确PID退出。修复验证工具后以相同产品断言重跑通过，没有把工具失败记为产品通过或降低检查范围。

### 49.3 回归、性能和发布

- Python 3.9.13语法检查和81项单元测试全部通过；24组离屏UI回归全部通过，覆盖FUSE菜单/命名/生成、两套旧版库、Bifang真实库、5,120寄存器大库、2,000项规则报告、保存、并发检查及撤销重做。
- 5,120寄存器库四类编辑约0.014～0.026秒，保存返回约0.094～0.101秒，完整检查约0.87～1.19秒，单Module增量检查约0.033～0.127秒，峰值工作集约152.2 MiB；本补丁未修改数据库、规则算法或大库读写路径。
- 版本矩阵一致：运行时`__VERSION__=1.17.2`、窗口标题`RegisterManager 1.17.2 - 苏州领慧立芯科技有限公司`、构建目录`build_v1.17.2`、成品`release\LSR_V1.17.2.exe`、程序集清单`1.17.2.0`。
- 成品`--smoke-test`和`--shutdown-smoke-test`退出码均为0，日志确认V1.17.2窗口标题和`CLEAN_SHUTDOWN`；成品大小56,510,137字节，SHA-256为`BCC31CF7A975EBDA5ABE1D2D05BB7BB253A9EC39195CB405D5B6663317D34821`。
- 发布后`release\Bifang.db`的SHA-256仍为`4493D9000D6B8F35C68622F62DF5FC8F7F849B83AB4354D4636A2F5D8D26C350`，真实数据库未被改写；用户已有LSR进程未被关闭。

## 50. V1.17.3：FUSE MAP空片默认值修正

### 50.1 问题与实现

- V1.17.2把数据库BIT逻辑复位默认值拼成FUSE MAP初值，导致Bifang的`TEC_CH0_0`等行初始为`80/40`；但FUSE MAP描述的是未烧写空片，物理熔丝初态应与寄存器逻辑复位默认值解耦并固定为全0。
- V1.17.3保留BIT/Register默认值的数据库真值、合法性检查和其他生成行为，只把FUSE MAP逐位输入、隐藏十进制缓存、HEX缓存及PART1～PART4初值统一为0；8位输出`00`，16位输出`0000`。
- PART1～PART4单元格使用Excel内建文本格式，保留`00/0000`前导零；用户从HEX列归档结果时使用“选择性粘贴 → 值”，避免普通公式复制产生相对引用偏移。

### 50.2 专项验证

- 以数据库BIT默认值`0xA5`和`0x1234`构造反向测试，确认8位N～U、16位V～AK仍全部为0，隐藏十进制为0，HEX分别为`00/0000`，四个Part为等宽文本零值；公式、BIT合并和完整框线保持不变。
- 真实`release\Bifang.db`的TRIM_MMR共54个Register全部生成全0逐位初值；生成前后源库SHA-256均为`4493D9000D6B8F35C68622F62DF5FC8F7F849B83AB4354D4636A2F5D8D26C350`，没有改写数据库。
- 独立Spreadsheet解析和全宽渲染确认8位A1:Z2、16位A1:AP2及Bifang A1:Z55无公式错误、框线和布局未退化；隔离桌面Excel分别打开三份文件，无修复日志，地址、公式、前导零文本格式和Bifang全行零值均通过。

### 50.3 完整回归与发布

- Python 3.9.13语法检查和81项单元测试全部通过；24组离屏UI回归全部通过，覆盖FUSE菜单和生成、Bifang、旧版库、5,120寄存器大库、2,000项规则报告、保存、并发检查及撤销重做。
- 版本矩阵一致：运行时`__VERSION__=1.17.3`、窗口标题`RegisterManager 1.17.3 - 苏州领慧立芯科技有限公司`、构建目录`build_v1.17.3`、成品`release\LSR_V1.17.3.exe`、程序集清单`1.17.3.0`。
- 成品`--smoke-test`和`--shutdown-smoke-test`退出码均为0，隔离日志确认`START version=1.17.3`、V1.17.3窗口标题和`CLEAN_SHUTDOWN`；成品大小56,510,186字节，SHA-256为`FEFBDA3CB657968E6AAA755382F6469C69105702F2E98C004A24D455A5F20BF8`。
- 任务启动的Excel、Python、PyInstaller和测试版LSR进程均已退出；任务开始前的用户Excel进程PID 48132保持运行，未被关闭。

## 51. V1.17.4：BIT取值原文编辑与FUSE菜单统一

### 51.1 当前有效行为

- BIT取值使用普通表格原文单元格，不提供进制下拉、固定前导或自动转换；编辑一条取值只写回该单元格，不联动修改同一BIT的其他枚举行。
- Module、Register、BIT复制粘贴、Add、Copy、Delete、撤销重做和保存均沿用原始数据，不创建或传播进制元数据。
- `BIT_ENUM_OUT_OF_RANGE`保持最低级warning，并宽松兼容整数、十进制、`0x`、`0b`、历史`b`及裸`1x`等位模式；2位字段中的裸`10/11`可按二进制兼容解释，8进制不纳入支持范围。
- `Gen Fuse Map`使用规范标题，与`Gen Document`保持相邻同级，并在初始化、DPI切换和界面缩放时使用相同字体；FUSE MAP动态位宽、空片全0、Excel公式和Part文本格式保持不变。

### 51.2 验证基线

- 旧库原文专项在LSR 1.1.3真实数据库临时副本中放入`0x3/b10/1x`，确认界面完整原文显示，编辑`0x3→0x4`后只有目标行变化且`PRAGMA quick_check=ok`。
- 首轮规则专项把合法十六进制`0xF`误走入通配符分支而失败；修正为优先识别明确`0x`前导后，16项规则检查通过。
- Python 3.9语法检查、86项单元/集成测试和25组离屏UI回归全部通过；5,120寄存器大库、2,000错误报告、旧库、Bifang、FUSE MAP、保存、撤销重做和并发检查均通过。
- 成品启动和关闭烟测通过，V1.17.4成品SHA-256为`172DCD834087CC8FC6481BA17E19515FDB63566AFB2C3E6793BB4BDA3A784546`；`release\Bifang.db`未修改。

## 52. LSR 2.0：正式发布整理

### 52.1 发布范围

- LSR 2.0以V1.17.4已验证功能为唯一代码基线，不增加新的数据库字段或业务行为。
- 软件内部版本、窗口标题、构建目录、程序集清单、EXE名称和发布目录统一升级为`2.0`。
- 权威需求文档只保留当前有效功能；发布包包含精简说明、当前脚本、配置、样例和唯一的`LSR_V2.0.exe`启动入口。

### 52.2 发布验收

- Python 3.9.13语法检查通过，86项单元/集成测试和25组离屏UI回归全部通过；覆盖旧库、Bifang、5,120寄存器大库、2,000错误报告、FUSE MAP、保存、撤销重做和并发检查。
- 5,120寄存器库打开后全量检查约1.24秒，保存返回约0.10秒，单Module增量检查约0.13秒，检查期间编辑约0.017秒；并发修改、WAL收敛和内存回落通过。
- 语法门禁首次因Windows通配符未展开、第二次因历史脚本路径写错而失败；修正测试命令后以实际源文件重跑通过，两次均为测试命令问题，未弱化产品断言。
- 首轮成品冒烟用PowerShell直接调用GUI EXE时无法取得`$LASTEXITCODE`，不计为通过；改用可跟踪PID的`Start-Process -Wait -PassThru`后，`--smoke-test`和`--shutdown-smoke-test`退出码均为0。
- 首个2.0成品的Windows文件属性未携带版本资源；增加`LSR.version_info`并重新打包后，`FileVersion=2.0`、`ProductVersion=2.0`，窗口标题和日志均显示`RegisterManager 2.0`。
- 最终成品为`LSR_V2.0.exe`，大小56,512,375字节，SHA-256为`2A81A07684F1D5917360B4A1CE0C46DE4957ED10877A0F7A19734CA51822C648`；独立发布目录`C:\codex_area\LSR_V2.0`只保留唯一2.0 EXE启动入口。

## 53. LSR 2.0：完整验证计划补全与执行

### 53.1 验证计划和测试隔离

- 新增`LSR_V2.0_详细验证计划.md`，把版本、86项单元/集成、25组UI、旧库/异常库、5,120与51,200寄存器性能、全部生成器、FUSE MAP、RTL语法、物理跨屏和成品关键路径统一成一套有门限的发布验证计划。
- 全部数据库编辑、迁移、保存和合并均要求使用临时副本；`ui_requirement16_smoke.py`首次违反该约束并污染5,120样例后，已修复为内部创建临时副本、确定性重建样例并确认最终SHA-256恢复且复跑不再变化，真实故障未被隐藏。
- 新增`ui_generator_matrix_smoke.py`，对15个现有生成器建立统一的有效数据库输入，检查输出存在、非空及DOCX ZIP结构；首次历史类名映射错误明确失败，修正测试别名后15项全部完成。

### 53.2 基础、UI、兼容与稳定性结果

- 77个项目Python文件语法通过，86项单元/集成测试全部通过，25组UI回归全部通过；5,120库打开返回0.391秒、全检1.196秒、保存0.106秒、增量0.160秒，2,000错误报告打开0.166秒、保存刷新0.100秒。
- 两套旧库、Bifang以及零字节、损坏SQLite、缺字段、孤儿行和扩展表/列等异常场景通过；测试后六个受保护数据库SHA-256全部与基线一致。
- 规则报告累计40轮、400次200错误刷新最大0.008593秒；保存/撤销定位/旧库往返15轮通过，未发现Python/Qt native崩溃、持续内存增长或任务进程残留。

### 53.3 FUSE MAP与成品门禁

- FUSE MAP的8位、16位、混合位宽和Bifang输出通过结构、公式错误扫描及全宽渲染；桌面Excel真实只读打开8位26列、16位42列和Bifang 55行文件，无修复日志，`0x`地址、完整合并边框、全0空片值、隐藏十进制列和Part文本前导零均通过。
- 现有`C:\codex_area\LSR_V2.0\LSR_V2.0.exe`的FileVersion/ProductVersion/文件名均为2.0；复制到中文加空格目录后的`--smoke-test`及`--shutdown-smoke-test`退出码均为0，本次没有重新打包或覆盖发布目录。

### 53.4 阻断发现

- 51,200寄存器库的正确性、并发编辑、WAL和内存回落通过，但保存最慢1.381秒超过1秒目标，全量检查最慢22.36秒超过20秒目标，性能阶段失败。
- 生成器文件非空不等于RTL有效：统一Icarus语法门禁发现`gen_scan_handler`重复信号/非法空十六进制、`gen_trim_mmr`空目标时尾逗号、`gen_verilog_bus`两个Module缺普通BIT端口并留尾逗号、`gen_uvm_reg_model`把`.db`带入SV标识符，共5项阻断失败。
- 当前自动化会话只见`1280×800`单屏，14寸/27寸物理跨屏和打包EXE可见编辑/保存保留为人工验收；在上述生成器和性能问题修复前，不声明本轮整体验证通过或可重新发布。

## 54. LSR 2.0开发基线：RTL生成器基础修复

### 54.1 实现

- `gen_scan_handler.py`不再对SCAN文本做易产生空串的切片，SCAN=0稳定输出完整的`<width>'h0`；非法数值和位宽溢出明确失败，用户要求忽略的跨Module同名信号逻辑保持不变。
- `gen_trim_mmr.py`按名称字段识别`TRIM_MMR`等Trim Module；没有目标时在创建文件前失败，Trim Register统一强制为8位，非8位明确拒绝。
- 实际Bifang验证发现原生成器读取只读BIT时引用`I_<REG>_<BIT>`却未声明端口；本轮同步修正只读及可写字段端口，使端口声明、Fuse写入和总线读回一致。
- `gen_verilog_bus.py`恢复Python 3.9兼容的普通BIT端口/写逻辑生成，使用声明列表统一处理尾逗号，并为组合读数据增加默认值。
- `gen_uvm_reg_model.py`及主界面调用共同剥离数据库路径和扩展名，对空格、连字符等非法字符作下划线清洗，阻止`.db`进入SystemVerilog标识符。

### 54.2 自动验证

- 新增7项专项回归，先复现SCAN空常量、无Trim目标、Trim识别/8位约束/只读端口、Bus端口与写逻辑及UVM非法标识符，再在修复后全部通过。
- Python 3.9语法检查通过；全量93项单元/集成测试和25组离屏UI回归全部通过。
- 15个代码生成器矩阵全部生成非空结果；8位Trim和两个Bus输出通过Icarus，SCAN输出不再存在空十六进制，剩余报告仅为用户明确接受的跨Module同名声明。
- `release\Bifang.db`的54个8位TRIM_MMR Register生成176,683字节RTL并通过Icarus语法/展开检查；验证前后源库SHA-256均为`4493D9000D6B8F35C68622F62DF5FC8F7F849B83AB4354D4636A2F5D8D26C350`。
- 5,120寄存器UI回归本轮打开返回0.425秒、完整检查1.329秒、保存返回0.112秒、单Module增量检查0.162秒；修改未影响既有性能路径。

### 54.3 工具异常记录

- 首次从PowerShell直接运行MSYS2的`iverilog.exe`时，子进程`ivl.exe`缺少DLL搜索路径并弹出`libgcc_s_seh-1.dll`错误；这是验证工具启动环境错误，不是LSR或数据库故障。
- 后续统一在MSYS2 Bash中设置`/ucrt64/bin`、独立TMP目录后重跑，Icarus正常退出且没有残留`iverilog/ivl/vvp`进程；该真实失败保留记录，没有通过隐藏弹窗计为通过。

## 55. LSR 2.0：RTL生成脚本修复重新发布

### 55.1 发布范围

- 按用户决定，本次只发布需求31的RTL生成脚本修复，不增加大库性能优化；51,200寄存器库偶发超时数据继续保留，但因相同库隔离复测恢复且功能正确，作为非阻断环境波动观察项。
- Trim Register保持8位产品约束；SCAN跨Module同名信号继续按用户决定不修改，避免扩大兼容性范围。
- 运行时`__VERSION__`、窗口标题、PyInstaller输出名、EXE文件名及Windows FileVersion/ProductVersion全部保持为`2.0`。

### 55.2 发布前验证

- RTL专项7/7、全量单元/集成93/93、离屏UI与兼容回归25/25、生成器矩阵15/15全部通过，失败0、跳过0。
- 8位Trim和两个Verilog Bus矩阵文件通过Icarus；SCAN输出不存在空十六进制，UVM package/class不存在`.db`非法标识符。
- 5,120寄存器回归本轮打开0.391秒、全检1.197秒、保存0.107秒、单Module增量检查0.148秒；旧库、Bifang、2,000错误报告、FUSE MAP、保存、并发检查及撤销重做均通过。

### 55.3 成品结果

- 使用项目固定Python 3.9.13和PyInstaller 5.13.2重新构建；成品大小56,517,926字节，SHA-256为`718D3EA2E4B212AB481B64DDE467168F45A26F09DCF53FDD7B9E9837FE0F59D8`。
- 构建目录及中文空格便携目录的`--smoke-test`、`--shutdown-smoke-test`退出码均为0；日志确认`START version=2.0`、标题`RegisterManager 2.0 - 苏州领慧立芯科技有限公司`和`CLEAN_SHUTDOWN`。
- 独立发布目录`C:\codex_area\LSR_V2.0`使用干净白名单暂存后覆盖，根目录只保留唯一`LSR_V2.0.exe`启动入口；外置四个已修复脚本与开发源码SHA-256一致。

## 56. LSR 2.0开发补丁1：检查只读与默认值用户确认

### 56.1 最终需求与安全边界

- 用户明确取消打开数据库后的Register默认值自动修复：打开、SQLite完整性检查、格式兼容检查、前台规则检查和后台规则检查全部改为严格只读，不修改源库或工作副本业务行，不制造未保存状态，不写入撤销栈。
- Bifang中的`TRIM_MMR → TEC_CH0_0/TEC_CH1_0`仍按真实状态报告`REGISTER_DEFAULT_MISMATCH`；规则定位改为高亮该Register首个BIT的默认值单元格，并提示按Enter或重新提交当前BIT值。
- Enter确认和BIT编辑提交都会按全部BIT重新组合Register缓存；BIT文本不变也能触发。发生变化后状态栏显示Register名称及`旧值 → 新值`，修改只进入工作副本、可撤销重做，必须Ctrl+S才写回目标库。
- 项目专用Skill同步增加永久约束：任何可确定性修复也必须先报告并等待可见用户动作，检查失败必须显示真实错误和日志，禁止静默修复或伪装成功。

### 56.2 兼容性流程调整

- `DatabaseWorkspace.open_existing`不再在打开旧库时自动替换源文件；即使历史调用传入`upgrade_source=True`，也只迁移临时工作副本并明确记录打开阶段禁止升级源库。
- 当前格式数据库在规则报告中显示`DATABASE_COMPATIBILITY_OK`提示；旧格式显示`DATABASE_COMPATIBILITY_WORKING_COPY`，明确检测格式、临时工作副本已转换及原数据库未修改。提示不计作错误或警告，不影响绿色检查通过状态和生成门禁。
- 两套真实Register V1.0.0/LSR V1.1.3数据库均在临时副本上验证：打开前后源文件指纹和全部业务字段一致，工作副本业务字段一致且结构可被当前版本读取；只有显式保存后目标库才升级为当前格式并可正常重开。

### 56.3 专项与完整验证

- Bifang专项确认打开后工作副本和目标仍为`0x40/0x40`、操作栈为空且选择不跳动；定位`TEC_CH0_0`后按Enter只把其工作副本缓存更新为`0x80`，状态栏显示`0x40 → 0x80`，撤销/重做和保存后复查均通过，未确认的`TEC_CH1_0`继续报告错误。
- 异常专项覆盖4类可重算缓存及7类非法定义：打开后一律只报告，4类逐项Enter确认形成4个独立命令，连续撤销/重做顺序正确；非法定义始终不被猜测修复。
- Python 3.9语法检查、93项单元/集成测试、25组离屏UI回归和15个生成器矩阵全部通过；5,120寄存器库打开全检1.164秒、保存返回0.094秒、单Module增量检查0.127秒，既有性能路径未退化。
- 集成兼容提示后，首轮后台并发测试和大库增量测试因历史断言要求规则列表完全等于业务错误而失败；保留产品断言并仅排除新增的兼容性“提示”后，同一并发、修订号和全量一致性场景重跑通过，没有删除业务错误或放松错误数量。

### 56.4 正式2.0覆盖发布

- 使用固定Python 3.9.13/PyInstaller 5.13.2构建，构建目录、白名单暂存目录、中文空格便携目录和最终`C:\codex_area\LSR_V2.0`的`--smoke-test/--shutdown-smoke-test`均为0，日志确认`START version=2.0`、窗口标题`RegisterManager 2.0 - 苏州领慧立芯科技有限公司`和`CLEAN_SHUTDOWN`。
- 正式目录根部只保留一个`LSR_V2.0.exe`；FileVersion/ProductVersion均为`2.0`，大小56,517,978字节，SHA-256为`EBB1ABEAEFB25D6A6DD292B372B51B9B87D56D28B6AD2FDCC4D014910E2782EF`。
- `release\Bifang.db`最终SHA-256仍为`4493D9000D6B8F35C68622F62DF5FC8F7F849B83AB4354D4636A2F5D8D26C350`，确认开发、测试和发布过程未改写真值样本。

## 57. 长期开发迁移到本地Work项目

- 以`C:\codex_area\LSR_V1.2.0`作为Codex本地Project主目录，不创建第二套源码，也不把正式发布目录当作开发源。
- 新增`project`治理层，分别维护当前状态、完整需求索引、长期决策、验证基线、发布基线、迁移审计和Work任务模板；根目录需求与开发记录仍是唯一权威来源。
- 更新`AGENTS.md`和LSR项目专用Skill，规定每个新任务先读取项目入口与当前状态，并采用“一项明确成果一个Work任务”的方式继续开发。
- 迁移不移动、不删除、不重命名源码、测试、脚本、固定Python、数据库、发布EXE或历史构建目录；通过关键上下文和全项目SHA-256清单保留可审计证据。
- 需求索引显式保留已取消的3/4号和未正式建立的29/30号，不根据压缩后的对话推测填充；下一项新需求从33开始。
- 迁移校验发现`release\Bifang.db`在历史最后验证之后已有新的修改时间，当前SHA-256为`B4092710A619B7000FD1A3E8F6CBC6A59D1C80BD9DB5A3E92BB7A54E91FCF6F1`，与此前金样本`4493D900...`不同；本次未写库、不自动恢复，差异已作为后续使用前需确认的真实状态记录。正式`LSR_V2.0.exe`哈希仍与发布记录一致。

## 58. Work项目Git基线与Bifang demo定位

### 58.1 用户最终决定

- 用户于2026-08-21明确确认`release\Bifang.db`只是可编辑demo，不是golden sample或必须保持固定内容的文件；历史`4493D900...`与迁移时`B4092710...`哈希均只保留为各自时点的审计证据，不需要恢复、解释或作为发布阻断条件。
- 需求22、31、32及验证基线改为由确定性夹具提供稳定前置数据；当前Bifang demo只有在内容满足相应前置条件时才作为补充体验或烟测输入。任何输入数据库的打开、检查和生成操作仍不得意外改写文件，自动化破坏性、迁移、合并和保存测试继续使用临时副本。

### 58.2 Git与回退基线

- 审计确认`C:\codex_area`及`C:\codex_area\LSR_V1.2.0`此前均不是Git仓库；本轮只在固定Work目录初始化本地仓库，没有把正式发布目录或上级目录作为开发源。
- 首个提交`fae3e0465c69255fa2090069e4f5cfde1bbe157c`以`chore: establish LSR 2.0 development baseline`固定当前2.0开发源码，并建立注释标签`LSR-2.0`。仓库本地提交身份为`Codex <codex@local>`，不修改用户全局Git身份。
- 初始基线跟踪115项源码、测试、脚本、模板、治理文档和必要小型数据库样例；`.gitignore`排除约5.7GB历史构建、固定Python环境、测试输出、缓存、安装包和生成EXE，`release\Bifang.db`作为可编辑demo单独纳入跟踪。
- 长期流程改为一个明确成果一个Work任务；中大型需求使用独立需求分支，正式发布通过门禁后建立注释标签。已合入或已发布内容需要回退时使用`git revert`或从已知标签建立新分支，不用破坏性重置抹除历史。

### 58.3 文档修改、失败证据与验证

- 本轮更新权威需求、验证计划、项目Skill、AGENTS、Work入口、当前状态、需求索引、长期决策、迁移报告、验证/发布基线和任务模板；不修改Python源码、数据库内容或`C:\codex_area\LSR_V2.0`发布目录。
- 首次组合文档补丁因“文档与交付说明”原句与预期上下文不完全一致而被`apply_patch`完整拒绝，没有产生部分修改；重新读取实际段落后拆分补丁并成功应用，没有绕过校验或模糊覆盖大文档。
- `git diff --check`与`git fsck --full`退出码均为0；相对`LSR-2.0`标签的变更仅为治理和权威文档。Bifang demo SHA-256仍为`B4092710A619B7000FD1A3E8F6CBC6A59D1C80BD9DB5A3E92BB7A54E91FCF6F1`，正式`LSR_V2.0.exe`仍为`EBB1ABEAEFB25D6A6DD292B372B51B9B87D56D28B6AD2FDCC4D014910E2782EF`。
- 因本轮没有运行时源码或数据库变更，Python单元/UI/成品烟测均不执行；这不是发布任务，也没有覆盖或重新发布2.0成品。

## 59. LSR 2.0客户快速上手文档

### 59.1 交付范围

- 以`release\寄存器工具快速上手_参考.docx`为内容参考，新增客户交付用`release\寄存器工具快速上手_LSR2.0.docx`；文档采用10章分层结构，包含数据库四层关系、主界面阅读顺序、推荐工作流和文件生成门禁4幅示意图。
- 内容覆盖软件边界、启动和打开数据库、主界面、编辑、刷新编址、规则检查、显式保存、搜索、库追加/合并、文件生成、快捷键、常见问题和交付前自检。
- 按当前2.0权威行为修正旧参考中的自动保存表述：打开和检查严格只读，编辑进入临时工作副本，只有Ctrl+S或“保存”才写回目标数据库；Bifang仅作为可编辑demo，不作固定内容或哈希门禁。

### 59.2 文档验证

- Microsoft Word可正常打开文档、更新目录并导出14页PDF；逐页检查封面、目录、4幅图示、表格、页眉页脚、页码和章节衔接，未发现截断、重叠或异常空白。
- 文档结构审计确认1个纵向Letter节、10个一级标题、28个二级标题和4幅带替代文本的内嵌图；无障碍审计高/中/低级问题均为0。
- 本项只新增和维护客户说明文档及项目记录，不修改Python源码、数据库内容或`C:\codex_area\LSR_V2.0`正式发布目录；下一项功能需求编号仍为33。

## 60. 需求33：Design生成精简与LSR 2.0覆盖发布

### 60.1 实现范围

- “工具 → Run Scripts For Design”固定只调用`gen_mmr_APB`、`gen_parm_addr`和`gen_uvm_reg_model`，输出只包含`APB_mmr`、`Parm_Addr`和`UVM_Reg_Model`三个必要目录；Trim、实例、SCAN和DV mask历史脚本仍保留，但不再进入Design菜单，因此无Trim数据库不再触发已退出流程的`No Trim MMR`错误。
- `gen_parm_addr.py`每次清理自身`Parm_Addr`目录并同时生成`parm_addr_offset.v`和`parm_addr_absolute.v`；前者只含Register偏移地址，后者使用Module基地址加Register偏移地址，文件头明确标识地址模式。
- 客户版`寄存器工具快速上手_LSR2.0.docx`同步说明三个目录和两个Parm文件；项目Skill、构建脚本和发布基线新增永久门禁，要求每个客户发布根目录都包含经渲染检查、可编辑且与开发副本SHA-256一致的快速上手Word。

### 60.2 真实失败与测试隔离修复

- 项目Skill的`quick_validate.py`分别在固定Python和文档运行时执行，均因环境缺少PyYAML而报`ModuleNotFoundError: No module named 'yaml'`；未把校验记为自动通过，改为人工核对frontmatter、名称、描述、必需发布规则和占位符。
- DOCX标准`render_docx.py`因本机缺少LibreOffice报`WinError 2`；保留失败并改用Microsoft Word更新目录、导出PDF，再用Poppler渲染14页逐页复核，结构、图示、表格、页眉页脚均无截断或重叠，无障碍审计0项问题。
- 首轮UI批处理漏传`ui_merge_append_smoke.py`参数，修正调用后重新执行；`ui_position_dpi_smoke.py`又因在可编辑Bifang demo上构造非法BIT位置，于离屏`QMessageBox.warning`路径原生退出`-1073741819`，堆栈定位`LSR.py:7264 handlerSaveBit`。测试改为在临时工作副本动态创建合法尾部位槽后，同一位置/DPI断言通过，没有屏蔽产品警告或降低规则。
- 默认值修复用例曾假设Bifang固定存在两条`0x40→0x80`记录，规则窗口用例也依赖其可变内容；两者分别改为内部确定性夹具和维护的2,000错误样例，完整UI重新运行并通过。Bifang继续是可编辑demo，不恢复历史哈希，也不作为发布门禁。

### 60.3 完整验证与成品

- 78个受控Python文件语法通过；需求33单元/集成专项、全量95/95项单元/集成、27/27组离屏UI和15/15个生成器矩阵全部通过，失败0、跳过0。无Trim Design UI专项确认三个目录均有非空文件、两个Parm文件地址正确且输入数据库SHA-256不变。
- 5,120 Register库本轮打开0.318608秒、全检1.053456秒、保存0.083801秒、单Module增量0.115488秒；需求16三次编辑为0.013840、0.012489和0.011080秒，2,000错误报告和规则报告刷新继续通过既有门限。
- 使用固定Python 3.9.13/PyInstaller 5.13.2重新构建；开发目录、中文空格便携目录和正式目录的启动/关闭烟测均为0。最终`C:\codex_area\LSR_V2.0`根部只有一个`LSR_V2.0.exe`，FileVersion/ProductVersion均为`2.0`，大小56,518,404字节，SHA-256为`DAB5961ED1B389821C9A584505EEF8CA0A1F7149718F626D5F2BAC6549FE011A`。
- 正式目录包含可编辑`寄存器工具快速上手_LSR2.0.docx`，其SHA-256与开发侧一致，均为`C579FF5719C89812A2E7A56185B02385182AFE9EACCF33A557FD33F1E275E084`；发布配置和`gen_parm_addr.py`哈希与开发源一致。
- 需求分支以合并提交`c54be2626fd126fe152169ad83f965477434d8a5`进入`main`；本次正式覆盖发布建立独立注释标签`LSR-2.0-requirement-33`，原始`LSR-2.0`标签保持不动，便于按两个2.0基线分别审计和回退。

## 61. 分阶段Demo、试用确认与正式发布流程

- 根据需求33一次性完成实现、完整回归、客户文档、打包和发布后才进行用户体验确认所暴露的返工成本，后续新功能、界面交互和输出格式变化默认改为三阶段门禁：需求草案/Demo确认、最小功能试用确认、用户明确确认后的完整验证与正式发布。
- 预览阶段只运行使试用可信所需的专项与最低安全检查，试用版本只能位于源码运行环境、`test_output`或其他明确的非发布位置；不得覆盖`C:\codex_area\LSR_V2.0`、建立正式发布标签、更新客户交付包或把预览描述为正式完成。
- 用户提出调整时，只迭代相关实现和专项检查，并把验收口径、反馈、剩余工作及Git检查点写入Work任务；用户明确确认后才冻结需求，执行完整单元/UI/兼容回归，回写权威需求与开发记录，更新客户快速上手，构建、验证和发布正式包。
- 明确、低风险且不存在交互选择的小修复可直接闭环；用户也可以明确要求跳过预览并直接正式完成，但不能由执行者根据版本号或“发布”字样自行推定跳过确认门禁。
- `skill-creator`的`quick_validate.py`分别使用项目固定Python和文档运行时执行，两个环境均因缺少PyYAML报`ModuleNotFoundError: No module named 'yaml'`；未记为自动通过，随后人工核对Skill frontmatter、目录名、描述、占位符、Demo/试用暂停点、正式目录保护、正式完成阶段及任务模板确认字段，全部符合预期。
- 本次仅更新项目Skill、长期决策、Work入口、当前状态和任务模板，不修改产品源码、数据库、客户指南或正式发布目录；运行时测试和成品烟测不适用。

## 62. 需求33打包版Design配置路径补丁与分阶段发布

### 62.1 缺陷、根因与试用确认

- 用户在正式打包版执行“工具 → Gen Scripts For Design”时得到“生成设计文件失败 / `No section: 'design_param'`”。`LSR.spec`已经同时包含包内配置，正式目录也有含`[design_param]`的外置配置；真实根因是三个顶层生成器用`dirname(dirname(__file__))`定位源码根，在PyInstaller one-file临时目录中会越过`sys._MEIPASS`并读取不存在的路径。
- `gen_mmr_APB.py`、`gen_parm_addr.py`和`gen_uvm_reg_model.py`统一改用`design_settings.runtime_path('design_param.cfg.txt')`，优先读取EXE同目录的可编辑外置配置，外置文件缺失时回退包内副本；源码运行方式、三个输出目录、Parm双文件和输入数据库只读规则均未改变。
- 按新增分阶段流程先发布到`test_output\LSR_V2.0_REQ33_CONFIG_PREVIEW`，没有覆盖正式目录。用户于2026-08-24实际试用后明确确认“没问题，做的很棒”，并要求进入正式发布流程；确认后才执行本节完整回归、记录回写和正式覆盖发布。

### 62.2 增强烟测与真实失败

- `LSR.py --smoke-test`不再只验证启动：它创建确定性合法、无Trim的临时SQLite夹具，实际运行三个Design生成器，并断言输出目录和Parm文件集合准确、输入数据库SHA-256不变。
- 首个增强预览烟测使用历史`dayu_top.db`时，UVM生成器因样例访问权限空值报`KeyError: None`并退出1；这是新烟测输入不满足生成器前置条件。改为内部确定性合法夹具后重新构建并重跑相同的UVM产品断言通过，没有删除生成器、吞异常或降低断言。
- 正式候选首次由PowerShell直接调用GUI子系统EXE时，`$LASTEXITCODE`为空，按不可判定失败处理；确认无残留后改用`Start-Process -Wait -PassThru`对同一候选重跑，增强启动烟测和关闭烟测退出码均为0。

### 62.3 完整验证与成品候选

- 80个受控Python文件语法通过，96/96项单元/集成、27/27组离屏UI和15/15个生成器矩阵通过，失败0、跳过0。需求33真实Design入口只产生`APB_mmr`、`Parm_Addr`、`UVM_Reg_Model`，Parm双文件正确，输入夹具SHA-256保持`f193280985553be4d8c9b48dc5070fdc1df99a7e263c25aef59691d59d6994d9`。
- 5,120 Register库本轮打开0.330340秒、完整检查1.071391秒、保存返回0.094088秒、单Module增量检查0.126345秒；2,000错误报告打开0.150302秒、保存后刷新0.068564秒，均在既有门限内。
- 固定Python 3.9.13/PyInstaller 5.13.2构建的正式`LSR_V2.0.exe`大小56,519,350字节，SHA-256为`C99A8112A23C589A3118D0A2AF2AC9364C7F1E49377C813F72CBBA2C2AA0329D`；开发目录、中文空格便携目录和正式`C:\codex_area\LSR_V2.0`的增强启动/关闭烟测均退出0，FileVersion/ProductVersion均为2.0且正式根目录只有一个EXE。
- 正式覆盖时保留原外置`design_param.cfg.txt`，覆盖前后SHA-256均为`AA56A9966830785E3D25A1C8129E9CCDEAD5D908C6E7667E18E9FF4473A53584`；客户快速上手SHA-256保持`C579FF5719C89812A2E7A56185B02385182AFE9EACCF33A557FD33F1E275E084`。旧正式目录完整备份到`C:\codex_area\LSR_V2.0.pre_req33_configfix_backup`供回退。
- 补丁分支通过合并提交`ac1cfa6d45c2e36261d6c7aa1aa8c48dbda5d7a4`进入`main`，正式状态建立独立注释标签`LSR-2.0-requirement-33-config-fix`；原始`LSR-2.0`和`LSR-2.0-requirement-33`标签均保持不动。

## 63. 无窗口打包前后生成物差分发布门禁

- 用户要求后续每次发布都自动验证“开发态功能”和“打包后功能”一致，不再依赖人工打开窗口和菜单点击。项目Skill、Work任务模板、验证基线及发布基线已增加命令行差分门禁。
- 门禁使用同一个确定性临时数据库和字节一致的测试配置分别运行源码入口与打包EXE，以相对路径、文件大小和SHA-256形成规范化清单并精确比较；输入数据库哈希必须保持不变，打包执行必须来自成品EXE自身，不能借用工作区源码模块。
- 针对需求33暴露的配置路径问题，门禁固定覆盖外置配置哨兵值和外置配置缺失时的包内回退，并要求正式发布位置再次执行。`No section: 'design_param'`、外置配置被忽略、包内配置漏打、生成文件缺失或内容不同均应得到非零结果并阻止发布。
- 为控制耗时和token，正常路径只输出`PASS/FAIL`、聚合清单哈希、退出码和差异文件名；完整清单与日志保存在`test_output`或验证证据目录，只有出现差异时才读取对应文件内容。
- 本次只固化治理规则，没有修改产品源码、数据库、构建产物或正式发布目录。当前2.0增强`--smoke-test`已覆盖打包内三个Design生成器实际执行，但完整的可留存生成物/清单命令行接口尚未实现；下一次正式发布前必须先作为独立小改动实现、Preview并接入发布命令。
- 用户进一步明确触发边界：完整差分仅在用户明确要求正式发布、客户交付或覆盖正式目录时执行；普通开发、源码验证、Demo、Preview、试用包、本地测试构建和一般版本迭代均不触发，以避免无必要的耗时和token消耗。

## 64. 需求34：Design生成物顺序与界面一致

### 64.1 根因、范围与实现

- LSR V1.1.3编辑Register地址后，旧数据库的关系/ID顺序可能保留历史插入顺序；LSR 2.0界面通过地址及稳定ID规则显示可视顺序，但Design菜单此前把原始`module.regs`直接交给三个生成器，导致生成物顺序与界面不同。
- 用户真实样本中，关系顺序在`0x33`后先出现`0x38、0x39`再出现`0x34～0x37`；界面正确显示地址顺序，而APB MMR曾把`0x34、0x35`等Register放到靠后位置。
- 新增`design_generation_order.py`只读代理：Module和Register按`get_real_module_order`/`get_real_reg_order`得到的当前界面顺序暴露给Design生成器，内部仍引用原SQLAlchemy对象，不改关系、不flush、不写数据库。
- 修改仅接入`handlerRunScriptsForDesign`，同时覆盖`APB_mmr`、两个`Parm_Addr`文件和`UVM_Reg_Model`。三个原始生成脚本字节保持不变；BIT顺序、访问逻辑、其他菜单生成器及数据库格式均不改变。

### 64.2 分阶段确认

- 先在分支`fix-requirement34-design-output-order-preview`制作源码Preview，提交检查点为`8dcd4d0`；Preview输出位于`test_output\LSR_REQ34_DESIGN_ORDER_SOURCE_PREVIEW_20260824_1700`，没有覆盖正式发布目录。
- 确定性夹具和真实Bifang用户样本副本均显示`0x34、0x35、0x36、0x37、0x38、0x39`；真实源库及工作副本生成前后SHA-256均为`A1B8552B4EF2B1A12D258B2E9A79E522016009B22E19299ACB76E14145B5E65D`。
- 用户于2026-08-24明确确认源码Preview可以继续改动，随后才冻结需求34并执行完整源码回归与权威记录回写。
- 用户随后要求直接试用成品Preview；使用固定Python 3.9.13/PyInstaller 5.13.2在`test_output\LSR_V2.0_REQ34_ORDER_PREVIEW\package`构建`LSR_V2.0_REQ34_PREVIEW.exe`，没有覆盖正式目录。Preview EXE大小56,520,358字节，SHA-256为`50358AFAE484C0081B5A1EFFFD11C7EE6BAAC32863F1D29A16C4B97357C47D0E`。

### 64.3 完整源码验证与发布边界

- 83个Git受控Python文件通过Python 3.9语法检查；97/97项单元/集成测试和28/28组离屏UI回归全部通过，失败0、跳过0。
- 需求34UI专项真实经过`handlerRunScriptsForDesign`，断言界面寄存器表及三个Design输出顺序一致、两个Parm文件完整、输入确定性数据库SHA-256不变；需求33Design专项继续通过。
- 15/15个现有生成器矩阵产生非空结果；真实Bifang样本副本生成的`user_pid_mmr.v`通过项目统一Icarus Verilog 2012语法编译。
- 本任务没有打包、没有改动客户快速上手、没有执行只在正式发布时触发的打包前后差分门禁，也没有覆盖`C:\codex_area\LSR_V2.0`或建立发布标签。下一次用户明确要求正式发布时，必须先把“Design输出顺序跟随界面”同步到客户指南，并按项目Skill执行完整发布门禁。
- 上述“没有打包”特指没有执行正式发布构建；后续隔离Preview已完成启动/Design烟测与关闭烟测，退出码均为0，正式EXE及用户正在运行的正式实例未被操作。Preview只用于用户试用，不能替代正式发布差分、客户指南和正式目录复验。

## 65. 需求35整合：统一生成快照与BIT/多生成器顺序一致

### 65.1 实测确认与用户决定

- 对SVD、Gen Document和DV路径分析确认：这些生成器仍可能消费原始关系顺序；C Header/Test Header还会原地排序关系，造成后续生成结果依赖菜单执行历史。
- 使用1.1.3自带`dayu_top.db`测试副本构造合法BIT顺序异常：`IWDG_SR.PVU(id107)`由bit0改为bit1，`RVU(id108)`由bit1改为bit0，其他业务字段不变。1.1.3界面算法及当前界面均应按位置显示`RVU → PVU`，两版APB MMR实际均按关系输出`PVU → RVU`。
- 1.1.3原始APB脚本、当前APB脚本及当前Design菜单排序代理等价路径生成的`iwdg_mmr.v`SHA-256均为`08161AD982B499FE07496504E7FDBC2492FB05A987122C370454DD641955445D`。两版Icarus编译和APB读回仿真均通过，确认RTL映射仍为`PVU → bit1`、`RVU → bit0`，缺陷属于顺序和视觉一致性。
- 用户认可“Preview A：统一生成快照”方案，并决定把SVD、Document、DV、菜单历史依赖和BIT注入暴露的问题合并为需求35的一次改动，不拆分为两个Preview。

### 65.2 冻结方案与兼容边界

- 每个活动生成脚本使用独立的Module/Register/BIT完整快照，顺序来自当前界面稳定规则；脚本对列表的原地排序只能影响自身副本，不能改变ORM关系、其他脚本或数据库。
- 范围覆盖C Header、SVD、Test Header、三个Design输出、DV和Gen Document；Fuse Map保持显式地址/位置实现并只做防退化验证。原始生成脚本必须保持不变。
- SVD/Document/Design/DV的接口外内容按界面顺序；DV和APB MMR既有顶层端口默认保持兼容排列，保护位置实例化。RTL访问逻辑、地址、BIT位段、默认值、set/clr、Key和UVM语义不变。
- 本段是实施前冻结结论；后续源码与成品Preview证据见65.3。正式目录和客户指南仍未修改。

### 65.3 源码实现、专项验证与隔离Preview

- 源码检查点`3ad8d40`把需求34的Register只读视图扩展为每脚本独立的Module/Register/BIT完整快照；C、Test、Design、DV和Document处理函数在每个脚本调用前重新构建快照，旧生成器的`sort`只能改变私有列表，不再污染ORM关系或后续菜单。原始`scripts`目录Git差异为空。
- APB MMR使用“界面Register顺序+旧BIT关系顺序”的端口兼容视图，DV使用“旧Register+旧BIT关系顺序”的完整端口兼容视图；二者生成的端口声明分别与对应修复前兼容基线逐字相同，接口外注释、参数、字段及读回逻辑按界面顺序生成。
- Python语法、需求33/34/35与RTL修复组合单元14/14、需求33/34/35离屏UI 3/3通过。需求35真实入口依次生成C Header、SVD、Test Header、APB/Parm/UVM、DV和DOCX，SVD XML与DOCX结构解析、菜单历史独立性、ORM关系不变和输入SHA-256不变均通过。
- APB与DV输出通过Icarus Verilog 2012编译；两Register、四BIT的APB读回仿真分别交换高低位输入，输出`PASS Register/BIT read mapping preserved after ordering fix`。首次Icarus因默认`C:\msys64\tmp`不可写失败，改用项目独立临时目录后通过；当前UI尝试打开1.1.3注入库时被现行规则门禁阻止，没有绕过门禁或把失败记为通过。
- 隔离成品`test_output\LSR_V2.0_REQ35_GENERATION_SNAPSHOT_PREVIEW\package\LSR_V2.0_REQ35_PREVIEW.exe`大小56,521,848字节，SHA-256为`F6A46AE99D4FC2F677EF069C8AC83D078DBBDA8A15ED0280F85CC0A963B816CC`，FileVersion/ProductVersion均为2.0；增强启动烟测和关闭烟测均退出0，日志记录`CLEAN_SHUTDOWN`，无相关残留进程。
- 该成品只用于用户试用确认。当前未执行高成本完整回归、客户快速上手更新、正式发布前后差分、正式目录覆盖或发布标签；正式`C:\codex_area\LSR_V2.0\LSR_V2.0.exe`SHA-256仍为`C99A8112A23C589A3118D0A2AF2AC9364C7F1E49377C813F72CBBA2C2AA0329D`。

### 65.4 用户确认后的完整源码回归与发布准备

- 用户确认需求35 Preview后要求继续完整回归并正式发布为LSR 2.0。命令行差分门禁检查点`b56b41e`新增全活动生成CLI、外置配置哨兵、包内回退、规范化DOCX清单和正式构建自动调用；聚焦语法、9项组合单元及3项门禁单元通过。
- 88个Git受控Python文件语法、103/103项单元/集成、29/29组离屏UI/兼容回归和15/15个生成器矩阵全部通过。5,120 Register库打开0.349001秒、完整检查1.096412秒、保存0.086488秒、单Module增量0.118766秒；2,000错误报告与报告刷新满足既有门限。
- 需求35输出的APB MMR和DV Verilog通过Icarus 2012编译，APB读回仿真输出`PASS Register/BIT read mapping preserved after ordering fix`；`scripts`目录相对当前正式源码基线没有改动。
- 客户快速上手补充C/SVD/Test/Design/DV顺序、Document高位到低位BIT表、APB/DV端口兼容及生成不改数据库说明。Word/Poppler渲染14页逐页检查通过，无障碍审计0项问题，更新后SHA-256为`57E02460BC46E63FC925D8D57DC2AAF8CB6249C6EA89893461979FCBBA2FC967`。
- 首次生成器矩阵误用当前存在重复Reserved信号的`dayu_top.db`而按规则门禁停止，改用无阻断错误的Bifang demo后15/15通过；Bifang仍不作为golden或固定哈希。首次Icarus因临时目录环境变量不完整失败、标准DOCX渲染器因缺LibreOffice失败，均保留记录并在项目独立临时目录/隔离Word导出路径完成等价复测。
- 首个正式候选构建完成后，自动差分在外置配置哨兵场景真实捕获打包DV生成仍报`No section: 'design_param'`，因此候选未进入发布。保持`gen_verilog_bus.py`原脚本不变，在产品入口增加仅包裹旧生成器构造阶段的运行时配置适配；源码、外置配置和包内回退统一经过`design_settings.runtime_path`。DV输出新增`ADDR_WIDTH=27`哨兵断言后，88文件语法、103/103单元/集成和29/29 UI再次完整复跑通过，失败候选不复用。
- 第二个候选越过配置读取后，差分又发现APB/DV四个文件大小相同但端口顺序不同；最小文本差异定位为未声明`order_by`的ORM关系在源码/打包两个进程中返回顺序不同。兼容端口视图由“依赖查询偶然顺序”收紧为显式主键ID顺序，稳定保留历史插入身份；内部参数、读写逻辑和界面顺序不变。6项需求35/差分单元及真实UI生成专项通过，第二候选不复用。
- 本节尚未声明成品发布完成；候选与正式目录差分、版本/烟测、外置配置保留、正式备份覆盖及发布标签结果在后续小节闭环。

### 65.5 候选差分拦截与正式2.0覆盖发布

- 首个候选在打包DV配置读取处被自动差分拦截，第二候选在跨进程APB/DV端口排列处再次被拦截；两次均保留完整证据、没有覆盖正式目录。产品入口配置适配提交`461a2e4`，稳定历史主键端口顺序提交`5d14aeb`，全部原始生成脚本继续保持不变。
- 第三候选的外置配置哨兵、包内回退和实际候选目录三场景均`PASS/differences=none`；FileVersion/ProductVersion 2.0，增强启动/关闭退出0，37个脚本及客户指南与源一致。
- 正式配置staging复验通过后，旧正式目录整体备份到`C:\codex_area\LSR_V2.0.pre_req35_backup`；新`C:\codex_area\LSR_V2.0`再次完成外置哨兵、包内回退、实际配置三场景差分、增强启动/关闭、版本、48文件清单及无残留门禁。
- 最终`LSR_V2.0.exe`大小56,526,365字节，SHA-256为`632E73851996F40AE14D134B673D18D79B38B9B7B25E59FFD95BDFC14B355B6C`；外置配置哈希覆盖前后均为`AA56A9966830785E3D25A1C8129E9CCDEAD5D908C6E7667E18E9FF4473A53584`，客户快速上手开发/正式哈希均为`57E02460BC46E63FC925D8D57DC2AAF8CB6249C6EA89893461979FCBBA2FC967`。
- 需求34/35至此正式发布为LSR 2.0，后续新需求编号为36；使用独立注释标签`LSR-2.0-requirement-35`固定本次最终源码与发布记录，既有2.0标签不移动。

## 66. 需求36：搜索定位交互与LSR 2.0正式覆盖发布

### 66.1 Preview确认与实现

- 搜索窗口改为非模态单实例，重复打开只激活已有窗口；结果保存对象类型、字段及稳定数据库ID，定位时按当前视觉顺序查回行，不复用旧行号。
- 范围支持`Module名`、`Register名`、`BIT名`、`BIT取值`、`其他属性`和`描述`多选；默认选择前五项且不选描述。名称、BIT取值、其他属性和全部描述栏严格隔离，未选范围时阻止检索。
- Enter/Shift+Enter保持前后循环；目标不可见时滚动定位，已在视野内时只更新主/辅高亮。末项提示继续操作将回到开头，实际回到第一项时显示独立、居中、加粗的醒目提示。
- 用户经过四轮源码Preview确认最终交互，并于2026-08-25明确要求正式发布2.0；实现检查点为`9b245fd`、`6e82780`、`be2418c`和`fc24f8e`。

### 66.2 完整源码与客户文档验证

- 88个Git受控Python文件语法、114/114项单元/集成、29/29组离屏UI/兼容及15/15个生成器矩阵全部通过，失败0、跳过0；6个受保护数据库SHA-256前后不变。
- 5,120 Register库打开0.417049秒、完整检查1.275578秒、保存0.100678秒、单Module增量0.135560秒；需求16三次编辑为0.022877、0.020489和0.016302秒，规则报告刷新最大0.006912秒。
- 客户快速上手同步非模态搜索、多选范围、描述隔离、低跳跃定位和首尾提示；标准LibreOffice/Poppler渲染14页逐页检查通过，无障碍审计高/中/低问题均为0，最终SHA-256为`DD38E204A0C414FF96A7F89C91598FA889239AA0DB774B9BDB79D43A083AF4AE`。
- 构建脚本把正式客户Markdown改为显式白名单，只复制发布说明和开发变更记录；正式包不再包含内部`LSR_功能改造需求.md`或`LSR_V2.0_详细验证计划.md`。

### 66.3 候选、正式差分与成品

- 从空目录构建候选。候选、替换为原正式外置配置的暂存包，以及最终`C:\codex_area\LSR_V2.0`均执行外置配置哨兵、包内配置回退和实际配置三场景生成物差分，所有场景均为`PASS/differences=none`；37个交付脚本逐文件与开发源一致。
- 候选关闭烟测首次未设置离屏Qt环境，隐藏窗口等待超时；确认两个PID均来自候选路径后精确终止，未操作用户进程。改用独立`LOCALAPPDATA/TEMP`和`QT_QPA_PLATFORM=offscreen`后，同一候选及正式位置增强烟测、关闭烟测均退出0，日志记录`CLEAN_SHUTDOWN`且无残留。
- 正式外置`design_param.cfg.txt`覆盖前后SHA-256均为`AA56A9966830785E3D25A1C8129E9CCDEAD5D908C6E7667E18E9FF4473A53584`；旧正式目录完整备份到`C:\codex_area\LSR_V2.0.pre_req36_backup`。
- 最终客户包共46个文件，根目录为唯一`LSR_V2.0.exe`、两份客户Markdown、快速上手、两份配置及`samples`/`scripts`。FileVersion/ProductVersion均为2.0，EXE大小56,528,563字节，SHA-256为`CBB4F925D191C2CC8535C7E99AB670585D502E71FEC161D176623990BF1158FD`。
- 需求36至此正式发布为LSR 2.0，后续新需求编号为37；使用独立注释标签`LSR-2.0-requirement-36`固定最终源码与发布记录，既有2.0标签不移动。

## 67. 需求37：损坏SQLite数据库恢复打开Preview

### 67.1 实现与数据安全边界

- SQLite `quick_check`失败时改为只显示“仍然打开”和“取消打开”，默认取消；取消不切换当前文档，“仍然打开”以只读物理扫描把可恢复的四层业务数据重建到全新当前格式工作副本，损坏源库不作为ORM编辑连接。
- 新增`database_recovery.py`统一完成多条完整性错误采集、当前结构验证、确定性首记录去重、无效/孤儿过滤、恢复统计、当前格式重建及完整`integrity_check`/外键复验。不可读取或已丢失的数据不猜测补齐，核心表或必要字段无法建立有效工作副本时明确失败。
- 恢复会话以标题`*`、左下角“损坏库恢复：待保存”和规则错误`DATABASE_RECOVERY_SQLITE_INTEGRITY_ERROR`持续提示。Ctrl+S再次确认后，在原目录建立逐字节一致且名称唯一的`*.before_lsr_recovery.db`，校验哈希并通过SQLite Backup API、同目录原子替换和保存后复验重建；外部修改即使请求强制覆盖也会阻止恢复保存。
- 保存成功后状态显示数据库已按当前可见数据重建，规则错误转为`DATABASE_RECOVERY_SAVED`信息；另存为只生成干净新库，不宣称原损坏文件已修复。健康库、旧格式兼容、Undo/Redo、搜索、规则定位和未保存生成门禁沿用原路径。
- 源码实现检查点为`46c4d1c`，打包恢复门禁检查点为`78ea231`；正式发布目录和客户快速上手均未修改。

### 67.2 专项验证和真实样本结果

- 39项需求37/数据库工作区/兼容/规则单元与集成测试通过；需求37离屏UI覆盖取消保持原工作区、继续打开、规则报告、待保存状态、保存取消、确认覆盖、精确备份、保存后状态和正常重开。普通保存、1.0.0/1.1.3兼容、默认值修复、状态栏、搜索及规则定位最低安全回归通过。
- `Bifang_err.db`只在临时副本上验证，原始错误为`*** in database main *** / On tree page 4 cell 16: Rowid 841 out of order`。实际扫描/保留为Module 13/13、Register 236/236、BIT 618/581（相同重复20、冲突重复17）、BIT取值441/441；备份与损坏副本SHA-256均为`6EA22C03ABD656377926F81E6177A8D2114628C116EAD4EE5573AB7FE44622FE`，重建库`quick_check`、完整`integrity_check`及`foreign_key_check`通过。
- 已恢复的正常`release\Bifang.db`只作为可编辑demo回归输入，当前SHA-256为`B4092710A619B7000FD1A3E8F6CBC6A59D1C80BD9DB5A3E92BB7A54E91FCF6F1`，没有修改；它仍不属于golden或固定哈希门禁。
- 首次单元夹具误假设损坏页扫描行数仍固定1000，实际物理扫描得到1024行，改为按扫描及确定性去重事实断言后通过；首次UI测试替身缺少`sync()`，补齐后干净重跑通过。若干旧UI脚本首次遗漏必需样本参数而只打印用法，按接口补参后通过。
- 规则定位旧烟测在可编辑Bifang demo的末端层级无法满足严格几何居中断言，改用既有1.1.3兼容样本后通过；该现象与需求37无代码交集，因此没有修改定位实现或弱化测试。

### 67.3 隔离Preview

- Preview为`test_output\LSR_V2.0_REQ37_RECOVERY_PREVIEW\package\LSR_V2.0_REQ37_PREVIEW.exe`，大小56,539,165字节，SHA-256为`0D89AEF0CD94CFC11E82D7F0990AB5F2F17EE2C6EF3BA880343DC49130DBB2A6`，FileVersion/ProductVersion均为2.0；包内附带原始哈希不变的`Bifang_err_试用副本.db`供用户试用。
- 打包后纯命令行恢复门禁、基础功能烟测和正常退出烟测均退出0；门禁从损坏副本恢复13个Module、236个Register、581个BIT和441条BIT取值，精确备份哈希一致，保存后数据库三项完整性复验通过，无Preview/Python残留进程。
- 当前仅完成需求37专项、最低安全回归和隔离Preview。用户明确确认前不执行完整回归、客户快速上手更新、正式生成物差分、`C:\codex_area\LSR_V2.0`覆盖或正式标签。

## 68. 需求37/38合并正式发布：损坏库恢复与寄存器位域图优化

### 68.1 用户确认与实现范围

- 用户实际试用需求37 Preview后确认功能没有问题，并要求与需求38一次性正式发布为LSR 2.0。需求38最终方案为：普通BIT字段使用确定性低饱和色板，Reserved常见命名及未使用位域使用浅灰色，当前字段叠加亮青粗外框、浅色内框和醒目文字，全部字段块首尾相接且无间隙。
- `LSR.py`只修改右下寄存器位域图的绘制样式和选择覆盖层，不改变数据库字段、Register/BIT排序、位号、位宽、权限、默认值、生成器输入或任何RTL/UVM语义。字段配色由视觉顺序确定，不使用Python随机哈希；字体使用随既有DPI/舒适度缩放的Segoe UI无衬线族。
- 需求37沿用已确认的损坏库恢复安全边界：取消打开不切换当前文档；仍然打开只读扫描并建立全新工作副本；Ctrl+S先保存逐字节一致的原损坏库备份，再按当前可见数据原子重建；另存为不覆盖原损坏库。

### 68.2 源码、UI与客户文档验证

- 93个Git受控Python文件语法通过，118/118项单元/集成、30/30组离屏UI/兼容及15/15个生成器矩阵通过，失败0、跳过0。需求38专项断言普通字段颜色互异且低饱和、Reserved精确为中性浅灰、相邻矩形边界严格相等、选中双描边及Segoe UI字重存在；需求37恢复、健康库、旧格式、保存、规则、搜索、DPI和全部既有生成路径继续通过。
- 5,120 Register库本轮打开0.371551秒、完整检查1.199745秒、保存0.101461秒、单Module增量0.143594秒；后台完整检查0.828552秒、UI加载0.332209秒、峰值155.797 MiB。2,000错误报告打开0.156896秒、保存后刷新0.075562秒，报告窗口刷新最大0.005570秒。
- 完整UI回归中，需求35生成快照脚本首次使用了预先创建的输出目录，产品生成已经完成但测试夹具按设计拒绝覆盖已有目录；改用新的不存在目录重跑同一产品断言后通过，没有修改或弱化产品逻辑。
- 客户版`寄存器工具快速上手_LSR2.0.docx`新增损坏库继续打开/备份重建、规则报告和右下位域图说明。标准Documents渲染器因本机未安装LibreOffice明确失败，改用独立隐藏Word进程导出PDF并由Poppler渲染15页逐页检查；无裁切、重叠、坏分页或缺字，无障碍审计高/中/低问题均为0。最终SHA-256为`ECFE88DF3C0EB6B114EB7DA4862F7A8441CAE66F73D80B499CC259A38B0A6978`，用户原有Word进程未被操作。

### 68.3 候选、正式配置暂存与正式成品

- 从空目录构建正式候选。候选及保留现有正式外置配置的暂存包均完成外置配置哨兵、包内配置回退和实际发布配置三场景源码/打包生成物差分，全部`PASS/differences=none`；增强功能、干净退出和损坏库恢复三个成品入口均退出0且没有残留进程。
- 损坏库成品门禁使用原哈希`6EA22C03ABD656377926F81E6177A8D2114628C116EAD4EE5573AB7FE44622FE`的隔离副本，恢复13个Module、236个Register、581个BIT和441条BIT取值，自动备份哈希与输入完全一致，重建库完整性复验通过。可编辑`release\Bifang.db`保持`B4092710A619B7000FD1A3E8F6CBC6A59D1C80BD9DB5A3E92BB7A54E91FCF6F1`，不作为golden门禁。
- 正式包共46个文件，根目录只有唯一`LSR_V2.0.exe`、两份客户Markdown、快速上手、两份配置以及`samples`/`scripts`；内部需求和详细验证计划未放入客户包，37个脚本逐文件与开发源一致。
- 旧正式目录整体备份到`C:\codex_area\LSR_V2.0.pre_req37_38_backup`，正式外置`design_param.cfg.txt`继续保持SHA-256 `AA56A9966830785E3D25A1C8129E9CCDEAD5D908C6E7667E18E9FF4473A53584`。正式目录再次通过三场景差分、三项成品冒烟、版本和清单门禁。
- 最终`LSR_V2.0.exe`大小56,540,058字节，SHA-256为`12A8F4F9E7F4FBBB3C6C2583AB4B9BC3DFB28F104E2264E36B94B801111E0549`，FileVersion/ProductVersion均为2.0；使用独立注释标签`LSR-2.0-requirements-37-38`固定最终源码与发布记录，既有2.0标签不移动。后续新需求编号为39。

## 69. 开发迭代版本与客户正式版本分轨管理

- 用户于2026-08-26明确：`C:\codex_area\LSR_V1.2.0\release`中的开发侧版本号后续必须继续迭代；`C:\codex_area\LSR_V2.0`则是本轮按用户要求交付的正式LSR 2.0目录。
- 后续每个构建任务在开始打包前分别记录“开发迭代版本”和“用户要求的正式版本”。开发侧保留成品使用递增编号，不覆盖既有编号；开发编号增加不自动改变正式目录、正式EXE、运行时版本、窗口标题、Windows版本资源和客户文档。
- 当用户明确要求新的正式版本时，才按该版本统一更新正式版本矩阵并发布到指定目录；若开发构建与正式构建需要不同嵌入版本或文件名，使用独立干净暂存构建并分别验证，不以简单改名伪造版本一致。
- 本节只更新项目Skill、发布基线、当前状态和Work模板，不修改源码、构建产物、数据库或当前`C:\codex_area\LSR_V2.0`正式成品；不占用功能需求39编号。

## 70. 需求39：宽屏寄存器位域图横向自适应Preview

### 70.1 原因与实现边界

- 用户提供的1913×1118与2545×1398截图表明：右下面板在大屏已经变宽，但位域图仍使用固定1000逻辑单位场景；紧凑高度上限与`fitInView(..., KeepAspectRatio)`共同使宽屏由高度限制缩放，图形居中并产生明显左右留白。
- 用户确认高度不需要过度增加，主要使用横向空间显示更多内容。实现保留1000最小场景宽度，在宽视口按场景高度和视口宽高比扩大逻辑宽度；Resize后用30毫秒单次定时器防抖，并按当前Register/BIT重新绘制字段几何、名称省略和引导线。
- 继续使用等比例`fitInView`，没有采用会横向拉伸字体和描边的`IgnoreAspectRatio`，也没有提高缩略图高度上限。BIT取值表仍为主要纵向伸展区域；数据库、规则、保存、Undo/Redo和生成器均无改动。

### 70.2 专项验证与真实失败记录

- Python 3.9语法、右下上下文UI、DPI/舒适度UI、上下分割UI及源码基础烟测通过。响应式专项覆盖1600×900和2545×1398：宽视口场景宽度显著增长，字段条使用超过90%的视口宽度，视图高度没有增加，纵横变换一致，字段标签不减少；需求38分色、Reserved、高亮和连续边界同时通过。
- 为验证真实用户最大化路径，测试只调整窗口尺寸并等待Resize防抖自动重绘，不手动再次调用绘图。首次测试使用当前PySide2不存在的`QTest.qWait()`而失败；改用Qt事件循环和60毫秒单次定时器后，相同产品断言重跑通过，没有修改产品逻辑或降低断言。
- 第一次直接从PowerShell调用GUI子系统Preview时`$LASTEXITCODE`为空，记录为退出码不确定；随后使用隐藏`Start-Process -Wait -PassThru`重新运行，`--smoke-test`和`--shutdown-smoke-test`均明确退出0，日志记录`GUI EXIT code=0`及`CLEAN_SHUTDOWN`。

### 70.3 隔离Preview

- Preview位于`test_output\LSR_V2.0_REQ39_RESPONSIVE_MAP_PREVIEW\package\LSR_V2.0_REQ39_PREVIEW.exe`，大小56,540,325字节，SHA-256为`14FF0CC2EFA0C3902CD7CAE79AAFC68BE1446449FC9727E4B0AA0CE3A4A7F44E`，FileVersion/ProductVersion均为2.0；同目录包含demo试用副本、配置和试用说明。
- demo源文件与试用副本SHA-256均为`B4092710A619B7000FD1A3E8F6CBC6A59D1C80BD9DB5A3E92BB7A54E91FCF6F1`；正式`C:\codex_area\LSR_V2.0\LSR_V2.0.exe`仍为`12A8F4F9E7F4FBBB3C6C2583AB4B9BC3DFB28F104E2264E36B94B801111E0549`。没有覆盖开发侧`release`成品或正式目录。
- 当前仅完成需求39专项、最低安全回归和隔离Preview。用户确认实际效果前不运行完整回归、不更新客户快速上手、不执行正式生成物差分、不覆盖正式目录或建立发布标签；后续新功能编号从40开始。

## 71. 需求39正式发布：宽屏位域图响应式与双轨版本

### 71.1 用户确认、范围与精简回归

- 用户于2026-08-26确认需求39 Preview“效果很好”，要求正式发布V2.0，并明确内部开发版本号持续迭代。本次冻结已确认的横向响应式行为，不改变位域图紧凑高度、BIT取值表纵向优先级、数据库、规则、保存、Undo/Redo或生成器语义。
- 按用户授权简化回归：94个Git受控Python文件语法、118/118项单元/集成及8/8组必要离屏UI通过。UI覆盖需求39/38位域图、DPI/舒适度、上下分割、自适应列宽、保存、撤销/重做、状态和搜索菜单；本次未重复与只读界面布局改动无关的历史性能压力、全部旧UI和15项独立生成器矩阵，需求37/38最近完整回归继续作为业务基线。
- 客户快速上手补充宽屏位域图说明并把文档日期更新为2026-08-26。标准Documents渲染器因本机缺少LibreOffice明确失败，改用任务独占的隐藏Word只读导出和Poppler渲染；15页逐页检查无裁切、重叠、缺字或坏分页，用户既有Word进程未被操作。最终SHA-256为`2DEB5DD9BC53C369B74EB25E9D1421C6A1912F6E21EC9E8735DA112311E05264`。

### 71.2 开发与正式双轨成品

- 开发侧从已提交源码归档建立隔离构建暂存，只在暂存内把运行时、标题、EXE名和Windows版本资源改为1.17.5；`release\LSR_V1.17.5.exe`大小56,454,562字节，SHA-256为`E3203B7640B35BB69AE6180770705EADD0DD0D91833B8C2F1B2872CE9F429F52`，FileVersion/ProductVersion均为1.17.5，基础和关闭烟测退出0。既有开发成品未覆盖。
- 客户正式构建继续使用权威源码中的2.0版本矩阵；正式候选`LSR_V2.0.exe`大小56,540,337字节，SHA-256为`B12E028E4B4E3E46D7986E3C652682E34E1DCA2BF410D93D38526C52207AED58`，FileVersion/ProductVersion均为2.0，OriginalFilename为`LSR_V2.0.exe`。
- 候选和保留原正式外置配置的46文件暂存包均完成外置配置哨兵、包内配置回退和实际目录配置三场景源码/打包生成物差分，全部`PASS/differences=none`；外置配置继续保持SHA-256 `AA56A9966830785E3D25A1C8129E9CCDEAD5D908C6E7667E18E9FF4473A53584`。启动、关闭和损坏库恢复成品入口均退出0。
- 首次候选恢复门禁误用了已修复且`integrity_check=ok`的`release\Bifang_err.db`，成品正确返回“输入意外通过SQLite完整性检查”。保留失败证据后改用原始损坏备份的隔离副本重跑同一产品断言，成功恢复13个Module、236个Register、581个BIT和441条BIT取值；损坏输入与自动备份SHA-256均为`6EA22C03ABD656377926F81E6177A8D2114628C116EAD4EE5573AB7FE44622FE`，重建库SHA-256为`517B42BE130BB18A5749D5C42C1A342493ECDE731B3DEEB825410EF673C49CFA`。这是测试夹具选择问题，不是产品接口兼容或恢复逻辑缺陷，未修改产品代码或降低断言。

### 71.3 正式目录与发布标签

- 第一次尝试整体移动旧正式目录时，Windows因两个用户正在运行的旧`LSR_V2.0`进程拒绝访问；没有终止用户进程。PowerShell的非终止错误使新暂存目录短暂作为`formal_staging`嵌套到旧正式目录，旧EXE仍保持原SHA-256 `12A8F4F9E7F4FBBB3C6C2583AB4B9BC3DFB28F104E2264E36B94B801111E0549`且未被覆盖；检测到46+46文件后立即把完整暂存目录移回测试区，旧正式目录恢复46文件。用户关闭旧版后继续发布。
- 旧正式目录完整复制到`C:\codex_area\LSR_V2.0.pre_req39_backup`，46个文件及旧EXE哈希一致；另把替换时退出的原目录保留在本次`test_output`证据区。新正式目录从客户实际路径再次完成外置配置哨兵、包内配置回退和实际配置三场景差分，全部`PASS/differences=none`。
- 正式路径的基础、关闭和损坏库恢复入口均退出0；FileVersion/ProductVersion为2.0，根目录只有唯一EXE、两份客户Markdown、快速上手、两份配置及`samples`/`scripts`，共46个文件，37个脚本与开发源逐文件一致。正式指南与开发指南SHA-256均为`2DEB5DD9BC53C369B74EB25E9D1421C6A1912F6E21EC9E8735DA112311E05264`，外置配置继续为`AA56A9966830785E3D25A1C8129E9CCDEAD5D908C6E7667E18E9FF4473A53584`。
- 最终`C:\codex_area\LSR_V2.0\LSR_V2.0.exe`大小56,540,337字节，SHA-256为`B12E028E4B4E3E46D7986E3C652682E34E1DCA2BF410D93D38526C52207AED58`。使用独立注释标签`LSR-2.0-requirement-39`固定本次源码与发布记录，既有标签不移动；后续新需求编号为40。

## 72. 开发环境目录治理

### 72.1 用户确认与安全边界

- 用户确认采用`resources`、`artifacts`、`.work`、`LSR_Archive`目标结构，并要求四阶段逐项执行、每阶段严格验收后继续。该工作是工程治理，不占用产品需求40，不改变界面、数据库格式、生成逻辑、正式版本身份或用户数据库。
- 固定开发目录`C:\codex_area\LSR_V1.2.0`、固定Python 3.9.13环境和当前正式目录`C:\codex_area\LSR_V2.0`保持不动。所有递归删除或移动均先验证绝对路径、直接父目录、重解析点和保全清单。

### 72.2 四阶段结果

- 阶段1清理44个可再生成的历史`build_v*`目录，共2,846,481,730字节；固定Python、118/118测试、demo只读完整性和正式EXE哈希门禁通过。
- 阶段2把demo、兼容输入、样例和客户文档迁入`resources`，把V1.17.5/V1.17.4开发EXE收敛为`artifacts\development`的当前/回滚窗口。旧混合`release`在96文件完整清单和三项差异副本保全后清理；旧目录不存在时完成第二次清洁构建和成品启动/关闭烟测。
- 阶段3对旧`test_output`生成不跟随目录联接的2,847文件完整清单，另保留706项可读证据。整体删除因数据库和历史路径风险两次被安全审查拒绝，最终只清理599个重复EXE/PyInstaller文件和3个目录联接，共3,946,678,980字节；保留315.52 MiB数据库、生成物、文档和交付证据。
- 阶段4把维护的Register V1.0.0/LSR V1.1.3数据库夹具放入`resources\compatibility\legacy`，完整历史软件移入`C:\codex_area\LSR_Archive\legacy`。五个过时正式完整备份在清单后清理；`C:\codex_area\LSR_Archive\formal_backups\2.0\LSR_V2.0.pre_req39_backup`为唯一即时回滚目录。

### 72.3 后续迭代规则与验收

- 构建中间文件统一覆盖`.work\build`和`.work\output`；稳定开发EXE只保留当前与上一稳定版。开发迭代号仍单调递增且不能复用已用编号，长期源码回滚由Git提交和标签承担。
- Preview使用`artifacts\previews\REQxxx`，正式完成后清理二进制并保留哈希及必要证据；发布门禁清单和日志进入`artifacts\evidence\REQxxx`。
- 兼容单元9/9、需求18离屏UI、即时回滚成品启动/关闭、阶段后118/118单元与集成均通过；当前正式EXE SHA-256始终保持`B12E028E4B4E3E46D7986E3C652682E34E1DCA2BF410D93D38526C52207AED58`。

## 73. 需求40：寄存器缩略图真实位号分组标尺Preview

### 73.1 用户确认与实现

- 用户否定把16位宽度边界`16`显示为刻度，确认真实BIT编号方案：16位为`15、11、7、3、0`，4/8/16位分别与`3/7/15`位号边界对齐，并在效果图确认后要求制作Preview。
- `_registerMapTicks()`改为按近似等宽视觉分组输出每组最高BIT编号，右端保留BIT 0；4位为`3、0`，8位为`7、3、0`，16位为`15、11、7、3、0`，32位为`31、23、15、7、0`，64位为`63、47、31、15、0`。非标准位宽使用整数分组边界并去重。
- 继续复用原字段权重与未使用区压缩坐标映射；只给刻度线增加测试可观察数据，不改变可见样式、缩略图高度、宽屏响应式、分色、高亮、数据库、规则、保存或生成器逻辑。

### 73.2 Preview专项与边界

- Python 3.9语法、需求40右下详情专项、DPI/舒适度、上下区域、源码基础和关闭烟测通过；16位用户截图同构场景同时断言五个真实位号和0%/25%/50%/75%/100%坐标，4/8/12/32/64位刻度值通过。
- 118/118单元与集成测试通过。demo测试使用临时副本，源文件测试前后SHA-256均为`B4092710A619B7000FD1A3E8F6CBC6A59D1C80BD9DB5A3E92BB7A54E91FCF6F1`。
- 首次执行右下详情专项时漏传必需数据库参数，脚本打印usage并退出1；补充`resources\demo\Bifang.db`参数后，同一完整产品断言退出0。该失败是调用错误，未修改产品或降低断言。
- 隔离试用包位于`artifacts\previews\REQ040\package`；启动文件`LSR_V2.0_REQ40_PREVIEW.exe`为56,505,747字节，SHA-256 `4A70FB9428A00212653097278742A6449A170DD1DF039700AADB52164029C485`，FileVersion/ProductVersion均为2.0，基础与关闭烟测均明确退出0。包内demo副本与项目demo哈希一致。
- 首次组包因`Copy-Item -LiteralPath`不展开通配符而没有复制构建内容，随后按源目录逐项复制并启用强制错误终止，完整41文件试用包成功；没有把非终止PowerShell错误当作成功。PyInstaller保留既有`tzdata`、`pysqlite2`和`MySQLdb`可选模块警告，成品实际烟测通过。
- 本阶段不占用稳定开发迭代号，不更新客户指南，不运行正式生成物差分，不修改`artifacts\development`或`C:\codex_area\LSR_V2.0`。正式EXE、V1.17.5当前开发EXE和V1.17.4回滚EXE哈希均保持原值；按用户要求，需求40确认后也只作为开发源码基线，不进入当前正式基线。
- 用户于2026-08-26确认需求40 Preview效果“可以的”，并明确要求暂不发布正式版本、下一项需求基于本版本演进。因此需求40冻结为已确认开发源码基线，当前正式LSR 2.0仍止于需求39；后续新需求编号为41并从需求40当前分支继续。

## 74. 需求41：规则错误非阻塞编辑与保存Preview

### 74.1 保存前置条件审计与实现

- 源码实现检查点为`3005d45 feat: allow nonblocking rule-error saves`，从需求40已确认源码基线`898e8ab`继续演进。
- 原交互把“待刷新视觉顺序”、连续编址和合法BIT布局串成保存前置条件；Copy后重名/交叠、先改位宽再排序等正常中间态会被单元格提交回滚，而保存又要求先刷新，形成用户描述的编辑—刷新—保存死锁。
- 保存门禁现只保留技术或致命条件：数值文本无法解析为数据库字段类型、SQLite事务/写入失败、目标不可用或不可写、外部修改冲突，以及损坏数据库恢复的备份/原子替换安全门禁。交叠、越界、重名、非正位宽、默认值超宽和派生默认值不一致均由只读规则检查报告，不阻止编辑、`Ctrl+S`或另存为。
- Save/Save As直接持久化当前BIT位置，不隐式调用刷新、不连续编址、不按视觉顺序重排；成功后清除会话中的“建议刷新”状态，并把“保存成功”与“存在规则错误”作为两个独立结论。重新打开后保留错误业务字段并重现规则报告。
- BIT布局不合法时保留最后一个可计算的Register派生默认值；布局恢复合法后再重算。显式刷新仅在全部位宽为正时按当前视觉顺序累计编址，超出Register宽度也保留结果并报告；非正位宽时不伪造累计位置，用户仍可直接保存。
- 新增最低级`BIT_POSITION_GAP` warning，只报告BIT 0到最高已定义位之间的内部空洞，不把最高定义位以上的未使用区误判为空洞，也不因该项自身阻止生成。严重位置、位宽、范围、默认值和派生默认值错误仍保留生成/硬件安全门禁。规则检查同时避免对异常超大位宽构造巨大整数或列表。

### 74.2 测试、真实失败与Preview

- 需求41专项覆盖8位Register中2位`RESERVED0`连续Copy两次、先改位宽、手填带空洞位置、无需刷新直接保存、重开规则重现、非正位宽、越界位置/默认值、严重错误生成阻断、修复后默认值重算、Undo/Redo、可选连续刷新、带待刷新视觉顺序直接另存为，以及需求40标尺`15、11、7、3、0`回归。
- 120/120单元/集成通过；需求11、异常基础、保存流程、后台规则检查、默认地址、默认值修复、右下上下文和状态栏等9组相关离屏UI通过。隔离Preview基础启动和正常关闭均退出0，日志记录`CLEAN_SHUTDOWN`。
- 第一次异常基础回归真实暴露测试夹具在新增/撤销后继续引用已脱离ORM对象，以及旧断言仍要求保存前刷新；改为按稳定名称重新取得对象并按需求41更新旧语义断言后通过，没有吞掉产品异常。第一次用Windows PowerShell 5执行构建时中文快速上手文件名发生编码解析错误，流程在PyInstaller前退出1；改用当前UTF-8 PowerShell后干净构建成功。PyInstaller仅保留既有OpenSSL支持及`tzdata`、`pysqlite2`、`MySQLdb`可选模块警告，打包后实际烟测通过。
- Preview位于`artifacts\previews\REQ041\package\LSR_V2.0_REQ41_PREVIEW.exe`，大小56,506,869字节，SHA-256为`FC1FD14C6248D37578B997DA4171258316113C89FDB0B32587611BDF056E7B1D`，FileVersion/ProductVersion均为2.0；包共41个文件，demo副本与项目demo SHA-256均为`B4092710A619B7000FD1A3E8F6CBC6A59D1C80BD9DB5A3E92BB7A54E91FCF6F1`。
- 本阶段不占用新的稳定开发迭代号，不替换V1.17.5/V1.17.4稳定窗口，不更新客户指南，不运行正式发布生成物差分或完整生成器矩阵，不修改需求40 Preview或`C:\codex_area\LSR_V2.0`。当前只等待用户试用确认；后续新需求编号为42。

### 74.3 Register地址不连续同级warning补充

- 用户确认同一Module内Register位置不连续也应进入规则检查，等级与BIT不连续一致。补充检查点`922d940`新增`REGISTER_ADDRESS_GAP`：规则检查按当前`register_address_step`排序有效且不重复的Register偏移，首地址不是0或后续地址不等于前一地址加固定步长时，在具体Register地址字段报告“警告/P2”。
- 该warning与`BIT_POSITION_GAP`边界相同：不阻止编辑、保存、另存为或因其自身阻止生成；不自动补地址、排序或调用刷新。非法负地址与重复地址继续由既有错误规则单独报告，不用gap warning替代。
- 纯规则检查器保留默认步长4；GUI同步检查和后台检查均显式传入当前数据库配置的固定步长，因此设置为1、2或4时使用对应连续序列。专项覆盖步长4缺少0x8、步长2连续通过、首地址非0、保存后后台报告和重开重现。
- 122/122单元/集成通过；需求41、需求11、保存流程、后台检查、默认地址和状态栏6组最低相关UI通过。Register补充专项首次因新增第二个Register后Save As测试仍比较全库BIT与单Register集合而失败，限定到被测Register后通过；后台检查首次打开计时0.543秒超过门限，独立原样复跑为0.085829秒并通过，未降低性能门限。
- REQ41-PREVIEW-2继续覆盖`artifacts\previews\REQ041\package`同一试用位。EXE大小56,507,254字节，SHA-256为`0347525BEE627ED731DDC1D94A17986EDF646ED4C688269B7E903E16B4D55A08`，FileVersion/ProductVersion均为2.0；41文件包的基础与关闭烟测退出0并记录`CLEAN_SHUTDOWN`。正式目录、稳定开发/回滚成品和需求40 Preview均未修改。

### 74.4 用户步长设置联动与旧结果失效修正

- 用户指出Register地址步长由“设置 → 寄存器参数”选择，连续性检查必须使用该步长，不能与Register位宽绑定。此前把大量告警归因于十六进制并建议固定按`+1`的分析结论错误，现明确撤回；正确序列为步长1：`0x0、0x1、0x2`，步长2：`0x0、0x2、0x4`，步长4：`0x0、0x4、0x8`。
- 原`REGISTER_ADDRESS_GAP`计算本身已经接收当前用户步长且未读取Register位宽；真实缺陷是保存新设置后只重新加载内存参数，没有使已显示规则结果失效或自动复查。正在运行的后台任务携带旧步长，但结果完成时只核对数据库文档修订号，因此可能在设置已变化后继续发布旧结论。
- 修正检查点`845dd79`在步长变化时立即使规则基线和报告失效，并自动排队整库复查；每个后台任务保留启动时步长，完成时同时核对文档修订号和当前步长。旧步长结果不发布、不合并，并由新步长任务替代。默认位宽和每个Register实际8/16/32/64位宽继续完全不参与地址连续性。
- 122/122单元/集成通过；需求41、后台规则检查和状态栏专项通过。专项真实执行“设置保存4→1”、运行中旧步长4任务、排队步长1全量任务，并确认最终只发布新结果；步长2夹具混合8/16/64位Register而不产生gap warning。第一次新专项因结果集合还含既有数据库兼容提示而误判失败，限定到Register业务规则后同一产品断言通过；最终后台链路0.095107秒。
- 用户关闭旧Preview后，REQ41-PREVIEW-3安全覆盖原`artifacts\previews\REQ041\package`。EXE大小56,539,897字节，SHA-256为`C55F68FAEDCCAE31667B16B58D7C054334163089039BFCCCB8F536278139C95C`，FileVersion/ProductVersion均为2.0；41文件包基础/关闭烟测退出0并记录`CLEAN_SHUTDOWN`。正式目录、稳定开发/回滚成品及需求40 Preview未修改。

## 75. 需求42：规则问题确认与Waive管理Preview

### 75.1 用户决策与分级边界

- 用户首先要求把大量已确认Warning Waive，避免真正错误被淹没；随后明确要求Error也可Waive，不应为普通业务规则增加过多硬限制。最终原则是“严重程度与强制门禁分离”：业务Warning/Error均由用户二次确认后决定，数据库崩溃、结构损坏、写入/原子替换失败和外部修改冲突等灾难性技术条件继续强制阻止。
- 用户要求Waive随DB文件流转，使同事打开同一数据库后无需重复确认。因此放弃最初的本机设置方案，新增独立可选`lsr_rule_waiver`扩展表；现有Module/Register/BIT/BIT取值表及字段不变，旧版可按未知扩展表保留或忽略。
- 用户补充规则报告浮窗必须是Windows“最顶层”。报告继续非模态单实例，不阻断主界面编辑，但正常显示时使用`WindowStaysOnTopHint`保持在普通窗口之上。

### 75.2 实现与交互

- 新增`rule_waivers.py`：以严重程度、规则代码、Module/Register/BIT稳定ID、字段、路径和完整说明生成SHA-256指纹。规则对象或内容发生变化即成为新结论，不会被旧Waive误隐藏。
- Waive/取消通过ORM修改当前临时工作副本，进入既有未保存状态并支持Undo/Redo；只有显式保存或另存为后才写入目标DB。保存后的DB复制到新路径仍携带确认记录，不需要旁路文件或同机设置。
- 规则报告使用整行扩展多选，提供`Waive选中项`、`取消Waive`和`显示已Waive项`。业务Error/Warning可混合确认，含Error的弹窗额外说明Waive后不再阻止生成且不代表修复；已Waive默认折叠，状态列区分`待确认`、`已Waive`和不可Waive技术项的`强制阻止`。
- 活动错误/警告计数、模块摘要、状态栏、Verilog规则门禁及保存后通用生成门禁均排除已Waive业务项；原始规则结果和DB确认记录继续保留可见。当前明确不可Waive的规则报告项为损坏库恢复完整性错误；其他写入、目标和冲突门禁位于规则报告之外，原硬阻止保持不变。

### 75.3 验证、真实失败与Preview

- 源码检查点`27ce183`；126/126单元/集成通过。11组相关离屏UI通过：需求42、规则报告与定位、后台刷新、200项报告压力、状态栏、保存、需求41非阻塞保存、需求37损坏恢复、需求18旧库兼容、异常基础和Register默认值异常。
- 需求42专项真实验证Warning+`BIT_OVERLAP` Error混合Waive、确认后默认隐藏/显示、活动状态和生成放行、SQLite损坏完整性项拒绝Waive、显式保存、DB复制后重新打开、取消Waive及Undo/Redo。现有未知扩展表迁移保全单元继续通过，四层业务数据格式未改。
- 首次Qt专项在设置当前行后覆盖扩展选择，调整测试选择顺序后确认产品一次写入两项。随后专项暴露批量删除绕过ORM撤销记录器，产品改为逐行删除后取消Waive与Undo/Redo通过。新增状态列首次挤占既有说明/生成名称弹性宽度，收敛默认列宽后规则报告回归通过。
- 规则定位旧烟测在可编辑Bifang末端层级再次触发已知严格居中夹具边界，按既有维护方案使用1.1.3兼容夹具后通过；未修改定位实现或降低断言。首次直接由PowerShell调用GUI子系统EXE没有可靠退出码，确认无残留后改用隐藏`Start-Process -Wait -PassThru`，同一成品启动/关闭明确退出0。
- REQ42-PREVIEW-1位于`artifacts\previews\REQ042\package\LSR_V2.0_REQ42_PREVIEW.exe`，大小56,546,145字节，SHA-256为`C1AD94175206CC049494803F927DAE1FE9CA7FBE2A9C8D862837DCAA574BAD69`，FileVersion/ProductVersion均为2.0；41文件包附带demo试用副本及操作说明，demo哈希与开发源一致。PyInstaller只保留既有OpenSSL提示和`tzdata`、`pysqlite2`、`MySQLdb`可选模块警告，实际烟测通过并记录`CLEAN_SHUTDOWN`。
- 本阶段没有更新客户快速上手、稳定开发V1.17.5/V1.17.4成品、需求40/41 Preview或正式`C:\codex_area\LSR_V2.0`，也没有运行完整生成器矩阵、正式差分或建立发布标签。正式、稳定当前与稳定回滚EXE哈希分别保持`B12E028E...`、`E3203B76...`和`172DCD83...`；当前等待用户试用确认。

## 76. 需求43：寄存器缩略图精致化视觉整合Preview

### 76.1 用户确认与实现

- 用户先确认Segoe UI字体与字号，再要求软件整体背景保持原深色；颜色经“深青/深蓝”“过亮薄荷绿/亮蓝紫”两轮对比后，最终确认第四版中等明度方案，并要求整合到当前Preview。
- `LSR.py`只调整位域图绘制：确定性色板改为桉树青绿、石板蓝紫及同明度辅助色；Reserved/未使用调整为中性浅灰；标尺和刻度使用冷灰层级。背景、面板、布局、字体、字号、缩略图高度和字段权重均未修改。
- 字段填充改为路径图元：最左和最右外角使用小圆角，内部字段边界保持笔直；填充本身无粗边框，边界由约1像素半透明冷灰线统一绘制。当前字段底色仅轻微提亮，叠加约2像素青蓝外框和轻微内描边，不使用光晕或厚重阴影。
- 需求39宽屏场景宽度、需求40`15、11、7、3、0`标尺、需求41非阻塞保存及需求42 Waive管理均保持；数据库、规则、保存、Undo/Redo、BIT取值和生成器无代码改动。

### 76.2 验证、真实失败与Preview

- Python 3.9语法通过；126/126单元/集成通过。7组离屏UI通过：右下上下文/需求38～40及43、DPI/舒适度、上下布局、需求41、需求42、保存流程和状态栏。实际Qt渲染截图保留为`.work\logs\req43_approved_example.png`。
- 源码基础与关闭烟测最终退出0。第一次源码烟测遗漏任务专用`LSR_LOG_DIRECTORY`，尝试写入被其他实例占用的默认`LSR.log`时真实报`PermissionError`并退出1；未关闭用户实例，改用`.work\logs\req43_source_smoke`后原样重跑通过。
- 第一次PyInstaller构建在解析无权限的用户级Python站点目录时真实报`WinError 5`并退出1，未生成候选EXE；显式设置`PYTHONNOUSERSITE=1`并使用`.work\python_userbase`后清洁构建成功。保留既有OpenSSL支持提示及`tzdata`、`pysqlite2`、`MySQLdb`可选模块警告，候选及最终Preview基础/关闭烟测均退出0。
- REQ43-PREVIEW-1位于`artifacts\previews\REQ043\package\LSR_V2.0_REQ43_PREVIEW.exe`，大小56,546,939字节，SHA-256为`73074658D1065EC23921F378BA776903320F14D9391E41D7065F5C7109D42AF2`；FileVersion/ProductVersion均为2.0，包共41个文件，demo试用副本与开发源SHA-256一致。
- 本阶段没有更新客户快速上手、稳定开发V1.17.5/V1.17.4成品、REQ40/41/42 Preview或正式`C:\codex_area\LSR_V2.0`，没有运行正式差分、完整历史UI或生成器矩阵，也没有建立正式标签；当前等待用户试用确认。

## 77. 需求40～43正式发布LSR 2.0

### 77.1 双轨发布与文档

- 用户于2026-08-27确认需求43效果并明确要求正式发布2.0。需求40真实位号标尺、需求41非阻塞编辑保存、需求42随DB共享Waive/最顶层报告和需求43位域图视觉一并冻结进入正式业务基线；不新增需求编号，后续编号仍为44。
- 开发侧独立构建V1.17.6，FileVersion/ProductVersion、OriginalFilename和窗口标题均为1.17.6；SHA-256为`A4D2AB738A8552BBD139BDBCCF463F6A6972B8678A8727083DA2EFD9666B3BA2`。`artifacts\development`轮换为current V1.17.6、rollback V1.17.5，旧V1.17.4不再占用稳定回滚窗口。
- 客户侧继续使用2.0身份、`LSR_V2.0.exe`和`C:\codex_area\LSR_V2.0`。客户快速上手同步需求40～43及实际保存/刷新/Waive规则，隐藏Word导出和Poppler逐页检查15页无裁切、重叠、缺字或坏分页；可访问性审计High/Medium/Low均为0，DOCX SHA-256为`BA535D3D7DB1917CDC322CA9A0A3EB5D1B0857B88FD5A4A015D41FFF054B0F1D`。

### 77.2 回归与真实测试问题

- 固定Python 3.9编译102个Python文件；126/126单元/集成通过。14组相关离屏UI通过，覆盖需求37恢复、需求38～40及43位域图、DPI/布局、需求41非阻塞保存、需求42 Waive/最顶层报告、规则定位/刷新/压力、默认值异常、保存和状态。
- 第一次需求42专项使用`scripts\dayu_top.db`，因该输入不含测试要求的受控业务问题而失败；换用正式demo临时副本后，同一测试和原断言退出0，未修改产品或降低断言。恢复门禁第一次复用了已被恢复的测试副本而正确报告“输入已通过完整性检查”；重新生成确定性损坏库后，同一候选成品退出0，自动备份哈希与恢复前损坏库精确一致。
- 标准DOCX渲染器因本机未安装LibreOffice明确失败；改用独立隐藏Word实例导出并由Poppler完成相同逐页视觉验收。首个Word导出辅助进程未获得窗口句柄，清理权限不足后经授权只终止该任务自建进程；没有结束用户Word或LSR进程。

### 77.3 正式门禁、切换与回退

- 候选和最终正式路径分别执行`external_sentinel`、`bundled_fallback`、`actual_distribution`三场景生成物差分，全部`PASS/differences=none`；最终路径启动、正常关闭、版本、哈希和39文件白名单通过。快速烟测不初始化会话日志，汇总脚本读取不存在的快速烟测日志时失败一次，但其退出码已为0；关闭烟测日志存在且记录干净退出。
- 正式`LSR_V2.0.exe`大小56,546,932字节，FileVersion/ProductVersion均为2.0，SHA-256为`5EA076808D1DD94A906E0347F85EB566CCBE7597037149A99161FFE9219A8F5C`。用户实际`design_param.cfg.txt`保持SHA-256 `AA56A9966830785E3D25A1C8129E9CCDEAD5D908C6E7667E18E9FF4473A53584`。
- 原需求39正式目录整体归档为`C:\codex_area\LSR_Archive\formal_backups\2.0\LSR_V2.0.pre_req43_backup`，备份EXE SHA-256保持`B12E028E4B4E3E46D7986E3C652682E34E1DCA2BF410D93D38526C52207AED58`。切换后从正式位置重新执行全部正式门禁。
- 两个用户REQ43 Preview进程在发布期间保持运行且未被任务终止；因此Preview二进制清理暂缓，不影响正式目录、正式门禁或回退能力。正式源码以注释标签`LSR-2.0-requirements-40-43`固定，历史标签不移动。

## 78. 需求44/45：全局字体缩放与缩略图等宽字体Preview

### 78.1 实现

- 需求44统一传播当前显示字体到菜单栏、菜单与全部Action、Module/Register/BIT/BIT取值四组操作按钮、状态栏及状态灯、四表横纵表头/行号、页签、弹出控件和已打开对话框。搜索循环提示保留相对正文`+1pt`，但改为每次以当前缩放后的正文重新计算。
- qdarkstyle的`QHeaderView::section { font-size: 13px; }`在载入时精确移除，避免主题样式覆盖动态表头字体；主题切换后重新应用当前显示比例。状态灯图标由固定20像素改为按80%～200%舒适度比例重绘，不改变状态、颜色或点击语义。
- 每屏缩放身份优先读取Windows显示设备ID/注册表设备键；无硬件身份时使用屏幕名称与物理尺寸，不再包含显示器坐标、横竖分辨率或窗口位置。首次发现旧版几何身份键时把原比例迁移到新键，重启和显示器重新排列不再导致局部恢复失败。
- 需求45把右下缩略图字段、Reserved/未使用、选中窄字段外置标签和位号标尺统一为等宽字体，按`Cascadia Mono → Consolas → Courier New → 系统FixedFont`回退；正文字号为当前显示字号90%且下限7pt，标尺保持次级字号。需求39～43的响应式宽度、标尺位置、色板、线条和几何不变。

### 78.2 验证、真实问题与Preview

- Python 3.9语法和`git diff --check`通过；126/126单元/集成通过。需求44/45全局字体启动恢复、DPI、右下缩略图、规则报告、状态栏和搜索等6组相关离屏UI通过；源码基础与关闭烟测均退出0。
- 新专项预置150%屏幕设置后首次构造主界面，逐项断言菜单/Action、四组按钮、状态栏/状态灯、横纵表头和页签同步恢复；再验证运行时缩至100%、已打开搜索框同步、动态`+1pt`提示、主题重载、状态灯尺寸、屏幕坐标/方向/分辨率稳定及旧键迁移。
- 相关规则报告烟测在纵向滚动条已经到边界、无法几何居中的末端Register上触发旧夹具的“必须严格居中”假设；测试修正为只有滚动条未到正确边界时才失败，定位产品逻辑未放宽。一次调用了不存在的`tests\ui_smoke.py`，随后改用实际入口`LSR.py --smoke-test`。第一次关闭烟测误写环境变量名，仍尝试占用用户实例正在使用的默认`LSR.log`并真实报`PermissionError`；改用权威`LSR_LOG_DIRECTORY`隔离后原样通过，未关闭用户实例。
- REQ045 Preview位于`artifacts\previews\REQ045\package\LSR_V2.0_REQ45_PREVIEW.exe`，大小56,549,361字节，SHA-256为`D05E4C0762AABC1086C09A006CDFD4614152991B384DA1F43E26287799C759A7`；FileVersion/ProductVersion均为2.0，41文件包的基础和关闭烟测退出0。
- 本阶段不占用新的稳定开发版本号，不修改V1.17.6/V1.17.5稳定成品、客户指南或`C:\codex_area\LSR_V2.0`，不执行正式生成物差分或建立正式标签。构建和烟测后无REQ045、Python或Pythonw残留；用户原有正式2.0 PID 6140/45208保持运行且未被终止。

## 79. 需求44/45正式发布LSR 2.0

### 79.1 用户确认、双轨版本与文档

- 用户于2026-08-31确认REQ045 Preview使用正常，并明确要求内部版本号继续演进、正式发布客户2.0。需求44全局字体缩放一致性和需求45缩略图等宽字体冻结进入正式业务基线；后续功能需求编号保持46。
- 开发侧独立构建V1.17.7，FileVersion/ProductVersion、OriginalFilename和窗口标题均为1.17.7；大小56,497,837字节，SHA-256为`E08153E41B521C0AB5D61506A174A5A70451F5A9B1ACC2FF54ED517A22F8418A`。稳定窗口轮换为current V1.17.7、rollback V1.17.6，旧V1.17.5退出稳定回滚位。
- 客户侧继续使用2.0身份、`LSR_V2.0.exe`和`C:\codex_area\LSR_V2.0`。客户快速上手补充全局字体同步、重启按显示屏恢复、缩略图等宽字体和约90%相对字号；隐藏Word导出与Poppler逐页检查15页无裁切、重叠、缺字或坏分页，可访问性审计High/Medium/Low均为0。DOCX SHA-256为`5DE27BD358ADB97B65F075E854F6F1D319906363FA6EFDA2D1C7CA15D43E6FF0`。

### 79.2 回归与真实流程问题

- 固定Python 3.9编译105个Git跟踪Python文件，126/126单元/集成和14组核心离屏UI通过；另行通过Register V1.0.0与LSR V1.1.3兼容、5,120寄存器大库编辑/规则检查及15个生成器矩阵。覆盖需求44/45专项以及需求37恢复、需求41非阻塞保存、需求42 Waive/最顶层报告、状态、搜索、布局和默认值异常等相关路径。
- 标准DOCX渲染器因本机没有LibreOffice明确失败；按既有正式流程启动独立隐藏Word实例导出PDF，再由Poppler渲染15页，Word实例正常退出且未碰用户进程。第一次内部V1.17.7关闭烟测误把数据库路径作为第二参数传给只接受`--shutdown-smoke-test`的接口，正确返回1；按实际接口去掉多余参数后同一成品退出0并记录`CLEAN_SHUTDOWN`，未修改产品或降低断言。
- PyInstaller保留既有OpenSSL支持提示及`tzdata`、`pysqlite2`、`MySQLdb`可选模块警告；内部与客户成品的真实启动和正常关闭均退出0。发布期间用户的REQ045 Preview PID 12680/25980保持运行，未被终止。

### 79.3 正式门禁、切换与回退

- 客户候选和最终正式路径分别执行`external_sentinel`、`bundled_fallback`、`actual_distribution`三场景生成物差分，全部`PASS/differences=none`；最终路径FileVersion/ProductVersion均为2.0，启动、正常关闭、窗口标题、39文件白名单和配置保留门禁通过。
- 正式`LSR_V2.0.exe`大小56,572,224字节，SHA-256为`E9B8F30AFDE71C37D06CA8AF4240BFC509742162A86CEDCFBD974DA4CAD20DF4`。用户实际`design_param.cfg.txt`保持SHA-256 `AA56A9966830785E3D25A1C8129E9CCDEAD5D908C6E7667E18E9FF4473A53584`。
- 原需求40～43正式目录完整归档到`C:\codex_area\LSR_Archive\formal_backups\2.0\LSR_V2.0.pre_req45_backup`，备份39文件，旧EXE SHA-256保持`5EA076808D1DD94A906E0347F85EB566CCBE7597037149A99161FFE9219A8F5C`。切换后从最终正式位置重新执行全部正式门禁。
- 正式源码以独立注释标签`LSR-2.0-requirements-44-45`固定，历史标签不移动；最终未留下本任务LSR、Python、Word进程，用户Preview进程保持不变。
