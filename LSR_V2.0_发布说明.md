# LSR 2.0 发布说明

LSR 2.0 是当前寄存器管理软件的正式发布版本，双击根目录中的 `LSR_V2.0.exe` 即可启动，无需另外安装Python。

## 主要功能

1. 大型寄存器库使用本地工作副本和局部刷新，打开、编辑与保存过程保持流畅。
2. 文件采用显式保存、另存为、未保存退出提示及Ctrl+Z/Ctrl+Y撤销重做逻辑。
3. 规则检查支持后台全检、保存后增量检查、Module筛选、最顶层非模态报告和三级定位高亮；业务Warning/Error只报告而不阻止编辑保存，用户可确认后Waive并把记录随数据库共享；可能破坏数据库的灾难性技术错误仍硬性阻止。
4. Module、Register和BIT支持拖动排序、插入、复制、剪切、粘贴及显式自动编址；手工顺序、地址和起始位可直接保存，刷新仅在用户要求连续重编址时执行。Register连续性严格依据设置中的步长检查且与位宽解耦，Register/BIT空洞均为warning。
5. 右下详情区支持长描述编辑、BIT取值原文编辑和紧凑寄存器位域图；标尺按真实位号及4位分组边界显示，普通字段采用与深色主题兼容的中等明度柔和色，Reserved及未使用位域为中性浅灰，并使用细分隔线、小圆角外框和克制的亮青色2像素选中描边。字段名称、Reserved/未使用文字和标尺使用可自动回退的现代等宽字体，正文字号约为全局显示字号的90%。窗口或右下区域横向变宽时，位域图会主动使用新增宽度，高度仍保持紧凑。
6. BIT取值检查兼容十进制、`0x`、`0b`、历史`b`和二进制`x`写法，仅以最低级warning提示异常。
7. `Gen Fuse Map`可按Module生成8位、16位及动态位宽Excel，空片初值固定为全0。
8. 支持Register V1.0.0、LSR V1.1.3及当前数据库继续打开、编辑、保存和生成既有输出；旧库只在临时工作副本中兼容转换，检查结论进入规则报告，原库在打开阶段保持不变。
9. 支持跨屏高DPI自适应及Ctrl+滚轮界面缩放；菜单/Action、四组操作按钮、状态栏和状态灯、横纵表头、页签、弹出控件及已打开对话框统一跟随缩放，重启后按稳定显示设备身份恢复当前屏幕比例。
10. 运行日志只记录当前会话，异常不会通过隐藏错误的方式伪装为成功。
11. 修复SCAN零值、8位TRIM MMR、Verilog Bus端口及UVM数据库名等RTL代码生成问题。
12. Register默认值与BIT组合不一致时先报告并定位到BIT默认值，用户按Enter或提交编辑后才重算，并在状态栏显示旧值到新值。
13. `Run Scripts For Design`只生成`APB_mmr`、`Parm_Addr`和`UVM_Reg_Model`三个必要目录，不再调用Trim等已废弃的Design输出脚本；没有Trim MMR的数据库也可正常执行。
14. `Parm_Addr`同时生成`parm_addr_offset.v`（仅Register偏移地址）和`parm_addr_absolute.v`（Module基地址+Register偏移地址），可按设计集成场景选择。
15. 修复打包EXE读取Design参数文件的路径问题；三个Design生成器优先使用EXE同目录的可编辑`design_param.cfg.txt`，缺失时回退包内配置，不再报`No section: 'design_param'`。
16. C/SVD/Test/Design/DV的Module、Register及内部BIT内容按当前界面顺序稳定生成，不再受旧数据库关系顺序或此前执行菜单的影响；Gen Document的Register顺序跟随界面，BIT表仍按高位到低位排版，APB/DV顶层端口保持2.0兼容。
17. 搜索窗口改为可保持打开的非模态单实例；搜索范围支持多选，默认搜索Module名、Register名、BIT名、BIT取值和其他属性，不默认搜索描述。Enter/Shift+Enter可前后定位，目标已在视野内时只更新高亮，越过最后一项及回到第一项均有醒目提示。
18. SQLite完整性检查失败时可选择“仍然打开”或“取消打开”；恢复模式只读扫描可见数据并持续报告原始错误和保留/丢弃统计。用户确认后按Ctrl+S，软件会先建立逐字节一致的`*.before_lsr_recovery.db`备份，再按当前可见数据重建健康数据库；无法读取或已丢失的数据不会被猜测补齐。

## 发布包内容

- `LSR_V2.0.exe`：唯一启动入口。
- `scripts`：现有代码和文档生成脚本。
- `samples`：大型MCU及规则报告测试样例。
- `LSR_V2.0_发布说明.md`：面向客户的版本功能与交付说明。
- `LSR_开发变更记录.md`：截至2.0的实现与验证记录。
- `design_param.cfg.txt`、`ReMSettings.ini`：运行配置。
- `寄存器工具快速上手_LSR2.0.docx`：客户可编辑Word使用说明，包含软件功能、推荐流程和文件生成方式。

数据库仍使用显式保存逻辑。建议在首次处理重要旧库前保留一份原始备份。

本次需求44/45正式更新执行了与界面改动风险相称的回归：105个Git跟踪Python文件语法、126项单元/集成、14组核心UI、旧库兼容、5,120寄存器大库编辑/规则检查和15个生成器矩阵全部通过，覆盖全局字体传播、启动恢复、屏幕身份、主题切换、状态灯尺寸及缩略图等宽字体。客户快速上手经15页逐页渲染检查无裁切、重叠、缺字或坏分页，可访问性审计High/Medium/Low均为0。

正式`LSR_V2.0.exe`的FileVersion/ProductVersion均为2.0，大小56,572,224字节，SHA-256为`E9B8F30AFDE71C37D06CA8AF4240BFC509742162A86CEDCFBD974DA4CAD20DF4`；候选和最终正式位置三场景生成物差分均为`PASS/differences=none`。
